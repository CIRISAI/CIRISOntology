[package]
name = "holon-ball-game"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
publish = false
description = "Interactive CIRISHolon ball-to-fracturing-wall Rust/WASM gate"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
ciris-sim-core = { path = "../ciris-sim-core" }
