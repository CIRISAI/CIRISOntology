[package]
name = "sphere-demo"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
publish = false
description = "Deterministic particles-on-a-sphere simulation and browser render artifact"

[dependencies]
ciris-sim-core = { path = "../ciris-sim-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
