diff --git a/tnc/Cargo.toml b/tnc/Cargo.toml
index 6956be2..5b8743e 100644
--- a/tnc/Cargo.toml
+++ b/tnc/Cargo.toml
@@ -15 +15,15 @@ rust-version = "1.85"
-cotengra = ["dep:rustengra"]
+default = ["hpc"]
+hpc = [
+    "dep:mpi",
+    "dep:hdf5-metno",
+    "dep:antlr-rust",
+    "dep:kahypar",
+    "dep:rand",
+    "dep:postcard",
+    "dep:genetic_algorithm",
+    "dep:rayon",
+    "dep:ordered-float",
+    "dep:cotengrust",
+]
+cotengra = ["hpc", "dep:rustengra"]
+local-only = []
@@ -18,2 +32,2 @@ cotengra = ["dep:rustengra"]
-mpi = { version = "0.8.2", features = ["complex", "derive"] }
-hdf5-metno = { version = "0.12.4", features = ["complex"] }
+mpi = { version = "0.8.2", features = ["complex", "derive"], optional = true }
+hdf5-metno = { version = "0.12.4", features = ["complex"], optional = true }
@@ -22,3 +36,3 @@ num-complex = { version = "0.4.6", features = ["serde"] }
-rand = "0.9.2"
-antlr-rust = "0.3.0-beta"
-kahypar = { git = "https://github.com/Ectras/rust-kahypar" }
+rand = { version = "0.9.2", optional = true }
+antlr-rust = { version = "0.3.0-beta", optional = true }
+kahypar = { git = "https://github.com/Ectras/rust-kahypar", optional = true }
@@ -27 +41 @@ serde = { version = "1.0.218", features = ["derive"] }
-postcard = { version = "1.1.3", default-features = false, features = [
+postcard = { version = "1.1.3", default-features = false, optional = true, features = [
@@ -34,3 +48,3 @@ ndarray = { version = "0.17.2", features = ["serde", "approx"] }
-genetic_algorithm = { git = "https://github.com/Ectras/genetic-algorithm.git", branch = "f64_score" }
-rayon = "1.11.0"
-ordered-float = "5.3.0"
+genetic_algorithm = { git = "https://github.com/Ectras/genetic-algorithm.git", branch = "f64_score", optional = true }
+rayon = { version = "1.11.0", optional = true }
+ordered-float = { version = "5.3.0", optional = true }
@@ -38 +52 @@ rustengra = { git = "https://github.com/Ectras/rustengra.git", optional = true }
-cotengrust = { git = "https://github.com/Ectras/cotengrust.git", branch = "rust_extension" }
+cotengrust = { git = "https://github.com/Ectras/cotengrust.git", branch = "rust_extension", optional = true }
diff --git a/tnc/src/contractionpath.rs b/tnc/src/contractionpath.rs
index fdca80d..bca104a 100644
--- a/tnc/src/contractionpath.rs
+++ b/tnc/src/contractionpath.rs
@@ -9,0 +10 @@ use crate::utils::traits::{HashMapInsertNew, WithCapacity};
+#[cfg(feature = "hpc")]
@@ -10,0 +12 @@ mod candidates;
+#[cfg(feature = "hpc")]
@@ -11,0 +14 @@ pub mod communication_schemes;
+#[cfg(feature = "hpc")]
@@ -12,0 +16 @@ pub mod contraction_cost;
+#[cfg(feature = "hpc")]
@@ -13,0 +18 @@ pub mod paths;
+#[cfg(feature = "hpc")]
diff --git a/tnc/src/lib.rs b/tnc/src/lib.rs
index 0224c73..65ef428 100644
--- a/tnc/src/lib.rs
+++ b/tnc/src/lib.rs
@@ -5,0 +6 @@
+#[cfg(feature = "hpc")]
@@ -7,0 +9 @@ pub mod contractionpath;
+#[cfg(feature = "hpc")]
@@ -9,0 +12 @@ pub mod gates;
+#[cfg(feature = "hpc")]
@@ -10,0 +14 @@ pub mod io;
+#[cfg(feature = "hpc")]
diff --git a/tnc/src/tensornetwork.rs b/tnc/src/tensornetwork.rs
index a8d1c0b..e60aed0 100644
--- a/tnc/src/tensornetwork.rs
+++ b/tnc/src/tensornetwork.rs
@@ -2,0 +3 @@ pub mod contraction;
+#[cfg(feature = "hpc")]
diff --git a/tnc/src/tensornetwork/tensordata.rs b/tnc/src/tensornetwork/tensordata.rs
index ccfa66b..1321daf 100644
--- a/tnc/src/tensornetwork/tensordata.rs
+++ b/tnc/src/tensornetwork/tensordata.rs
@@ -8,4 +8,3 @@ use serde::{Deserialize, Serialize};
-use crate::{
-    gates::{load_gate, load_gate_adjoint, matrix_adjoint_inplace},
-    io::hdf5::load_data,
-};
+use crate::gates::{load_gate, load_gate_adjoint, matrix_adjoint_inplace};
+#[cfg(feature = "hpc")]
+use crate::io::hdf5::load_data;
@@ -41,3 +40,12 @@ impl TensorData {
-                let mut data = load_data(filename).unwrap();
-                if adjoint {
-                    matrix_adjoint_inplace(&mut data);
+                #[cfg(not(feature = "hpc"))]
+                {
+                    let _ = (filename, adjoint);
+                    panic!("file-backed tensors require TNC's hpc feature")
+                }
+                #[cfg(feature = "hpc")]
+                {
+                    let mut data = load_data(filename).unwrap();
+                    if adjoint {
+                        matrix_adjoint_inplace(&mut data);
+                    }
+                    data
@@ -45 +52,0 @@ impl TensorData {
-                data
