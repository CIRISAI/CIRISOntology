# DESCRIPTOR_CHAIN — the pincer program

**Status:** program specification, synthesized from six tier specs + six adversarial reviews + the meet-in-the-middle analysis. All six tier verdicts were REPAIRABLE; every repair demanded by the reviews is incorporated below or recorded as an open misfit. Nothing in this document rounds up.
**Date:** 2026-08-23. **License:** AGPL-3.0-or-later.
**Ground truth:** [`MISSION.md`](MISSION.md), [`CIRIS_HOLON_ENGINE.md`](CIRIS_HOLON_ENGINE.md), [`MATERIALS_AND_FRACTURE.md`](MATERIALS_AND_FRACTURE.md), `CIRISOntology/Core/Lattice.lean`, `crates/ciris-sim-core/src/material.rs`.

---

## 1. The program in one paragraph

BOTTOM-UP, from the baryon floor to the classical continuum: a descriptor-holon DAG in which each tier exports a small set of invariants upward and certifies the validity domain under which the tier above may consume them — T0 (nuclei) → T1 (electronic structure/bonding) → T2 (crystal) → T3 (grain with defects) → T4 (rock/continuum) — every export a field on a descriptor holon of exactly the `MaterialBinding` pattern already shipping in `material.rs`, every refusal an explicit `GrainFloor` or `RefinementUnavailable`, never a silently wrong number. TOP-DOWN, from special relativity descending to the Newtonian limit (T5), per MISSION.md's realization order. The corrected floor interface is **mass, charge, EXCLUSION** (held today as the Boolean occupancy of `Core/Lattice.lean`, corrected below to the occupation cap), **and nuclear stability**. The meet-in-the-middle criterion: the relativity track's spin-statistics theorem (Pauli 1940, a strictly relativistic result) must discharge the floor's exclusion axiom — bottom-up assumes what top-down proves. Two binding rules govern everything: **(1) GATE PER TIER** — before any tier's effective description licenses the tier above, it is benchmarked against the strongest available reference simulation or dataset for that tier, matching 99.9% of trajectories 99.9% of the time at ≥2x speed; where the literal reading is physically unachievable at a tier, that failure is stated plainly, the honest equivalent gate (observable/distributional match with stated tolerances) is substituted, and the literal-reading failure is recorded as a MISFIT. **(2) MISFIT PROTOCOL** — anything that does not fit the holon model or the gate as stated is not routed around; it is recorded (what does not fit, why, what it implies for the metaphysics) and cashed as a discovery, in the M10/M11 precedent. This document contains 31 deduplicated misfits (§5) and 17 candidate M-numbers; the literal gate fails at four of six tiers, each for a *different* physical reason, and that pattern is itself the program's first discovery (L1/M12).

---

## 2. The corrected floor interface

The bottom of the bottom-up track assumes exactly four things. Stated with the corrections the adversarial round forced:

1. **Mass** m(Z, N, isomer) — per-nuclide, from AME2020 (atomic-mass convention, stated and tested; see T0). Mass is deliberately **not** in the additive ledger: the additive ledger carries (A, Z) exactly; mass = additive constituent sum + a strictly negative whole-only correction (binding), twice-nested — the nucleon's own mass is ~99% QCD binding (2m_u+m_d ≈ 9.0 MeV of 938.27 MeV, PDG/FLAG 2024), and nuclear binding removes up to 0.94% more (8.79 MeV/A at ⁶²Ni).
2. **Charge** Z — exact integer, conserved to measured limits (matter neutrality < 1e-21 e, Bressi et al. PRA 83, 052101 (2011)).
3. **EXCLUSION** — held as the axiom already machine-checked in `Core/Lattice.lean`: Boolean occupancy per direction, 64 local states in 53 (N,P) sectors (`sector_count`, `sector_dims`). Credited as-is, with two corrections that the meet depends on (§4):
   - **What is scale-free is the CAP, not the Boolean.** Antisymmetry's observable content is the occupation-number bound n ≤ 1 per mode (Coleman N-representability). Boolean occupancy is exact only in the mean-field (Slater-determinant) chart; correlated nuclear and electronic states carry fractional natural occupations (~30% depletion in nuclear matter, percent-level in silica). The floor axiom is therefore held as `pauli_cap` (an inequality), with Booleanity the mean-field chart of it.
   - **The mode set is tier-relative.** `Lattice.lean` has 6 FHP directions; nuclear exclusion is Boolean over |nljm⟩ with spin-isospin degeneracy g = 4; electronic exclusion over spin-orbitals. The axiom needs a mode-chart parameter (a Lean object, not prose) before any tier may claim the mechanized object as *its* exclusion — otherwise two instantiations of one principle get counted as one witness (the shared-lemma-one-witness trap, already a paid house lesson).
   - **The cap does not carry the exchange sign.** Hard-core bosons also satisfy n ∈ {0,1} with commuting different-site operators. The sign is invisible to every occupancy marginal — a NonFactoring-shaped datum (M17). The interface either strengthens to carry the sign explicitly or scopes itself to the cap and carries the sign as declared whole-only content. Until the meet closes, every bottom-up certificate carries the line: **EXCLUSION: assumed; discharge owed to the top-down track.**
4. **Nuclear stability** — which (Z, N, isomer) exist, from NUBASE2020, with half-lives, decay modes with **branching ratios** (a single `decay_mode` field cannot represent ⁴⁰K's 89.3% β⁻ / 10.7% EC dual branch), and an environment-assumption frame annotation (EC and bound-state-β lifetimes depend on the electronic environment: ⁷Be ±~1% with chemistry, Ohtsuki et al. PRL 93, 112501 (2004); bare ¹⁸⁷Re 33 yr vs 42 Gyr neutral, Bosch et al. PRL 77, 5190 (1996)).

**What the floor does NOT export** (corrections from the adversarial round): composite statistics is *not* an exportable flag of any tier — atom statistics is (A+Z) mod 2 = N mod 2, not A mod 2 (H-1 and ⁷Li are composite bosons, ⁶Li a composite fermion), and the confirmed-wrong flag is replaced by the Ehrenfest–Oppenheimer **composition rule** re-derived at every composition boundary from total fermion count (M18). Mineral magnetism is *not* a nuclear export (electronic, μ_B, 1836× μ_N — exports route by mechanism, never topic word; L19).

---

## 3. The tiers

Every tier section below is the **repaired** spec: adversarial findings are incorporated, and where a repair is still owed it is marked OWED with its ledger line (§5).

---

### 3.0 T0 — QCD/baryon floor (quarks → nucleons → nuclei)

**Grain:** the nucleon. Lattice QCD is not a per-run evaluator; g0 at T0 IS the nucleon. Below it: `GrainFloor`.

**State.** A population of nuclide descriptor holons, keyed **(Z, N, isomer index)** — NUBASE2020's own key; the original (Z,N) key was refuted by its own witness, since ¹⁸⁰ᵐTa (I=9⁻, t½ > 1e17 yr) and ¹⁸⁰ᵍTa (I=1⁺, 8.15 h) collide on every (Z,N)-derived field and natural Ta is ~0.012% the isomer. The isomer index is whole-only state serving as part of the KEY: whole-only at T0 is not just weighable, it is **individuating** (M21). The population table is declared as the coarse REG+ *view* of a latent holon population (instances materializable on demand) — not a second bookkeeping structure. Descriptor fields:

- `z:u16`, `n:u16`, `isomer:u8`.
- `atomic_mass_u:f64 + mass_unc_u:f64` (AME2020) with a mandatory **convention field**: AME tabulates ATOMIC masses (Z electrons included); nuclear mass = atomic − Z·m_e + B_e(Z). The difference is ~2.7e-4 relative for Si — inside a 1e-3 blanket tolerance yet 4+ orders above the export precision, and the *consumer-semantics check* (which convention T1 receives — atomic, correct for phonons and density) is a named CI gate, because it is the one numerically plausible way this tier corrupts T1 that table-conformance checking is blind to.
- `mass_source: Experimental(AME2020) | Extrapolated(AME2020) | Model(FRDM2012, σ=0.5595 MeV)` — a warrant field, never silent.
- stability from NUBASE2020: half-life, decay modes **with branching ratios**, `env_assumption` carried as a **frame annotation via the Record/warrant machinery**, not an artifact-local field (L16) — the coordinates-vs-frame category error is exactly what the Record axis exists to prevent.
- abundance: **CIAAW interval + specimen provenance**, not a bare `f64` (13+ elements have interval standard weights because abundance varies geologically; δ¹⁸O in granite quartz spans ~+8 to +10‰).
- spin `2I`, parity, magnetic moment μ_I (Stone table), **and electric quadrupole moment Q** (²⁷Al: 146.6 mb — ²⁷Al NMR of the demo granite's feldspar and mica is quadrupole-dominated); all flagged INERT for mechanics. A ⟨r²⟩ charge-radius column is added *scoped*: negligible for bulk mechanics (~1e-8 relative), load-bearing for nuclear-volume (mass-independent) isotope fractionation — the point-charge certificate carries the observable-class qualifier it previously lacked.
- The additive REG+ gross ledger is exactly **(A, Z)** — additive through arbitrary composition, conserved to measured limits (proton τ/B > 2.4e34 yr, Super-K PRD 102, 112011 (2020)).

At engine energy scales the tier is static apart from decay clocks — and "static" is a **rate claim, not a state claim**: real granite self-generates 4–9 MeV alphas (recoil daughters 70–100 keV), (α,n) and spontaneous-fission neutrons, and sits in cosmic-ray flux forever. Flat exclusion clauses are replaced by a **bounded-activity/dose budget per scene horizon** (L17).

**Exports upward.**

| Export | Why it survives / corrections |
|---|---|
| m(Z,N,isomer), AME2020-exact, CIAAW-interval-weighted | Identity preservation below nuclear thresholds; enters upward only as an additive scalar. Fully-additive constituent estimate overprices granite density ~0.9% (9× outside a 1e-3 gate); integer-A×u errs 0.03–0.12% on rock formers. Isotope mass carries real transport physics (isotopically enriched diamond: ~+50% thermal conductivity, Wei et al. PRL 70, 3764 (1993)). |
| Z, exact integer | Z + m determine the entire T1 problem; finite-size corrections scoped by observable class (⟨r²⟩ column). |
| EXCLUSION, as the **cap** (§2), mode-chart parameterized | The only scale-free export; warrant owed to T5. |
| Nuclear stability table + branching + env frame annotation | The alphabet of every tier above; degrades gracefully to a static list + decay clocks under the activity budget. |
| **Radiogenic channel — a typed SKIP EDGE T0 → T3/T4** (L19/M22) | Granite's ~4 ppm U, ~10–13 ppm Th, ~3–4% K yield 2–3 μW/m³ radiogenic heat, He/⁴⁰Ar accumulation, fission tracks (²³⁸U SF branch ~5.4e-7), radon. Needs branching ratios, γ/α/β spectra (ENSDF/DDEP class — data AME/NUBASE cannot represent), and the **neutrino loss term** (~half of every β Q leaves every scene: the ledger at the floor is OPEN, conservation-up-to-a-declared-leak — L7/M14). Factors through T1/T2 not at all: the DAG is a DAG, not a ladder. |
| E–O statistics **composition rule** (not a flag) | See §2; the rule, `composite of n fermions is a fermion iff n odd`, is the Lean obligation; it also repairs the ortho-para H₂ / superfluid He-4 domain inconsistencies. |

**Certificate to T1.** T1 may treat each nucleus as a point mass m(Z,N,isomer) (convention stated), point charge +Ze, fixed identity, with (I, μ_I, Q) inert columns — iff the machine-evaluated scene predicates hold: (1) energy transfer per nucleus < 1 keV, with **two** named exceptions pinned — ²²⁹ᵐTh at 8.4 eV (Tiedau et al. PRL 132, 182501 (2024)) and ²³⁵ᵐU at 76.7 eV (t½ ≈ 26 min, present in the demo granite via 0.72% of its ~4 ppm U; its internal-conversion rate is chemistry-dependent — a second instance of the environment-relative-stability misfit), real margin set by ²⁰¹Hg at 1.565 keV; (2) T < 1e7 K, ρ < 1e9 kg/m³; (3) **activity budget**: declared dose/displacement budget per scene horizon (replaces "no flux" — which excluded the demo material); (4) horizon vs shortest t½, or decays explicitly stepped as ledger transitions with the α (a ⁴He T0 citizen), the electron (T1), and the neutrino (declared leak) all booked, and the decay clock's seed or expected-value treatment declared (determinism, M28); (5) electronic environment inside the EC-validity band (frame annotation). Every domain condition is a named evaluator run by the gate, not prose. `GrainFloor`: sub-nucleon queries; nuclear dynamics. `RefinementUnavailable`: nuclides outside the experimental set when the caller refuses the Model(FRDM-2012) warrant.

**Reference benchmarks.** AME2020 (Huang/Wang/Kondev/Audi/Naimi, Chin. Phys. C 45, 030002/030003 (2021)) — masses, ~1e-9–1e-8 relative for stable species. NUBASE2020 (Kondev et al., Chin. Phys. C 45, 030001 (2021)) — stability, spins, isomers. CODATA 2022 — the u→kg bridge. CIAAW/IUPAC (Meija et al., Pure Appl. Chem. 88, 293 (2016) + updates) — abundance intervals and standard atomic weights. ENSDF/DDEP — decay spectra and branching for the radiogenic skip edge. BMW lattice QCD (Dürr et al., Science 322, 1224 (2008); Borsanyi et al., Science 347, 1452 (2015); FLAG 2024, arXiv:2411.04268) and GFMC/NCSM/coupled-cluster (Carlson et al., RMP 87, 1067 (2015); Hu et al., Nat. Phys. 18, 1196 (2022)) — **reduction-story warrants only**, never per-run references. FRDM-2012 (Möller et al., ADNDT 109–110, 1 (2016)) — flagged fallback, σ=0.5595 MeV.

**Gate, operationalized — and where the literal reading fails.** The literal 99.9/99.9/2x criterion cannot be run at T0, for two reasons that are *not* Lyapunov chaos: (i) no trajectory observable exists at the quantum floor — amplitudes, not paths; (ii) there is no reference simulator to race — lattice QCD costs O(1e7–1e8) core-hours for ~1%-accurate masses, i.e. it is both ~20 orders slower and ~1e6× less accurate than the measurement it would be checked against. **The strongest reference at T0 is MEASUREMENT; the certificate DAG bottoms out in data, not in a deeper simulation** (L4). Recorded as MISFIT (M12 family). The honest gate is a data-conformance audit whose in-band checks are, by construction, nearly unfailable — so it is stated as a *different kind of gate*, whose real failure modes are upstream (wrong pin, wrong convention, wrong key), and it is given teeth accordingly:

- **Leg A (coverage, contribution-weighted):** ≥99.9% of atoms-by-count resolve to Experimental(AME2020) descriptors, **and** coverage is additionally weighted by contribution to each declared macro-observable (0.1%-by-count U+Th carries ~100% of radiogenic heat; count-weighting alone contradicts the engine's own residual-weighted refinement philosophy).
- **Leg B (accuracy) + rollup:** 100% of exported numbers within max(tabulated 1σ, 1e-3) of pinned tables, **plus the falsifiable cross-table rollup** — AME2020 masses ⊗ CIAAW abundances must reproduce CIAAW standard atomic weights (an independent measurement) within stated intervals for all 84 elements with standard weights; this check can actually fire on a transcription, convention, or weighting bug. Pins are cryptographic hashes of the published source files, with pin-to-publication audited as a separate CI step (a wrong pin must not prove a wrong lemma).
- **Leg C:** there is **no speed gate at T0** — only a latency budget derived from the T1 consumer's stepping loop (O(1) descriptor resolution, allocation-free after scene construction, `RuntimeFrontier` discipline). The "beaten by 20 orders" comparison across fidelity classes is retired.
- **Consumer-semantics check:** the atomic-vs-nuclear mass convention handed to T1 is asserted and tested, not footnoted.

`gate_literal_achievable: false`.

---

### 3.1 T1 — Atomic/electronic structure (nuclei + electrons → bonding)

**Grain:** ~1 Å (g0 = Bohr radius). **Palette:** Si, O, Al, K, Na, Ca, Fe **+ H** — hydrogen is mandatory, not optional: water-assisted subcritical cracking (Michalske & Freiman, Nature 295, 511 (1982)) sets real granite's rate- and humidity-dependent fracture energy (subcritical exponents n ~ 15–40; tens-of-percent strength drop dry→wet); biotite and muscovite are hydrous; feldspar hydrolysis is the weathering chemistry. A T1 certificate without H reads "anhydrous vacuum fracture only," in bold, and the fracture engine's floor cannot speak about wet fracture (L29).

**State.** Three holon kinds, no new entity classes. (1) **Element descriptor holons** (8): Z and mass from T0; core/valence partition as a NAMED pseudopotential/PAW dataset hash (SSSP Efficiency 1.3, Prandini et al. npj Comput. Mater. 4, 72 (2018), or PseudoDojo v0.4) — the frozen-core split is itself a certified approximation, so the dataset ID is a warrant coordinate (handled correctly per review: warrant, not a new class); NIST ASD ionization energies (Si 8.1517 eV, O 13.6181 eV, Al 5.9858 eV, Fe 7.9025 eV); electron affinity; radii; Fe spin multiplicity flagged (terminal hole, below). (2) **Bond relation holons**: Si–O D_e ≈ 452 kJ/mol (Luo), r_e = 1.61 Å, k_r ≈ 450–550 N/m (IR ~1080 cm⁻¹), O–Si–O 109.47° stiff vs Si–O–Si ≈ 144° SOFT (the floppy hinge behind silica polymorph near-degeneracy), Born charges Z*_Si ≈ +3.0, Z*_O ≈ −1.5 (DFPT), validity envelope. **The angle terms force relation arity ≥ 3** — `CohesiveBond` is strictly binary; the architecture decision (n-ary relation holons, or angle terms as chart state of a 3-atom parent holon) is made explicitly, not smuggled (L20/M23; first tier where the relation pattern must generalize). (3) **Whole-only state, repaired** (L14/M20): the ≥3-body angle/cooperativity terms are **order-3 Model-coordinate descriptor data** — they factor through 3-site views by construction and do NOT belong in the whole-only slot; whole-only carries the electronic **correlation content** (natural-occupation deviation from Boolean, correlation-energy fraction), honestly labeled as *summary statistics of* a non-factoring state, not the state. Ledger: atom counts per element, N_e, Q, P; exclusion appears as the spin-orbital cap, exact only in the Slater chart (the Boolean-vs-correlation fence is a Lean obligation).

**Exports upward.**

| Export | Why it survives / corrections |
|---|---|
| Bond dissociation energies (Si–O 452 kJ/mol class; full pair table) | BO ground-state energy differences are basis/pseudopotential/code-independent once converged (Delta project: ~1 meV/atom inter-code, Lejaeghere et al. Science 351, aad3000 (2016)). |
| Harmonic force constants + geometry, with a computable validity radius | Second derivatives at a nondegenerate minimum; the Lipschitz/Taylor bound (Lean obligation) makes them a certificate rather than a number. |
| **Third-order IFCs / mode-Grüneisen tensor** (NEW — L29/M25 family) | A harmonic model has α = 0 *exactly*; quartz α_a ≈ 13e-6/K, α_c ≈ 7e-6/K, and quartz–feldspar expansion MISMATCH is granite's thermal-microcracking mechanism. Thermal conductivity and internal friction ride the same anharmonic sector. Gate leg: QHA lattice-vs-T against dilatometry. Without this export three fields of the tier chain's own targets are unreachable. |
| Born charges + polarizability, **with a named dispersion correction** | Long-range Coulomb does not truncate in silicates; PBE without D3/MBD gets layered-silicate (mica) interlayer binding qualitatively wrong — the reference stack names its dispersion treatment or inherits a known-direction bias on the one cleavage energy the crack model needs most. |
| **Surface energies and hetero-interface work of separation** (NEW — L29) | Fracture IS surface creation, and it exits the fixed-topology validity domain: under-coordinated Si, non-bridging O, reconstruction (~1–2 J/m² quartz faces), discontinuous charge shifts at scission. Granite fracture is largely intergranular: quartz–feldspar and feldspar–mica adhesion cannot come from single-phase bond tables. Own DFT slab battery leg. |
| Correlation summary (whole-only, labeled) + polymorph ordering (quartz vs cristobalite ~ +2.8 kJ/mol per SiO₂) | Pairwise-additive potentials measurably fail on polymorph ordering and C_ij (BKS 10–20%, van Beest et al. PRL 64, 1955 (1990)) — the measured pair-ledger insufficiency; the many-body block is Model-coordinate data (see state). |
| Validity envelope as data (gap 8.9 eV, 4→6 coordination ~8–9 GPa, cohesive envelope) | Defines T2's GrainFloor triggers. |

**Certificate to T2.** Licensed iff: (1) the configuration battery passes (below); (2) domain declared on the certificate: BO ground state, |strain| ≤ 5%, P ≤ 2 GPa, fixed topology except through the exported cohesive envelope **and the exported surface/interface data**, no electronic excitation; hydrous chemistry covered only where the H-battery leg ran; Fe-bearing chemistry excluded or carried flagged; (3) error propagation is monotone AND **signed**: functional error is a bias with a sign (PBE softens quartz C_ij), carried per-observable against the experimental anchors, never laundered as a symmetric band (L31); (4) the named undischarged axiom: **EXCLUSION assumed; discharge owed** (§4). Feldspar configurational disorder (Al/Si order–disorder, plagioclase solid solutions, perthite) is an ensemble object no single cell represents — recorded as an idealization limit on the certificate (L29). `GrainFloor`: bond rearrangement beyond the envelope, excitation, coordination change, charged defects → re-materialize electronic-structure children, whose REG+ residency chart (Wannier/localized-orbital children with per-child charge weights) is a declared object, or GrainFloor materializes into nothing defined (L20). `RefinementUnavailable` is **TERMINAL at the floor**: for Fe d-electron multiplets no controlled method exists (DFT+U's U is not ab initio) — the status means "no controlled method," not "children not resident"; the enum's semantics split at the floor and the certificate type at the base is **statistical** (benchmark-calibrated Confidence, not deductive Facts) — both recorded as architecture discoveries (L22/M24). Composed consequence stated plainly: without the Fe and H legs, T1 certifies leucogranite minus water, not granite.

**Reference benchmarks.** QE 7.x / VASP 6, **PBEsol or SCAN — PBE is disqualified as the named reference** because it reverses quartz/cristobalite ordering (Demuth et al. JPCM 11, 3833 (1999)) at the same ~10 meV/atom scale as the battery tolerance (jointly unsatisfiable for a correct descriptor — L31 family); experimental anchors Landolt-Börnstein III/29a, McSkimin 1965, Heyliger–Ogi–Ledbetter 2003 (RUS). CP2K (Kühne et al. JCP 152, 194103 (2020)) AIMD as configuration *source*, not trajectory target. CCSD(T)/CBS fragment rung (~1–4 kJ/mol; NIST CCCBDB). Delta project — the measured floor of "match the reference simulation." NIST-JANAF (ΔfH° α-quartz −910.7 kJ/mol); DFPT phonons (Baroni et al. RMP 73, 515 (2001)). GAP-silica (Erhard et al. npj Comput. Mater. 8, 90 (2022)) — existence proof the honest gate is passable; BKS as the **mandatory negative control**. Materials Project mp-7000.

**Gate, operationalized — and where the literal reading fails.** No trajectories exist at this tier (the reference emits energies/forces/geometries), and manufactured AIMD trajectories decohere in a few ps (condensed-phase Lyapunov 0.1–1 ps) — recorded as MISFIT. The honest gate is the pre-registered configuration battery, with the review's repairs applied:

- **B1** equilibrium cells (quartz + one named feldspar): lattice within 1.5%, C_ij within 2% of the named reference; **B2** ±5% strain grid: |ΔE| ≤ 5 meV/atom; **B3** ≥3,000 AIMD snapshots at 300 K **and 700 K** (the 1000 K leg is dropped — 846 K α–β transition puts it outside the single-minimum descriptor's own envelope): force RMSE ≤ 0.1 eV/Å **plus 99th-percentile per-atom force error gated** (RMSE dilution hides a 1 eV/Å hinge-atom error in 100 easy atoms); **B4** Si–O stretch scan vs the CCSD(T)-calibrated curve within 10 kJ/mol; **B5** Γ phonons within 3%, polymorph ordering sign-correct **against experiment** (the truth anchor is pinned, not the reference functional); **B6 (NEW)** pre-registered crack-tip/notched-slab and surface-scission leg — without it a 3000/3000 pass certifies the elastic regime only, and the certificate must say so; **B7 (NEW)** hydrolytic-attack leg (strained-bridge + H₂O barrier vs high-level reference).
- **Statistics:** rule of three (0 failures in n i.i.d. draws ⇒ rate ≤ 3/n at 95%) with **effective N**, not enumerated N — AIMD frames are correlated on ~ps timescales; block-decorrelate, report τ_int, or draw from independent trajectories (the enumerated-count-is-not-effective-count lesson, already paid).
- **Negative control (power check):** BKS must FAIL B1/B5 before any candidate is graded; a battery too weak to reject the pair ledger certifies nothing.
- **Speed:** ≥2× the cheapest method from a **frozen, named, versioned reference-method list** achieving the same battery scores, at a declared N and cell (DFT is O(N³), MLIPs O(N) — an unpinned N swings the ratio by orders). The trivial reading ("10⁶× faster than DFT" — true of wrong force fields too) is rejected as unfalsifiable.

`gate_literal_achievable: false`.

---

### 3.2 T2 — Crystal / unit cell (α-quartz; K/Na/Ca feldspar; muscovite)

**State (repaired).** Per mineral phase: (1) identity/structure — space group + handedness (P3₁21/P3₂21; **handedness is locally dense** — one 9-atom cell reads the screw sense; it is erased by isotropic *averaging*, a chart limitation, not non-factoring; demoted from whole-only per review), cell parameters (quartz a=4.9134 Å, c=5.4052 Å, Levien–Prewitt–Weidner 1980), density 2648 kg/m³. (2) Composition — feldspar ternary with the coupled substitution Na⁺+Si⁴⁺ ↔ Ca²⁺+Al³⁺ as a conserved charge sector; **Loewenstein Al-avoidance retyped as a SOFT energetic constraint with a measured violation rate** (Al–O–Al penalty of order tens of kJ/mol; ²⁹Si/²⁷Al NMR sees violations) — typing a violable regularity as the ledger's hard-core invariant would corrupt what the ledger means; the discovery is the **exclusion gradient**: exact theorem at the floor, statistical constraint by T2 (L12/M19). Long-range order parameter Q_od (sanidine vs microcline) is the tier's honest whole-only *candidate* — but the claim must first survive the site-resolved control: site-LABELED one-point marginals already distinguish the two (microcline Al ≈ 1.0 on T1(0); sanidine ≈ 0.25 flat), so "agreeing under every partial view" holds only under deliberately site-blind views, and the broken-phase-metastability artifact (house memory) must be excluded before the NonFactoring flag may even be raised (L15-iv). (3) Response tensors, frame convention declared: quartz class 32, exactly 6 constants — C11=86.7–87.3, C33=105.8–107.2, C44=57.2–58.2, C12=6.6–7.0, C13=11.9–12.0, C14=±(17.9–18.1) GPa (Bechmann 1958 Phys. Rev. 110:1060; Heyliger–Ledbetter–Kim 2003 RUS; **IEEE-1949 vs 1978 axis convention flips C14's sign — mandatory field**); muscovite 13 constants (Vaughan & Guggenheim 1986 JGR 91:4657, C11=181 vs C33=58.6 GPa — the 3× ratio IS the sheet); feldspar 21 (Brown–Angel–Ross 2016). Piezo d11=2.31, d14=0.727 pC/N **plus the permittivity tensor (ε11=4.52, ε33=4.64) — without it the piezoelectric constitutive system cannot close at T3** (L29). (4) Cleavage + surface energies as **conditioned functions, typed**: γ(environment) — mica {001} 0.38 J/m² in air vs ~4.5 J/m² vacuum (Obreimoff 1930), quartz conchoidal 0.4–1.0 J/m² (Brace & Walsh 1962) — the condition is a typed axis (Circumstances coordinate or condition-holon relation), or "the environment" becomes an un-modeled ambient, a second entity class by omission (L25). This CONVICTS the scalar `fracture_energy_j_m2` field and CONFIRMS the descriptor prose in MATERIALS_AND_FRACTURE.md — the metaphysics was ahead of the struct. (5) Thermal/phonon: **phonons are a declared bosonic dynamical CHANNEL** — the engine already provides "zero or more simultaneous dynamical channels"; filing a field sector as whole-only would be the junk-drawer route to a second ontology (L14, L28). Dispersion reference Strauch & Dorner 1993 (INS); Θ_D ~ 470 K; α_a(T), α_c(T); the 846 K α–β transition with measured C_IJ(T) softening (Lakshtanov–Sinogeikin–Bass 2007). **Transport summary exported: κ(T) tensor (quartz ~10.7 W/mK ∥c, ~6.2 ⊥c at 300 K), c_p(T) (~740 J/kgK), Grüneisen data** — previously leaked while the tier's own advertised T3 use (thermal-mismatch residual stress) needed them (L29/M11-T2). (6) Defect census gating the certificate: ρ_d, OH ppm, Dauphine twin fraction (the twin op flips C14's sign; 50/50 averages it to zero — a certificate field, not noise), microcrack ε — **with measured provenance required (Record-axis sources) or the certificate is refused, not defaulted**. **Dislocations are typed as LOOP HOLONOMY** — a nonzero Burgers vector measured around a cycle of INTACT relations: whole-borne on loops, invisible to every single-relation view, the carrier of the plasticity/hardness sector, landing on Core/RouteSymmetry's machinery and the maintained-holonomy campaign (only a design-knowing repair holds direction). Typed correctly it is a discovery (M26); untyped it forces a third vocabulary at T2.5.

**Exports upward.**

| Export | Why it survives / corrections |
|---|---|
| Full anisotropic C_ijkl per phase (6/21/13 constants, sources above) | T3 homogenization consumes exactly C-per-grain + orientation distribution; the k→0 phonon slope IS Christoffel elasticity (wavelength gate λ ≥ ~20a). |
| Cleavage geometry + conditioned γ_hkl | T3 crack-path selection needs plane geometry and γ ordering; tolerance repair in the gate below. |
| Griffith floor: G_c ≥ 2γ ~ 1–4 J/m², K_Ic floor ~0.4–0.6 MPa·√m | A thermodynamic lower bound, exported AS a floor: `DEMO_CALIBRATION` (né DEMO_STONE)'s 110 J/m² is visibly 30–100× above it, and the gap is T3/T4 process-zone dissipation, minted above, never claimed here (L23/M25). |
| Density, cell parameters, α_a(T), α_c(T), **κ(T), c_p(T)** | Additive/averaging quantities + the transport summary that closes the thermal-stress problem. |
| Quartz piezo d + ε + handedness, tagged texture-conditional | The SO(3)/random-texture average of any class-32 tensor is centrosymmetric-isotropic (kills d and C14 identically) — so granite's isotropy is a THEOREM ABOUT TEXTURE, not a material property; the isotropic chart carries a random-orientation warrant, and textured rocks (mylonites) void it (L24). |
| Charge-sector bookkeeping + Loewenstein as *soft* constraint with violation rate | Fixes the admissible feldspar composition space; retyped per M19. |
| Third-order/nonlinear refusal, declared | Acoustoelastic stress-dependence of wave speeds is refused explicitly, not silently. |

**Certificate to T3.** Perfect-crystal descriptors license T3 iff: defect gates with measured provenance (ρ_d ≤ 1e12 m⁻², ε ≤ 0.005 via O'Connell–Budiansky, OH < 100 ppm for load above ~600 K, twin fraction declared); thermodynamic window 250–700 K, P < 1 GPa, deviatoric strain < 1e-3; wavelength gate; texture declaration for any isotropic collapse. `GrainFloor`: T → 846 K softening (measured C_IJ(T) or refusal); water at a crack tip (subcritical growth is time-dependent — Atkinson 1984 JGR 89); fields within ~1 nm of a tip. `RefinementUnavailable`: phases with neither a validated potential nor a measured tensor. Single-source rule: where only one complete dataset exists (plagioclase 21 constants = Brown–Angel–Ross 2016 alone), the tolerance floor is the measurement's own error bars, declared.

**Reference benchmarks.** Landolt-Börnstein III/29a + Bass 1995; Bechmann 1958 / Heyliger 2003 (the ~1% spread sets the honest tolerance floor). Strauch & Dorner 1993 INS. Lakshtanov 2007 (C_IJ 295–1000 K). Brown–Angel–Ross 2016; Vaughan & Guggenheim 1986. QE/VASP DFPT (PBEsol/SCAN; LDA disqualified — mis-orders quartz vs stishovite, Hamann PRL 76, 660 (1996)). LAMMPS with silica GAP (Erhard 2022) / Si–O ACE (Erhard et al. Nat. Commun. 15:1927 (2024)) — the only gate-tolerance potentials; BKS/CHIK/ReaxFF as documented-failure controls. Brace & Walsh 1962; Obreimoff 1930; Atkinson 1984 (v–K subcritical data).

**Gate, operationalized — and where the literal reading fails.** Literal trajectory match dies twice at T2: (i) chaos (Lyapunov 0.1–1 ps); (ii) deeper — below Θ_D ~ 470 K the nuclei are quantum, and a classical per-atom trajectory is **not an observable of the system at all**. Recorded as MISFIT. Honest gate, with the review's repairs:

- Observable battery O = {6 C_IJ + density + cell; dispersion on Γ–M–K–Γ–A + Γ Raman/IR; α_a(T), α_c(T) on 250–700 K; conditioned γ for declared faces; sound speeds; **κ(T), c_p(T) via a declared anharmonic method**}. Tolerances pinned to reference spread, never tighter than the references disagree: C_IJ ±2% (Bechmann–Heyliger spread ~1%), phonons RMS ≤ 2%, speeds 1%, γ ±20% **with the caveat recorded that ±20% per leg is ~±40% on the γ ratios that steer T3 crack paths — the crack-path license is degraded accordingly on the certificate**, expansion 5%.
- **Quantum-statistics leg:** thermal observables below Θ_D come from QHA/PIMD, not classical replicas (a correct classical ensemble misses the 250–300 K α band by construction — equipartition). The classical-replica machinery keeps the stationarity/coverage roles above Θ_D with a declared box-size protocol near the 700 K edge (finite-size Dauphiné nucleation false-fires otherwise).
- **Rule-6 leg (out-of-sample forward prediction, per phase):** at least one unfitted prediction — C_IJ(T) at unfitted temperatures, an unfitted polymorph, or QHA α predicted from phonons — because a descriptor storing Landolt-Börnstein numbers "benchmarked" against Landolt-Börnstein is 0% error certifying nothing (circularity repair).
- Ensemble reading: ≥1000 seeded replicas, ≥99.9% land in-band; stationarity: ≥99.9% of 10-ps blocks in-band over ≥10 ns — **with the honest note that the certificate's own GrainFloor processes (subcritical growth, twin-wall motion, OH diffusion) live at seconds-to-years, 10+ orders beyond this window; the stationarity clause cannot detect them and says so.**
- Speed: the ≥2× clause bites only at **certificate-construction time** (MLIP vs DFPT: 10³–10⁵×); reading a stored tensor is O(1) at runtime. This is the general discovery about the 2× clause (L2): it is a statement about *where certificate cost is paid* — construction, not run time — i.e. the certificate-directed-refinement thesis itself.
- **Feldspar and mica have no gate-quality simulator** (M7): the battery is DFT-vs-measured-tensor, and the descriptor enters as *measured*, Record-axis provenance, experiment as the reference class. Stated, not hidden: the "strongest reference simulation" clause has no subject for ~60–70% of the demo stone's modal volume.

`gate_literal_achievable: false`.

---

### 3.3 T3 — Mineral grain with defects

**Reference realization:** MD (LAMMPS, Thompson et al. Comput. Phys. Commun. 271:108171 (2022); HOOMD-blue 4/5, Anderson–Glaser–Glotzer 2020) with DFT-certified ML potentials (GAP/ACE silica, Erhard 2022/2024). Classical BKS is disqualified at crack tips (documented ductility artifacts).

**State (repaired).** Phase identity (demo grain: α-quartz, P3₂21); full trigonal C_ij (**NOT an IsotropicMaterial** — C14 = −18.0 GPa is identically zero in every isotropic tensor; the isotropic chart's validity begins one tier up; fence lemma `no_isotropic_chart`); anisotropic α (13.3/7.1 e-6/K) **and κ, c_p in the consumable set** (previously in state but absent from the certificate — leak repaired); defect census: ρ_d 1e8–1e12 m⁻² (frozen at ambient in dry quartz — glide needs >~600 K + water, Griggs & Blacic 1965), point defects ([Al]/[Si], O vacancy ~5 eV, OH 1–1000 ppm — the strongest environmental lever), twin fractions; **the brittle-only flag gains a third value: no-dislocation-glide-but-twin-reorientation-ACTIVE** — stress-induced Dauphiné twinning operates at room temperature (ferrobielastic switching; Tullis 1970), reorients C14 and d, dissipates energy; Griffith flaw population (Poisson intensities, Pareto tail N(>c) = (c/c0)^(1−p) over [~0.1 μm, 0.1·d]). **Individual/ensemble repair (A1/M28):** the Weibull CDF is a property of the mineral CLASS and lives on the DESCRIPTOR; the subject grain carries ONE quenched flaw realization, seeded at materialization, the draw persisted in the subject's Record — otherwise replay resamples the wall's strength (mission gate 4 violated by an unhoused "aleatoric ensemble" entity). Flaws are **latent relation holons** summarized by the census — the engine's existing latent-children machinery, connected explicitly; refinement to an explicit flaw is exactly the materialization transaction. **Open-system ledger:** species-N conservation is scoped dry/closed, or carries an explicit boundary-flux term — H enters through crack tips on precisely the subcritical timescales the (v0, n) export covers (L7).

**Exports upward.**

| Export | Why it survives / corrections |
|---|---|
| Per-mineral **tensile** strength distributions: Weibull (m, σ0, V0) + size law σ(V)=σ0(V0/V)^(1/m) | Weakest-link min-stability (Fisher–Tippett–Gnedenko): m invariant under composition, σ0 rescales. Real numbers: theoretical ~E/10 ~ 10 GPa (Tromans & Meech 2002); pristine silica fibers 5–14 GPa (Kurkjian); grains 50–300 MPa; bulk granite 6 MPa — the 1e-2–1e-3 knockdown is carried entirely by the flaw census via Griffith. m is a property of the census, not the mineral (silica spans m~3 abraded to >50 pristine); the census travels with the number. **Volume/effective-volume convention attached (see T4).** |
| **Compressive mode: explicitly NOT Weibull** | Weakest-link is a tensile theorem; compressive failure is cooperative wing-crack coalescence (Ashby & Hallam 1986; Nemat-Nasser & Horii 1982) — no extreme-value transport theorem exists. Exported as a coalescence-model family or declared experiment-pinned; DEMO_CALIBRATION's own 95/6 ≈ 16 is the witness Weibull cannot produce (L23). |
| Full anisotropic C_ij + density | Stress-fluctuation MD to 1–2% vs RUS <1%; orientation-averaging at T4 lands in the Voigt–Reuss bracket. |
| γ_s and K_Ic per plane + Charles law (v0, n), n ~ 15–40 | The calibration inputs of T4's cohesive laws — exactly `CohesiveLaw`'s two knobs. **K_Ic's absolute error (MD 20–30% from experiment) is carried upward as a line item** — via Griffith c ~ (K/σ)², a 30% K error is ~70% flaw-size error; passing an MD-agreement gate does not launder the fidelity class (L31). |
| **Grain-boundary / interface constitutive data — owned by RELATION-DESCRIPTOR holons** (NEW, the largest repair) | Granite fails predominantly along boundaries and feldspar cleavage (quartz–feldspar boundary energy, mica basal ~0.3 J/m² class); the quantity controlling the flagship demo factors through NO single-crystal export. It belongs to descriptors FOR RELATIONS — a pattern `CohesiveLaw` already anticipates and the tier ladder previously had no rung for (L20/M23). Today: experiment-pinned. |
| Twin/plasticity thresholds + the three-valued brittle flag | Zeroth-order yield surface with validity box; absence-of-channel is as load-bearing as a value. |
| α, κ, c_p; crack-face friction (Byerlee 0.6–0.85); residual-stress summary | Previously leaked or stored-and-never-exported; now in the consumable set or refused explicitly. |
| Exclusion trace g(r < 0.8 Å) = 0 | **Demoted to a consistency check, not a gate** — an empirical zero cannot distinguish "excluded by physics" from "unsampled," and it would pass an ML potential's documented short-range hole right up until a crack-tip hot spot finds it. The spin-statistics obligation is traced by the certificate text, not by this check. |

**Certificate to T4.** Domain, field by field: T 250–350 K (brittle flag fails first outside); **strain rate split honestly**: MD-derived certificates cover 1e7–1e9 s⁻¹ (a 1-ns run to 10% strain IS 1e8 s⁻¹); laboratory quasi-static 1e-6–1e0 s⁻¹ is covered by the experiment-pinned Charles-law bridge (Atkinson 1984 — 12–14 decades no simulation crosses; **the pin is a warrant VALUE on an ordinary certificate edge — the existing orthogonal warrant/source coordinate — not a new edge species**, per the review's correct kill of the "third edge type" (L21)); the 1e0–1e7 s⁻¹ gap is interpolation the consumer must explicitly accept — **and the demo's ball-on-wall impact (~1e0–1e3 s⁻¹) sits inside that gap: the flagship use case is currently certified by interpolation, stated as the demo's actual status, not buried** (L21). Volume validity [c_min, c_max]; water band (n swings ~2× dry↔wet); below the compressive scale. `GrainFloor`: quantiles outside [1e-3, 1−1e-3] (unsampled tail — refuse, don't extrapolate); d < ~1 μm (distribution FAMILY changes toward deterministic strength); T/water/rate out of box; individual-flaw resolution below the census scale. `RefinementUnavailable`: minerals without fracture-grade certified potentials — **today that includes feldspar (~60% of granite): the DEMO_CALIBRATION T4 descriptor cannot be reached bottom-up today for any field; the honest current diagram has T3 certificates feeding a hypothetical pure-quartzite T4 with the granite demo hanging off declared experiment pins.** Saying so is the misfit protocol (L21).

**Reference benchmarks.** LAMMPS + GAP/ACE (strongest available T3 reference for SiO₂); HOOMD as second correct simulator — whose role includes *measuring the decoherence itself* (divergence saturation in 4–40 ps) and the cross-code distributional floor; RUS elastic ground truth (Bechmann/Heyliger/L-B III/29a/Bass); Atkinson 1984 + Atkinson & Meredith 1987 (K_Ic 0.3–1.0 MPa·√m, n 15–40, granite G_F 40–150 J/m² bracketing 110); Amaral et al. 2008 and Martin & Chandler 1994 (granite Weibull m ~ 5–17); Kurkjian fiber datasets (pristine crossover → GrainFloor); QE/VASP defect handshake (~5% C_ij, ~0.2 eV formation energies) **with the training/validation manifold NAMED** — a crack tip samples strained-bond configurations outside the bulk/surface training manifold, exactly where ML potentials extrapolate silently; a certificate states its manifold, not just its on-manifold accuracy (L5-general).

**Gate, operationalized — and where the literal reading fails.** λ_max ~ 1–10 ps⁻¹ at 300 K: machine-epsilon differences reach thermal amplitude in 4–40 ps; at 1 ns two correct simulators are fully decorrelated; LAMMPS GPU builds are not even self-reproducible run-to-run (atomic-add ordering, documented) — **the reference "trajectory" is an equivalence class before any comparison starts; the gate must be distributional because the strongest reference only SUPPLIES an ensemble** (M12 family; the trajectory is Circumstances-grade frame-local detail, what survives is Facts/Model-grade). Three layers, with the review's repairs:

- **Layer 1 (per-run, relabeled consistency-not-gate):** symplectic shadow-Hamiltonian certificate (Engle–Skeel–Drees JCP 206:432 (2005)): energy drift < 1e-4, momentum < 1e-12/step (the engine's existing residual gate), exclusion trace. These pass wrong potentials; they are necessary hygiene, **plus a planted-error control** so the layer can fire at all.
- **Layer 2 (distributional):** an ensemble per declared thermodynamic ensemble — **each layer names NVE/NVT/NPT; the drift certificates are NVE statements and the T/P bands are NVT/NPT statements; one run cannot satisfy both** (a general pincer requirement discovered here: every gate declares its ensemble per layer). Null shape measured before staking (the 3.29σ band puts a perfect ensemble exactly ON the 1e-3 boundary — thresholds set off the measured null, autocorrelation accounted, no "declared instrument tolerance" escape hatch); KS replaced by tests with a declared i.i.d. sampling unit; no MSD-slope leg (degenerate for a crystal — liquid-state checklist artifact). Weibull export gated on a **CDF distance (Anderson–Darling / quantile band) with stated V0, mode, and stressed-volume convention** — (m, σ0) fit errors are anti-correlated and separate ±10%/±3% gates fail near-identical CDFs. Cost stated up front: ~5e12–5e13 atom-steps at ACE throughput (tens–hundreds of GPU-days); the gate is budgeted or it never runs.
- **Layer 3 (speed), both readings reported, one gated:** like-for-like MD 2× faster than LAMMPS-Kokkos at matched potential/accuracy is NOT claimed. The export-level reading (descriptor route: MD only at flaw tips + weakest-link Monte Carlo, vs brute force) is cleared by ~1e9× — **against a denominator that cannot exist** (a 1-mm³ grain is ~8e19 atoms vs Gordon-Bell ~1e13), so it is not a gate either. The honest statement, recorded per binding rule 1: **T3 has no falsifiable speed gate; the speed clause's status here is "failed-as-a-gate in both readings"; the certified deliverable is the calibrated export set with stated absolute error** (m ±20% at best, limited by flaw metrology).

`gate_literal_achievable: false`.

---

### 3.4 T4 — Rock/continuum (where "stone" becomes stone)

**State (repaired, coherence fixes C1–C4 applied).** Granite-class descriptor: modal composition φ = {qtz 0.30, afs 0.35, plag 0.25, mica 0.10}, Σφ=1; grain-size lognormal (Westerly d50 ≈ 0.75 mm, Lac du Bonnet ≈ 3 mm); porosity 0.005–0.01; microcrack density ε — **stored as a certified SUMMARY of the subject's relation network, not as descriptor property** (the stiffness deficit lives in relations; the descriptor holds crack-free mineral-tier values), and **pinned independently by the modulus-vs-confinement curve (Birch 1960: V_p 4.5–5 km/s ambient → ~6 km/s crack-closed), not fitted to make 45 GPa come out**; imported T3/T2 single-crystal moduli (Bass 1995; McSkimin/Heyliger quartz; Waeselmann 2016 afs; Brown–Angel–Ross 2016 plag; Alexandrov & Ryzhova 1961 biotite); derived crack-free VRH E ∈ [81.0, 84.2] GPa, ν ≈ 0.227. **C1 (FIXED):** dry O'Connell–Budiansky cracking lowers BOTH E and ν (O'Connell & Budiansky 1974, JGR 79:5412), so any dry-cracked descriptor must carry ν_cracked < 0.227 — the old preset pair (45 GPa, 0.24) had ν moving the WRONG way and was never derivable from the dry story. Resolution: the wrong leg was the preset ν — since C4 the preset is a stage calibration making no granite claim, and the T4 descriptor's cracked pair is (E_cracked, ν_cracked) with BOTH values below their crack-free VRH anchors, the crack density ε pinned INDEPENDENTLY by the modulus-vs-confinement route (Birch 1960, JGR 65:1083: V_p 4.5–5 km/s ambient → ~6 km/s crack-closed), never fitted to a preset. The gate tests BOTH E and ν against the O'Connell–Budiansky-consistent pair; a saturated-crack ν requires the water coordinate declared, not smuggled. Strength: volume-tagged Weibull (m ≈ 10±3, σ0 ≈ 6 MPa at V0 ≈ 5e-5 m³ Brazilian) **quoted at the effective volume (stress-volume integral ∫(σ/σ_max)^m dV, not disc volume — else composition upstairs composes wrong volumes)**, **with the export's validity domain attached: granite's measured size-effect exponent (~0.18) is 2× the Weibull prediction (1/m ≈ 0.1) — the plain scaling law is falsified by the spec's own cited data; the correlated-flaw remainder is a measured NonFactoring companion (L15-iii), and the export ships the corrected/bounded law, never the broken invariant unamended.** Hoek–Brown envelope (σ_c = 95 MPa, m_i = 32±3) — **σ_c demoted to a validation output**, arithmetic corrected (C3): the staked closed-crack criterion is R(μ) = σ_c/σ_t = 4·(√(1+μ²)+μ)/(√(1+μ²)−μ) (McClintock & Walsh 1962, Proc. 4th U.S. Nat. Congr. Appl. Mech. 1015–1021, uniaxial form); R(0) = 4, which is NOT Griffith's open-crack ratio of 8 (Griffith 1924) — open-crack Griffith and closed-crack MW at zero friction are DIFFERENT criteria, and the old corollary conflating them is dead, kept in the ledger (§5 dead items); the Byerlee bracket is R(0.6) = 12.5 to R(0.85) = 18.7 (the previously quoted 18.3 was wrong); 95/6 = 15.8 inverts to μ = 0.74, inside the bracket; the compressive/tensile asymmetry is a theorem of crack closure + friction on the relation network (the BPM literature's 4:1 failure with disc bonds and its grain-based fix, Cho–Martin–Sego 2007 / Potyondy 2010, is the field's independent measurement of the same fact). Fracture: K_IC 1.5–2.5 MPa·√m ⇒ G_F 50–130 J/m² (110 preset), ℓ_ch = E·G_F/f_t² = 13.7 cm, process zone 4–7 cm. **C4:** the ground-truth dataset (Lac du Bonnet σ_c = 200±22 MPa, Martin & Chandler 1994) flunks the preset by 2×; the preset is re-warranted to a real specimen record (as `material.rs`'s own doc comment already demands) and the certificate names which granite it certifies. Relations: grain-adjacency `CohesiveBond` network extended with Coulomb friction for the closed state — **with a single declared owner per (D, contact) regime** (A3): the spec previously had a partially damaged closed bond governed by two laws in two places with a switch at D=1. Damage D and `maximum_opening_m` are Record-axis (irreversible). **Distributed damage bound:** D ∈ (0,1) is real stiffness loss invisible to the crack observable {r | D=1}; the elastic export carries either a damage-updated effective modulus or a declared bound (certificate void when mean bond damage exceeds δ). **Whole-only eviction (A2/M20):** specimen strength and percolation state are extreme-value functionals computable from resident children by running the chart — they are certified chart outputs with warrant/Record, NOT whole-only; "does not volume-average" is not "does not factor through children."

**Exports upward.**

| Export | Why it survives / corrections |
|---|---|
| Effective isotropic elastic descriptor (ρ=2650, E ambient/crack-closed, ν, under the random-texture warrant) | Elasticity self-averages (RVE ≈ 15–20·d50, Kanit et al. 2003), provably bracketed Voigt–Reuss/Hashin–Shtrikman — the one T4 scalar export. Aligned-fabric (rift/grain/hardway, 5–20% V_p anisotropy) voids the isotropic chart and is a declared refusal. |
| Volume-tagged Weibull + validity domain + Hoek–Brown envelope | See state; a scalar strength survives only with its volume tag and effective-volume convention. |
| Cohesive pair (G_F, f_t) → per-relation `CohesiveLaw` scaled by tributary area, carrying ℓ_ch | Mesh-objective once the process zone is resolved (h ≤ ℓ_ch/10 ≈ 1.4 cm); structure-preserving onto the shipped struct. |
| Byerlee μ = 0.6–0.85 on closed relations | Scale-free 1e-3–1e2 m; makes σ_c a theorem upstairs instead of a second axiom. Effective-stress caveat: wet scenes need the pore-pressure coordinate or return refusal (L29). |
| Rigid-body certificate below f_t/2 nominal, **with a stress-concentration budget and a damage/Record tag** | Bond strains < f_t/E ≈ 1.3e-4 invisible at T5 tolerance. Repairs: (i) a corner/notch contact at nominal f_t/4 can exceed f_t locally — the screen carries a concentration budget; (ii) **the export is OUR rigid chart over the same holon** — "exports as a Rapier-class rigid body" violated MISSION.md's structural refusal (the wall stopped being a holon exactly when certified healthy); Rapier 0.35 stays the limiting-case CONTROL (A4); (iii) the tuple carries the damage tag — a pristine wall and one pre-loaded to 0.9σ_c previously exported identically: Record dropped at a tier boundary, `repairable_does_not_factor` instantiated at the seam (L18); (iv) granite cyclic fatigue falls to ~50–70% of static — the certificate carries a cycle/duration budget or scopes it out. |
| **Solver damping vs material dissipation, split** (A5) | damping_ratio 0.055 and restitution 0.32 have NO ancestor in any tier export: intrinsic granite Q ~ 10²–10³ ⇒ ζ ~ 5e-4–5e-3 (0.055 is 1–2 orders too dissipative — a solver-stabilization number in material-constant costume); restitution is a pair/velocity/geometry OUTCOME. Split into separately-warranted fields with their own benchmarks (resonant column / ultrasonic Q; restitution-vs-velocity), or the bottom-up pincer provably cannot reach the shipped struct — **a concrete interface falsifier that stands today**. |
| Thermal set (α, κ ~2.5–3.5 W/mK, c_p ~790 J/kgK) + water coordinate | Previously the certificate conditioned on temperature and dryness while the interface carried neither — **a validity predicate may not reference state the interface cannot represent; that mismatch is itself a misfit of the interface** (L29 general). α ~8e-6/K × E=45 GPa ⇒ ~0.36 MPa/K constrained: a 17 K constrained ΔT reaches f_t. |
| Scoped-refusal list, explicit | Chemical weathering kinetics (Palandri–Kharaka 2004), radiogenic/radon (arrives via the T0 skip edge, not through T4), magnetic remanence, dielectric — the interface claim is scoped to the mechanical chart and SAYS so. |

**Certificate to T5-Newton.** Specimen ≥ 20·d50; stress inside the envelope with declared margin and concentration budget; strain rate < ~1 s⁻¹ (dynamic-increase-factor onset ~1e0–1e1 s⁻¹, Zhang & Zhao 2014 — the 18 m/s demo throw produces ~1 s⁻¹ contact rates: borderline, declared); T < 200 °C with the 573 °C α–β wall; dry/short-duration unless the subcritical law is added; single-G_F claims restricted to cracks longer than the process zone (~5 cm) — cm-scale chips are on the rising R-curve, outside the single-G_F chart; scalar strength carries its volume tag. `GrainFloor`: sub-d50 nucleation/crack-path queries (the continuum chart discarded the realization — `not_computable_from` in this tier's clothes); sub-RVE specimens; **and the current fixed 288-node wall frontier, which spaces nodes coarser than ℓ_ch/10 on a meter wall: the demo's declared constants force crack-tip refinement finer than its own frozen frontier, so the gate returns GrainFloor on the crack path — honestly — until the adaptive crack-tip step (already named as next in MATERIALS_AND_FRACTURE.md) lands. The adaptive step is demanded by the numbers already in DEMO_CALIBRATION, not optional polish (C2).** `RefinementUnavailable` (forced T5→T4 re-entry): contact stress exits the certified envelope mid-run.

**Reference benchmarks.** Akantu (EPFL cohesive FEM; Camacho & Ortiz 1996 lineage) and/or phase-field (Bourdin–Francfort–Marigo 2000; Miehe 2010, MOOSE) — regimes A/B; Irazu/Y-Geo FDEM (Mahabadi et al. 2012; Lisjak & Grasselli 2014) and PFC BPM calibrated to Lac du Bonnet (Potyondy & Cundall 2004) — regimes B/C; laboratory ground truth: Martin & Chandler 1994, Brace–Paulding–Scholz 1966, Lockner et al. 1991 (Nature 350:39 AE fault growth), Atkinson & Meredith 1987, Birch 1960; Bass 1995 + Hill 1952 + Hashin–Shtrikman 1963 as analytic homogenization controls; Rapier 0.35.2 (f64) as the rigid-limit control via the existing SPHERE_RAPIER machinery — **whose precedent covers exactly what it validated (24 checkpoints, three-way 99.9%, 4.47–4.55×) and NOT friction, rotation, or stacking, which it scopes out; the precedent is not overreached**.

**Gate, operationalized — and where the literal reading fails/holds.** Split by regime; the literal reading is ACHIEVABLE in regimes A and D, fails in B (cross-code) and C — from **quenched-disorder bifurcation sensitivity, not chaos** (the crack path is an extreme-value functional of the flaw field; inter-code peak-load scatter 2–5%): recorded as MISFIT with the honest corridor/distributional equivalent. Repairs applied:

- **Regime A (intact continuum):** nodal displacement fields at 24 synchronized checkpoints vs converged FEM/analytic (Hertz/Boussinesq); agreement normalized by **local per-checkpoint scales, not the global maximum** (global normalization buys slack for O(1) near-tip errors); **plus a stress-field agreement stitch check** — displacement agreement does not bound stress agreement, and regimes B/C initiate from stress. PASS ≥0.999 at ≥99.9% of checkpoints. Literal: runnable today.
- **Regime B (single dominant crack, quenched disorder fixed):** same-seed self-consistency across refinement levels is **convergence, not validation** (nested refinement inherits lattice-direction bias) — cross-code validation is mandatory, on **benchmarks with nontrivial crack paths**: Nooru-Mohamed mixed-mode, L-shaped panel, Lockner-style AE localization (symmetric Brazilian discs are symmetry-forced and have no discriminating power; phase-field references import no discrete flaw seed). Peak load ±1%, work of fracture ±1%, path corridor Hausdorff ≤ max(grain diameter, one cohesive element) — the 0.1%→1% relaxation is forced by the measured 2–5% inter-code floor and recorded as MISFIT.
- **Regime C (fragmentation):** impulse ±0.1% is vacuous alone (any momentum-conserving integrator) — gated alongside dissipated energy ±1%, fragment-mass CDF within the reference's seed-to-seed scatter, **and largest-fragment mass + fragment count as separately gated observables** (the KS tail-blindness repair). Grady 1982 as analytic cross-check.
- **Regime D (rigid limit):** the existing Rapier solution gate, scope-honest.
- **Ensemble claim honesty:** certifying ≥99.9% pass at 95% confidence needs ≥2,996 zero-failure runs (rule of three) — computationally implausible with converged-FEM references in the loop; the certificate therefore states **which** branch ran (the 15-combination sweep or the certified-rate branch), and the headline never advertises the strong branch when the weak one ran.
- **Speed:** ≥2× wall-clock at the same certified tolerance on the same host, construction excluded — with two honesty clauses: the comparator is **the fastest simulator that also passes the same certified tolerance** (e.g. a calibrated FDEM/BPM), not a mesh-converged research code that any coarse model beats; and adaptive crack-tip materialization is **runtime work, never migrated into the excluded construction bucket**. Expected mechanism: certificate-directed refinement (288 resident nodes carrying 1e6 constituents, refining only the crack tip), precedent 4.47–4.55×.

`gate_literal_achievable: true` (regimes A, D; corridor/distributional in B cross-code and C, recorded).

---

### 3.5 T5 — Top-down: special relativity descending to the Newtonian limit

**State (repaired).** All SI, frame annotations explicit. **Chart descriptor** (forced by simultaneity): chart_id, signature (+,−,−,−), foliation for ledger snapshots — **flagged as a live 12+2 risk**: the taxonomy is proved closed at 11+1 with exactly ONE frame-relation (Record; `record_not_site_generated`), so the chart coordinate must either be mechanized as an aspect of Record (`chart_is_record`-shaped theorem) or located inside the eleven (Circumstances), or explicitly refused — until then it is a candidate second frame axis riding beside the ledger (L8/M15). Per subject holon: x^μ, p^μ, m0 (CODATA 2022), q = integer × e (exact), spin flag (read by the meet certificate, not the flat integrator), proper time τ. Mass shell ⟨p,p⟩ = (m0c)² — **demoted from certificate observable to representation invariant**: in the (x, γv) representation every named pusher uses, the residual is an algebraic identity satisfied by arbitrarily wrong trajectories; it re-enters as a certificate only if p⁰ is evolved independently. Gross ledger: additive P^μ (linear — Λ(Σp) = ΣΛp), Q (unconditionally conserved), **N with a validity domain — a domain-indexed conservation law, a genuinely different algebraic object from `SectorPreserving`'s unconditional shape, mechanized as such (M14); the threshold is process/frame-dependent (Bethe–Heitler vs two-photon), so the flag carries its process class.** The 53-sector table survives IN-CHART by relabeling ((N,P) → P^μ injective on the 64 states, single-speed gas; `sector_table_is_pmu_table`) — and the chart's non-innocence is a theorem: no relativistic velocity addition preserves a finite single-speed set with |v| < c (the retyped `no_lattice_boost` — the original O(2,1)-on-spatial-vectors draft did not typecheck; the honest statement is about velocity space). **Whole-only repair (L9/M16):** momentum-in-flight on force-mediating relations is the RELATION HOLON'S GROSS ledger entry — conservation-bearing state cannot be whole-only or P^μ additivity fails during every retarded interaction; whole-only keeps only what no partial view recovers. This is the CJS no-interaction theorem (Currie–Jordan–Sudarshan, RMP 35, 350 (1963)) cashed: **relations are never free — they rent ledger space and leave receipts — proved BY THEOREM at this tier**, the ledger metaphysics' strongest independent corroboration in the pincer. Composite invariant mass M ≥ Σm_i is gross-derived (reverse triangle inequality) — with the von Laue caveat recorded: an OPEN stressed subsystem's foliation-summed 4-momentum is not a 4-vector (the 4/3 problem); for granite at 6 MPa the effect is ~4e-22 relative — record and bound (L8). Relativistic temperature has no agreed transformation law (Planck–Einstein vs Ott) — flagged now, cheaply, before the thermal channel lands on the ledger.

**Exports.**

| Export | Why it survives / corrections |
|---|---|
| Mass-shell invariant (representation invariant; certificate only with independent p⁰) | Lorentz scalar, per-holon identity. |
| Additive P^μ ledger (closed systems), commuting exactly with every frame change | Theorem-shaped generalization of `Lattice.lean`'s P. |
| M² = ⟨ΣP, ΣP⟩ with M ≥ Σm_i, von-Laue-scoped to closed systems | Mass defect without a new entity class; AME2020 binding is the floor's face of the same identity. |
| Q unconditional; N domain-indexed (M14) | Owned vs rented ledger entries. |
| Mandelstam s, t, u on interaction EVENTS — **entering the scene as Record entries on existing holons, never as new pointlike entities** | Scalars attached to events; **scoped: bound states cannot ride them** — s,t,u are asymptotic S-matrix labels; an elastic modulus is ∂²E/∂config² and no T5 export carries energy-vs-configuration; stone's constitution reaches the upper tiers through the bottom-up track, not through this interface (L29). |
| Light-cone partial order | The chart-free causality primitive for scheduling, snapshots, Record. |
| Newtonian chart with certified O(β²) remainder | The load-bearing control (MISSION.md); explicit bound \|x − at²/2\| ≤ a³t⁴/(8c²). **Scope repair — the gravity misfit (L29, largest T5 miss):** flat SR contains no g; Newtonian gravitation is the weak-field limit of the DEFERRED curved tier, CJS bans the instantaneous 1/r² force exactly as it bans contact springs, and the flat-space rescue (Nordström) is empirically dead. What this tier exports is the **force-free/EM Newtonian chart**. Weight arrives only when the curved tier lands or via an exported Rindler/uniform-g chart with the equivalence principle CITED as an imported premise (a GR axiom, not an SR theorem). Until that decision node (Gantt A3) closes, **the top-down jaw stops short of the engine's own demo scene, and says so.** |
| O(β²) correction Hamiltonian (mass-velocity, Darwin, spin-orbit with the Thomas ½) for tiers that must KEEP relativity | The interface is bidirectional where chemistry needs it (fine structure, heavy-element chemistry at β ~ Zα); previously the tier only licensed discarding. |
| Spin-statistics deliverable, honestly labeled | **Discharged BY PAPER, witnessed BY MACHINE** — see §4. The Bose half (commutation, unbounded occupancy — phonons, photons, Debye c_p, dielectric response) is exported alongside as the bosonic channel's warrant; the fermionic half alone cannot serve the tiers above (L28). Maxwell/quasi-static EM constitution is a missing field-sector export with a named hazard: the Galilean limit of Maxwell is not unique (Le Bellac & Lévy-Leblond 1973 — electric vs magnetic limits); the certificate that eventually exports it must select. |

**Certificate.** Upward (flat→curved): |Φ|/c² < tol (Earth surface 6.95e-10), tidal budget; curvature-requiring observables → `RefinementUnavailable`. Downward (SR→Newton): error O(β²) + retardation O(L/(cT)) + energy-domain flag; β < 0.0447 certifies 0.1% momentum fidelity; the measured convergence **coefficient and exponent** both staked (below). **β-grain-floor, arithmetic corrected:** β² < 4.4e-16 means v < **6.3 m/s** (not 6 km/s — the original was off 10³ in v and would have grain-floored away ~1e6-ulp corrections); the demo 18 m/s throw (β = 6.0e-8, correction 1.8e-15 ≈ 8 ulps) is formally resolvable per step and unresolvable after rounding accumulates — the tier returns the Newtonian chart WITH a floor certificate. **GrainFloor arriving from ARITHMETIC — two charts certifiably indistinguishable at working precision — is a new, physics-imposed instance of the engine's existing mechanism (M27).** CJS-floor: a covariant-force query with no field channel on the relation → `RefinementUnavailable` by theorem. N-flag: above threshold → `RefinementUnavailable` pointing at the quantum tier — and since AME-scale physics is MeV-scale, **the certificate chain to the floor is carried only by the paper spin-statistics citation, never by a runnable T5 gate; stated in the certificate, not left implicit.**

**Reference benchmarks.** Closed-form kinematics suite (constant-B gyration ω = qB/(γm); hyperbolic motion; E×B drift-frame; relativistic oscillator via elliptic integrals) — the one family where the literal gate is EXACTLY checkable, references without error bars. PDG 2024 kinematics (p*, s+t+u = Σm², CM/lab boosts) — **with the cancellation-safe factorization [M−(m1+m2)][M+(m1+m2)] pinned** (the naive form loses all digits near threshold; a correct module would otherwise fail the 1e-12 gate on part of any near-threshold ensemble). CODATA 2022/SI exact. WarpX / Smilei pushers (Boris 1970; Vay 2008; Higuera–Cary 2017 — the E×B high-γ discriminator). In-house Newtonian-limit convergence experiment with Rapier 0.35 as the β→0 control (MISSION.md gate 7) — **with the ulp-agreement claim scoped to SR-chart-vs-Newton-chart inside ONE stepper on force-free/uniform-field segments; the Rapier comparison carries an integrator-tolerance budget, never an ulp bar** (two different engines never agree to ulps on contact scenes; the two-fp32-runtimes lesson, already paid).

**Gate, operationalized — and where the literal reading fails/holds.** Literal: ACHIEVABLE on the integrable closed-form sector; FAILS on the chaotic subset (driven/three-body — any two correct codes diverge); recorded. Repairs applied:

- **Trajectory clause:** 10,000 pre-registered ICs per family, **dt PINNED per family, gated on the error ENVELOPE at fixed dt, with a planted-error control** (an unpinned dt passes trivially by shrinking cost-free, or fails a correct second-order integrator on secular phase drift — which gates scene duration, not correctness). Match: max(|dx|/L_ref, |dp|/p_ref) < 1e-9 per step against the closed form at the pinned dt.
- **Limit-order clause:** log-log slope over β ∈ [1e-3, 0.3] staked at 2.000 ± 0.05 **and the COEFFICIENT staked at 1/2 (momentum observable) with the named remainder constant a³t⁴/(8c²)** — an exponent-only gate passes wrong-constant implementations; the pump-law precedent (coefficient confirmed to 3e-4) is the house bar; the ruler is gauged with planted wrong coefficients before the band is staked.
- **Chaotic sector:** distributional gate **pre-registered with named observables, tolerances, ensemble sizes** (per-step invariant drift: closed-system P^μ < 1e-12 relative; ensemble-observable match) — discipline rule 1; as previously written it was unrunnable.
- **Speed clause, decoupling recorded:** the binding rule benchmarks accuracy and speed against the SAME strongest reference; at T5 accuracy is matched against closed forms (free — no simulator to race) while speed can only be compared to a named kernel. That decoupling is **itself recorded as a misfit against binding rule 1**. Reading (a), intra-pincer: the licensed Newtonian chart must step ≥2× faster than full SR at equal tolerance — falsifiable (γ overhead is only ~1.5–2.5×), and **failure blocks the license** (a gate whose failure has no consequence is logging); anti-gaming clause: the SR comparator is the tier's own best implementation, benchmarked. Reading (b), external parity-within-2× of WarpX's Higuera–Cary kernel — only on a **workload-matched** comparison (a field gather against a nontrivial field included; uniform-field scenes skip the gather that dominates WarpX's loop and prove nothing), else dropped.

`gate_literal_achievable: true` (integrable sector; chaotic subset distributional, recorded).

---

## 4. The meet in the middle

### 4.1 MEET-1: does spin-statistics discharge the floor's exclusion axiom?

**Verdict: NO, not as originally stated — and the repaired criterion is stronger than the broken one.** The entailment runs the right direction (the pincer is not circular), but the deliverable as first drafted discharged a weaker statement than the bottom-up track consumes, mechanized it by assuming its own conclusion, and attached it to the floor's Lean object through an undeclared chart. Five separable gaps:

1. **Content (equivocation).** Three strictly nested statements: antisymmetry of the field ⊋ occupation cap n ≤ 1 (Coleman N-representability) ⊋ Boolean occupancy in the Slater chart (`occ : Fin 64 → Fin 6 → Bool`). Pauli 1940 proves the first; the floor axiom states the third; **hard-core bosons witness the strictness** (Boolean occupancy, commuting different-site operators, no Pauli). The bottom-up track consumes the FIRST (Slater determinants, Fermi–Dirac, Hund's rules, E–O all need the exchange sign), which is invisible to every occupancy marginal. As drafted, top-down proved more than the interface carried and bottom-up assumed more than the interface stated — the pincer closed only verbally.
2. **Mechanization (circularity).** The CAR brick takes anticommutation as an INPUT and derives Boolean spectrum — the theorem's conclusion, not its derivation from covariance. No proof assistant holds Wightman axiomatics. CI must never show green on "relativity implies exclusion."
3. **Chart (mode map).** Pauli lives in continuum Minkowski QFT; `Lattice.lean` is a preferred-frame finite chart (no boost preserves a single-speed set), and the mode set is tier-relative (6 FHP vs |nljm⟩⊗g=4). The mode-chart parameter must be a Lean object before the discharge lands on THIS axiom rather than a sibling instantiation.
4. **Exactness (observable).** What is empirically exact is the cap; correlated states have fractional natural occupations. The CI-enforceable target is the inequality (`pauli_cap`), not Booleanity.
5. **Composition (chaining).** Even granting fundamental-fermion exclusion, tiers above need composite statistics, and the original T0 export was factually wrong (atom statistics = N mod 2, not A mod 2). Statistics is re-derived at every composition boundary (E–O); the chain must include that link.

**The repaired meet criterion — five links, each with a named warrant class:**

| Link | Statement | Warrant |
|---|---|---|
| (i) | Lorentz invariance + microcausality + spectrum condition + d ≥ 3 ⟹ half-integer-spin fields anticommute | **PAPER, permanently** (Pauli, Phys. Rev. 58, 716 (1940); Streater–Wightman Thm 4-10; Guido–Longo, CMP 172, 517 (1995)); d ≥ 3 / π₁ = S_n premise cited (Laidlaw–DeWitt 1971; Leinaas–Myrheim 1977) |
| (ii) | anticommutation ⟹ n ≤ 1 per mode | **MACHINE** — `pauli_cap` |
| (iii) | cap + single-determinant chart ⟺ Boolean occupancy; deviation = correlation weight | **MACHINE** — Boolean-vs-correlation fence |
| (iv) | mode-chart parameter identifying \|nljm⟩⊗g and 6-FHP as instantiations of one hard-core shape | **MACHINE** — degeneracy-parameterized `sector_count` |
| (v) | E–O composition: composite of n fermions is a fermion iff n odd; statistics re-derived per boundary from TOTAL fermion count | **MACHINE** — block-exchange permutation sign |

Plus the interface decision: the floor either **strengthens** to carry the exchange sign explicitly, or **scopes** itself to the cap with the sign carried as a declared whole-only NonFactoring datum (itself a discovery — M17). The CAR/S_n shadows are relabeled **consistency witnesses** (the 64-state space IS a 6-mode fermionic Fock basis with N and P matching — real and valuable, correctly named); the discharge line on every bottom-up certificate reads **"EXCLUSION: assumed; discharged BY PAPER, witnessed BY MACHINE"** until all five links are certified with these labels. That is the correct honest state, not a failure.

**Current gap, plainly:** links (ii), (iii), (v) are cheap Lean work with no blockers; link (iv) is the sector-table generalization; link (i) stays a citation-certificate indefinitely unless someone formalizes axiomatic QFT. MEET-1 is the cheapest keystone in the whole program and can close early (§6).

### 4.2 MEET-2: does T4 meet the existing Newton slice without a second ontology?

**Verdict: CONDITIONAL YES.** Every pressure point found by the six reviews resolves into a slot the engine already has (descriptor holon, relation holon, Record axis, warrant coordinate, dynamical channel, GrainFloor/RefinementUnavailable, latent children). No second entity class is REQUIRED — but three specs were caught actively misusing the whole-only slot (the erosion route to a second ontology), and the meet holds only under the amendments now baked into §3:

- **A1** Quenched-realization rule (descriptor law / subject draw / Record persistence) — mission gate 4.
- **A2** Whole-only eviction — the slot holds only NonFactoring-grade content (M20).
- **A3** Single friction owner per (D, contact-state) regime.
- **A4** Rigid export is OUR chart with a Record/damage tag; Rapier is the control.
- **A5** Solver damping / material dissipation split — the standing interface falsifier until it lands.
- **C1–C4** Coherence fixes (ν direction, ℓ_ch vs frontier, McClintock–Walsh constants, preset re-warrant) — the meet is dishonest until they land.

**The one genuine architectural extension:** n-ary relations (Si–O–Si forces arity 3) and relation-descriptor holons (grain boundaries) — a real, explicit generalization decision, not a smuggle (M23).

**Field-by-field reachability of `IsotropicMaterial` from the bottom-up track, stated plainly:** density — reachable (T0 masses + CIAAW). E, ν — reachable via VRH + measured ε, after C1. tensile f_t — reachable only as volume-tagged Weibull (A1). compressive — reachable as a THEOREM of (f_t, μ) on closed relations once A3 lands, else experiment-pinned. G_F — reachable as a tier-local value with exported Griffith floor. damping_ratio, restitution — **NOT reachable; orphans (A5)**. Six of eight fields conditionally reachable; two falsified as material constants.

---

## 5. The misfit ledger

Deduplicated across all twelve documents. CASH column: **M12–M28** = candidate M-numbers (M10/M11 pattern; Eric confirms numbering), **LEAN** = brick without a new number, **REPAIR** = interface/gate engineering (done in §3 or owed), **HOUSE** = a paid house lesson recurring. All tier verdicts were REPAIRABLE; nothing here is fatal. Misfits are discoveries; the fired kills are reported as plainly as the survivals.

### Family I — the gate criterion itself

| # | Misfit | Metaphysical reading | Cash |
|---|---|---|---|
| L1 | The literal 99.9/99.9/2x gate is tier-relative, failing for a DIFFERENT reason at each tier: T0 no trajectories and no simulator to race; T1 reference emits energies/forces, AIMD Lyapunov ~0.1–1 ps; T2 chaos + quantum nuclei (a classical trajectory is not an observable); T3 λ ~1–10 ps⁻¹ + the reference is not self-reproducible (the reference IS an ensemble); T4 quenched-disorder bifurcation (achievable in regimes A/D); T5 achievable on the integrable sector, fails on the chaotic subset. | The trajectory is Circumstances-grade, frame-local detail; what survives re-rooting and code substitution is Facts/Model-grade. **A tier boundary is exactly where Circumstances are shed and Facts are minted.** | **M12**, mechanized by `doubling_horizon_fence` |
| L2 | The 2× clause is really a statement about WHERE certificate cost is paid — construction vs run time — i.e. the certificate-directed-refinement thesis; literally it is vacuous (10⁵–10²⁰ across fidelity classes) or ill-posed (closed forms are free). T5's accuracy/speed anchor decoupling recorded as its own misfit. | The certificate is the purchase receipt for coarse-grain validity. | REPAIR + REG_GAPS note per tier |
| L3 | Unfailable checks are not gates: T0's three legs, T3's drift certificates, T4's impulse clause, T5's mass-shell identity. | Support comes only from checks that could have fired — discipline rule 6 extended to CI. | REPAIR (rollups, planted errors, consumer-semantics, independent p⁰) |
| L4 | The pincer bottoms out in DATA: the floor's strongest reference is measurement; its only falsifiable audits are cross-table and consumer-semantics checks. | The DAG's bottom anchor is empirical — matching MISSION.md's linchpin gate (sim-to-real against data). | REG_GAPS note under M12 |
| L5 | Recurring statistical-integrity defects, all previously-paid lessons: enumerated-vs-effective N; null shape unmeasured before staking; coefficient-vs-exponent; mandatory negative control; bias-as-band laundering. Two NEW generalizations: (a) every gate declares its thermodynamic ensemble per layer; (b) every certificate names its training/validation manifold. | — | HOUSE + REG_GAPS (a),(b) |

### Family II — the ledger

| # | Misfit | Metaphysical reading | Cash |
|---|---|---|---|
| L6 | Mass is not additive at the tier whose defining quantity it is: (A,Z) exact; mass = constituent sum + strictly negative whole-only correction, twice-nested (quark→nucleon ~99%, nucleon→nucleus ~0.9%). | E = mc² makes whole-only state WEIGHABLE. The identification of tabulated binding with the metaphysical slot is **wager-grade** (own kill owed); the theorem is on the model only. | **M13** + `binding_whole_only` |
| L7 | The floor ledger is OPEN: neutrinos carry ~half of every β Q out of every scene, belonging to no tier; N is rented (pair threshold, process/frame-dependent) while Q is owned; H enters grains on subcritical timescales. | Conservation at the floor is conservation-up-to-a-declared-leak — a frame-relative Record fact about what the scene can still prove. A validity-indexed conservation law is a different algebraic object from unconditional `SectorPreserving`. | **M14** — the domain-indexed ledger |
| L8 | The ledger is foliation-relative; open stressed subsystems' summed 4-momentum is not a 4-vector (von Laue, ~4e-22 for granite at 6 MPa — record and bound); relativistic temperature has no agreed transformation law (flagged before the thermal channel lands). | Physics forces a frame annotation onto the gross ledger UNBIDDEN, exactly where the 11+1 has its only frame-relative axis. | **M15** — `chart_is_record` theorem or explicit refusal; until then a live 12+2 risk |
| L9 | CJS forbids covariant instantaneous forces: a force-mediating relation must be a dynamical holon with its own GROSS ledger entries (momentum in flight), or P^μ conservation fails during every retarded interaction. | **"Never free, always rented, always leaving receipts" — proved BY THEOREM at T5.** The ledger metaphysics' strongest independent corroboration in the pincer. | **M16** |

### Family III — exclusion and statistics (the spine)

| # | Misfit | Metaphysical reading | Cash |
|---|---|---|---|
| L10 | The discharge equivocation: antisymmetry ⊋ cap ⊋ Boolean chart; hard-core bosons witness strictness; the exchange sign is invisible to every occupancy marginal. | Exchange statistics is itself a NonFactoring instance — a whole-only datum no occupancy view recovers. | **M17** — the exchange-sign witness + the repaired discharge rungs (§4.1) |
| L11 | Statistics is a composition RULE, not a flag: nucleus A mod 2; atom N mod 2; the staked flag inverted H-1, ⁷Li, ⁶Li — the three canonical cold-atom witnesses. Confirmed wrong export, kept in the record, marked dead. | No tier owns statistics; it is re-derived at every composition boundary from total fermion count. | **M18** — the E–O composition brick, never a stored flag |
| L12 | Exclusion degrades from theorem to statistic as tiers rise: Pauli exact at the floor; Loewenstein at T2 is violable (NMR-visible); the surviving scale-free shape is the cap/constraint (g(r) hard-core at T3). | The exclusion gradient. | **M19**; `feldspar_sector_count` carries a violation-rate condition or is dropped |
| L13 | The mode set is tier-relative (6 FHP vs \|nljm⟩⊗g=4); without a mode-chart parameter, two instantiations of one principle count as one witness. | — | LEAN — degeneracy-parameterized `sector_count` |

### Family IV — whole-only discipline

| # | Misfit | Metaphysical reading | Cash |
|---|---|---|---|
| L14 | Whole-only misuse, five independent occurrences: T1 3-body terms (factor through triples — Model coordinate); T2 phonons (a dynamical channel); T2 handedness (locally dense — chart limitation); T3/T4 ensemble CDFs/percolation (chart outputs / descriptor law); T5 momentum-in-flight (relation gross ledger). | Whole-only holds ONLY what provably fails to factor through the declared family of partial views; everything else has a named home. Left unrepaired the slot becomes "whatever my pair model missed" — **a second ontology by erosion**, the exact mechanism MISSION.md's closure exists to prevent. | **M20** — the eviction criterion, stated once, with per-instance fences |
| L15 | Nature's genuine NonFactoring witnesses, graded: (i) STRONG, data-named: ¹⁸⁰ᵐTa/¹⁸⁰ᵍTa — every constituent view agrees, lifetimes differ ~20 orders; and the isomer index refutes the (Z,N) key: **whole-only state is INDIVIDUATING at T0** (part of the KEY). (ii) STRONG, decidable: the exchange sign (L10). (iii) MEASURED companion: granite's correlated-flaw size-effect remainder (exponent 0.18 vs Weibull 0.1). (iv) AT-RISK: sanidine/microcline — site-labeled marginals already distinguish them; must survive the site-resolved null + metastability artifact first. (v) DEMOTED: gross-ledger-blind-to-strength is `not_computable_from` (lossy-summary blindness), not the founding shape — admitting it would dilute parity/cp-phase/record. | Whole-only at T0 is weighable (L6) AND individuating — a stronger claim than originally staked, made deliberately. | **M21** (`isomer_nonfactoring` + key corollary); (iii) measured companion; (iv),(v) graded, not cashed |

### Family V — frame/Record surfacing in matter

| # | Misfit | Metaphysical reading | Cash |
|---|---|---|---|
| L16 | Downward tier dependencies in descriptor fields: ⁷Be EC ±1% with chemistry; bare ¹⁸⁷Re 10⁹× faster; ²³⁵ᵐU IC chemistry-dependent. `env_assumption` as an artifact-local field is a coordinates-vs-frame category error. | Certificates name environment assumptions as frame data — the Record machinery, not artifact coordinates. | REPAIR via M15 |
| L17 | Scale separation has TWO holes (²²⁹ᵐTh 8.4 eV; ²³⁵ᵐU 76.7 eV — in the demo granite), margin set by ²⁰¹Hg 1.565 keV; and the literal validity domain excluded the demo material (self-activity, cosmic flux). | At the floor, "static matter" is a RATE claim: every certificate over real matter is about what the horizon makes negligible. | REPAIR — bounded-activity budget; `scale_separation_check` pins two exceptions |
| L18 | Record dropped at tier boundaries: sub-threshold damage exports an identical rigid body; dateability is physically carried by <1% accessory phases absent from every tier state. | Whether the past can still be proven is literally a mineral-census decision — the taxonomy's one frame-relation with a MATERIAL carrier. | LEAN — attach to `repairable_does_not_factor`; rigid tuple gains the Record tag (A4) |

### Family VI — DAG shape

| # | Misfit | Metaphysical reading | Cash |
|---|---|---|---|
| L19 | Skip-level exports: granite's radioactivity/heat/radon factor through T0 directly into T3/T4, bypassing T1–T2; ⁴⁰K's dual branch breaks the single decay_mode field; the mica-magnetism misroute (electronic, 1836× μ_N) is the cautionary instance of topic-routing. | The pincer's DAG is a genuine DAG, not a ladder; every above-tier observable names the tier it factors through; **exports route by MECHANISM, never topic word.** | **M22** — typed skip edges |
| L20 | Forced relation generalizations: grain-boundary cohesion is a descriptor FOR A RELATION no tier owned; Si–O–Si forces arity ≥ 3 against binary `CohesiveBond`; electron-children need a declared residency chart. | Descriptor holons exist for relations; the relation pattern generalizes — explicitly, or not at all. | **M23** — the one genuine architectural extension |
| L21 | Experiment-pinned edges are warrant VALUES on ordinary certificate edges (the proposed "third edge type" is killed — the orthogonal warrant coordinate suffices): Charles law (12–14 decades), compressive strength, and today the ENTIRE granite demo chain (no fracture-grade feldspar/mica potential exists anywhere). | The flagship demo edge is wild-data-pinned today; T3 certificates feed a hypothetical pure quartzite. Recording this plainly IS the misfit protocol. | REPAIR — pin the warrant coordinate; declared on the closing certificate |
| L22 | At the floor, `RefinementUnavailable` means "no controlled method exists" (Fe multiplets), not "children not resident"; and the floor certificate is benchmark-calibrated statistics (warrant in Confidence, not Facts). | The status enum's semantics split at the floor; certificate-directed grain admits a statistical certificate type at its base. | **M24** |

### Family VII — tier-local minting and chart limits

| # | Misfit | Metaphysical reading | Cash |
|---|---|---|---|
| L23 | Fracture energy does not commute across tiers: T2 exports 2γ ~ 1–4 J/m²; the 30–100× gap to 110 J/m² is process-zone dissipation MINTED at T4; compressive strength is cooperative (no min-stability transport). | The ledger in the concrete: the crystal pays the reversible surface bill; the polycrystal pays rent on every avoided, deflected, bridged path — and the receipts (the microcrack cloud) are observable. | **M25** — tier-local typing with exported lower bounds (`griffith_floor_bilinear`) |
| L24 | Isotropy is a theorem about texture, not a material property: C14 (~18 GPa) and d11 killed identically by random-orientation averaging; textured rocks void the chart; the chirality bit is carried by every grain, weighed by every response, read by no isotropic average. | A concrete small instance of "weighs everything, reads none of it." | LEAN — `texture_average_kills_odd`, `dauphine_average_c14_zero`; random-texture warrant field |
| L25 | Properties that are conditioned functions, not scalars: γ spans 0.38–4.5 J/m² on ONE mica plane (air vs vacuum); subcritical growth makes strength history-dependent. | The condition is typed (Circumstances / condition-holon) or "the environment" becomes a second entity class by omission. Convicts scalar `fracture_energy_j_m2`; the descriptor prose was ahead of the struct. | REPAIR — typed condition axes |
| L26 | Strength is a volume-tagged distribution; the scalar field is a category error; the exported triple is frame-dependent without the effective-volume correction; the plain Weibull scaling law is falsified by the spec's own size-effect data (0.18 vs 0.1). | — | REPAIR (schema) feeding M21's measured companion |
| L27 | Dislocations are loop holonomy: nonzero Burgers vector around a cycle of INTACT relations — whole-borne on loops, invisible to every single-relation view, carrier of plasticity/hardness; {damage=1} cannot type it. | Lands on Core/RouteSymmetry and the maintained-holonomy campaign (only a design-knowing repair holds direction). | **M26** — the strongest new tier-object discovery |
| L28 | The bosonic sector: phonons/photons have unbounded per-mode occupancy and cannot ride the Boolean ledger; exclusion nonetheless recurs as a CONSTRAINT where excitations are non-excluding; spin-statistics as exported delivers only the fermionic half. | — | REPAIR — declared bosonic channel with own certificate; recurrence noted under M19 |
| L29 | Missing physical sectors, each now a repaired export or a scoped refusal (§3): anharmonic third-order set; hydrogen/wet chemistry; surface creation + hetero-interfaces; dispersion; configurational disorder; piezo/dielectric closure; pore pressure; cyclic fatigue; aligned fabric; **gravity at T5** (not in flat SR's descent cone — equivalence principle by citation, Rindler chart, or the jaw stops short and says so); the non-unique Galilean limit of Maxwell; Thomas/BMT for tiers that keep O(β²); β-floor arithmetic corrected (6.3 m/s, not 6 km/s). Two generalizations: (a) a validity predicate may not reference state the interface cannot represent — that mismatch is itself an interface misfit; (b) GrainFloor can arrive from ARITHMETIC. | — | REPAIR each; **M27** for arithmetic-GrainFloor |
| L30 | Type/instance conflation (determinism family): population tables vs latent holon populations (T0); ensemble CDF at descriptor level (T3); quenched Weibull draw persisted in Record (T4); decay clocks' seed/expectation declared; deterministic engine vs ensemble-valued reference requires seed + sampling protocol in the Record. | Class-level law on the descriptor; realized draw in the subject's Record; replay-stable by construction. | **M28** — the quenched-realization rule |
| L31 | Two references diverge: match-the-named-simulation and match-reality are different gates (codes ~1 meV/atom inter-code, ~5% vs experiment on C_ij); fidelity-class laundering (K_Ic 20–30%); signed systematics, not symmetric bands. | — | HOUSE (compare within one runtime) + REPAIR: absolute error and reference-agreement as separate certificate columns |

**Tally:** 17 candidate M-numbers (M12–M28). Highest leverage five: **M16** (relations rent — theorem-backed), **M17** (exchange-sign NonFactoring — closes §4.1's gap honestly), **M20** (whole-only eviction — protects the architecture's central distinction), **M21** (isomer individuation — data-named founding-shape witness), **M26** (dislocation holonomy — new tier object on existing Lean machinery). Everything else is REPAIR/HOUSE/LEAN with named owners. Dead items kept and marked: the A-mod-2 atom-statistics flag (L11); the (Z,N) state key (L15-i); the 6 km/s β-floor (L29); the uncorrected McClintock–Walsh corollary (C3); the Rapier-object rigid export (A4).

---

## 6. Dependency order — pure GANTT (no dates)

### Nodes

**Lane E — engine** (parallel to physics until join points)
- **E1** adaptive crack-tip materialization (named next step in MATERIALS_AND_FRACTURE.md; *demanded* by ℓ_ch = 13.7 cm vs the 288-node frontier, C2)
- **E2** rigid-chart-not-Rapier repair + Record/damage tag on the rigid export (A4, L18)
- **E3** quenched-flaw Record rule + whole-only eviction + single friction owner (A1, A2, A3 / M20, M28)
- **E4** n-ary relation + relation-descriptor decision (L20 / M23)

**Lane B — bottom-up physics**
- **B0** T0 repairs (isomer key; statistics composition rule; mass-convention field + consumer-semantics check; cross-table rollup gate; activity budget; two scale-separation exceptions; env→Record) → **B0'** T0 audit green. No upstream blockers.
- **B1** T1 repairs (H in palette; surface/hetero-interface leg; crack-tip leg; PBEsol/SCAN pin; anharmonic exports; effective-N statistics; BKS negative control; needs E4 for angle-term schema) → **B1'** battery green. Blocked by B0' (formally) and E4 (schema).
- **B2q** T2 quartz gate (ACE/GAP exists; QHA/PIMD leg; restrict below 846 K; rule-6 forward-prediction leg). Blocked by B1'.
- **B2fm** T2 feldspar/mica measured-descriptor route (Brillouin/RUS tensors + Record provenance; experiment-pinned — no simulator exists). Blocked only by B0-class table pinning; runs parallel to B1/B2q.
- **B3** T3 quartz fracture gate (ensemble-declared layers; CDF-distance Weibull gate; manifold naming; costed Layer 2). Blocked by B2q.
- **B4A** T4 regime A — runnable now. **B4D** regime D — done; precedent not overreached.
- **B4BC** T4 regimes B/C — blocked by THREE joins: B3, B2fm + grain-boundary relation-descriptor data (experiment pin), and E1. C1–C4 are pre-gate repairs inside B4.

**Lane A — top-down physics** (fully parallel to Lane B; no bottom-up gate blocks any Lane A node)
- **A1** T5 gate repairs (pin dt + envelope; stake the coefficient ½; cancellation-safe p*; corrected β-floor; pre-registered chaotic-sector gate)
- **A2** closed-form trajectory + limit-order gates run. Blocked by A1.
- **A3** gravity decision: Rindler/uniform-g chart with equivalence principle CITED, or record that the top-down jaw stops short of the demo scene. Blocks SR→Newton coverage of any scene with weight — i.e. every engine scene.
- **A4** SR→Newton certificate (O(β²) remainder + retardation bound). Blocked by A2 + A3.

**Lane L — Lean / meet** (parallel to everything; joins one-way)
- **L-a** floor-interface restatement: exclusion scoped to the CAP, exchange sign carried explicitly (§4.1's repair).
- **L-b** `pauli_cap` + Boolean-vs-correlation fence + mode-chart parameter + E–O composition rule + CAR/S_n shadows relabeled as witnesses. No blockers.
- **L-c** `chart_is_record` decision (M15), `doubling_horizon_fence` (M12), `isomer_nonfactoring` (M21), exchange-sign NonFactoring witness (M17), holonomy typing (M26). No blockers.
- **MEET-1** (keystone): "exclusion discharged BY PAPER, witnessed BY MACHINE" certificate. Blocked by L-a + L-b ONLY. **The keystone is cheap and can close early.**
- **MEET-2** (Newton seam): T4 rigid chart ↔ T5 Newtonian chart over the same holon, Rapier as control. Blocked by E2 + B4D + A4.
- **CLOSE**: pincer closed on the demo stone, **with declared pins**. Blocked by MEET-1 + MEET-2 + B4BC; the wild pins (feldspar potential, Charles law, grain-boundary data, compressive mode, damping/restitution split) are DECLARED on the closing certificate, not resolved.

### Parallelism

Lane A never waits on Lane B. Lane L never waits on anything. B2fm runs beside B1→B2q. B4A and B4D are available immediately. E1–E4 run beside B0–B3 and join only at B4BC/MEET-2. The two jaws are genuinely concurrent; the only shared node is MEET-1, whose inputs are the cheapest items in the program.

### Critical path

```
B0' → [E4 joins] B1' → B2q → B3 → [E1, B2fm + GB-pin join] B4BC → [E2, A4 join] MEET-2 → CLOSE
```

The bottom-up chain T0→T1→T2→T3→T4, with adaptive crack-tip joining at T4 and the SR→Newton certificate joining at the final stitch. The top-down track is never on the critical path.

**The honest caveat (L21):** for GRANITE — as opposed to a hypothetical pure quartzite — the chain is severed at B2fm: no fracture-grade feldspar/mica potential exists anywhere. The certified-everywhere closure has an EXTERNAL dependency (feldspar MLIP development, not under project control) that either becomes the true critical path or is bypassed by declared experiment pins on the closing certificate. **Plan for the pin route and record it: the flagship demo edge is wild-data-pinned today, and CLOSE is reachable only in the with-declared-pins form until the external dependency lands.**

---

## 7. Lean obligations, ordered by what unblocks what

Sorry-free bar throughout; `decide`-style finite computations preferred where the objects are finite, exactly per `Lattice.lean`. Recorded commitments (`True`-field records) are never blurred with proofs.

### Tranche 1 — unblocks MEET-1 (cheapest, highest leverage; no blockers)

1. **`pauli_cap`** — Boolean/antisymmetric per-mode occupancy forces N ≤ g × (spatial modes): the cap as an inequality, discharge link (ii).
2. **`boolean_vs_correlation_fence`** — 2-orbital/2-electron CI model: natural occupations ∈ {0,1} iff single determinant, deviation = correlation weight. Link (iii).
3. **`sector_count_degenerate`** — `Lattice.lean`'s sector table generalized over a mode-chart parameter with internal degeneracy g; identifies |nljm⟩⊗4 and 6-FHP as instantiations of one shape. Link (iv); repairs L13.
4. **`eo_composite_statistics`** — sign of the permutation exchanging two disjoint n-blocks is (−1)ⁿ; corollary: composite of n fermions is a fermion iff n odd; statistics re-derived per composition boundary from total fermion count. Link (v); replaces the dead flag (M18).
5. **`car_fock_witness`** (relabeled) + **`sn_two_statistics`** — the 6-mode CAR Fock basis IS `Fin 64` with N, P matching; Hom(S_n, ℂˣ) has exactly two elements. **Consistency witnesses, never the discharge** — CI must not show green on "relativity implies exclusion."

### Tranche 2 — fences and founding-shape witnesses (no blockers; protect the architecture)

6. **`exchange_sign_nonfactoring`** (M17) — two finite mode algebras (hard-core boson vs fermion) with identical occupancy marginals under every partial view, differing in the exchange sign. The strongest new theorem candidate.
7. **`isomer_nonfactoring`** (M21) — the ¹⁸⁰ᵐTa/¹⁸⁰ᵍTa instance of `Core/NonFactoring.lean`, with the key corollary: T0 identity requires a coordinate neither in the additive ledger nor a function of the parts list.
8. **`doubling_horizon_fence`** (M12) — trajectory agreement to τ at horizon n forces initial agreement τ·2⁻ⁿ (doubling map, dyadic-decidable); instantiate n ~ 1000 to close the literal-gate route permanently.
9. **`binding_whole_only`** (M13) — mass export = additive sum + strictly negative non-factoring correction, on the model only; the physical identification stays wager-graded with its own kill.
10. **`domain_indexed_conservation`** (M14) — a conservation statement carrying its validity predicate, as an algebraic object distinct from `SectorPreserving`.
11. **`chart_is_record` decision** (M15) — theorem or explicit refusal; until it lands the T5 chart descriptor is a flagged 12+2 risk.
12. **`whole_only_eviction` fences** (M20) — per-instance: the T1 3-body rank witness restated as Model-coordinate content; T4 strength/percolation as chart outputs. Plus **`scale_separation_check`** with TWO pinned exceptions, decided over hash-pinned tables audited against publication.

### Tranche 3 — tier-gate support bricks (blocked only by their tier's data pins)

13. **`error_propagation_monotone`** — ε_{n+1} ≤ K·ε_n + ε_local for Lipschitz-K tier maps; one generic lemma reused at every tier.
14. **`harmonic_export_soundness`** — Lipschitz-Hessian quadratic-model bounds (energy ≤ L·r³/6, force ≤ L·r²/2); GrainFloor is provably the ball's boundary.
15. **`adiabatic_gap_bound`** — 2×2 avoided-crossing eigenstate-following error ≤ coupling/gap; the BO validity skeleton.
16. **`rule_of_three`** — zero failures in n i.i.d. draws bounds rate by 3/n at 95% — with the effective-N caveat carried in the statement (i.i.d. hypothesis named, not assumed).
17. **`elastic_dim_class32` / `_monoclinic` / `_triclinic`** (6/13/21) — representation-theory dimension counts typing the three descriptor columns.
18. **`texture_average_kills_odd`** + **`dauphine_average_c14_zero`** (L24) — the isotropic chart's warrant and the twin-fraction certificate field, mechanized.
19. **`voigt_reuss_bracket`** + **`binding_admissible`** — Reuss ≤ effective ≤ Voigt; an out-of-bracket descriptor is unconstructible (lands on the noted PSD-cone brick lineage).
20. **`acoustic_limit_is_christoffel`** — k→0 dynamical-matrix eigenvalue is (C/ρ)k²; the T2→T3/T4 dispersion-to-continuum bridge.
21. **`weibull_min_stable`** + **`flaw_tail_to_weibull`** (m = 2(p−1)) + **`griffith_strength_floor`** — the tensile transport theorems; the contrapositive of the floor IS the GrainFloor return.
22. **`survival_log_additive`** + **`correlated_flaws_nonfactoring`** — independence gives a log-additive ledger; a finite witness with identical pair marginals and different min-statistics quantifies the remainder (filed under graded L15-iii, not the founding shape).
23. **`feldspar_sector_count`** — ONLY with the violation-rate condition (M19), or dropped: no exact table about an idealization nature only statistically obeys.

### Tranche 4 — engine-vocabulary bricks (blocked by E-lane decisions where noted)

24. **`bilinear_work_eq_gf`** + **`damage_monotone`** + **`broken_transmits_zero`** — mechanizes `material.rs`'s stated claims (opening_at_failure = 2G_f/f_peak triangle identity).
25. **`crack_set_monotone`** — {r | damage(r)=1} nondecreasing along every trajectory; the crack as a write-once Record observable, connecting to `repairable_does_not_factor`.
26. **`mcclintock_walsh_ratio`** — **corrected constants** (C3): closed-crack ratio 4·(√(1+μ²)+μ)/(√(1+μ²)−μ), value 4 at μ=0 (open-crack Griffith 8 is a different criterion, stated separately), strict monotonicity, μ ∈ [0.6, 0.85] ⇒ ratio ∈ [12.5, **18.7**] covering 15.8. Blocked by E3 (friction owner).
27. **`lch_snapback_gate`** — h > c·ℓ_ch releases more elastic energy than G_f × tributary area ⇒ snap-back ⇒ the refinement predicate MUST return GrainFloor; mechanizes the resolution wall the 288-node frontier violates. Pairs with E1.
28. **`dissipation_le_input`** — discrete energy inequality for the cohesive integrator under a stated timestep bound.
29. **`griffith_floor_bilinear`** (M25) — within `CohesiveLaw`: G_c ≥ 2γ with equality iff process-zone dissipation is zero; why 110 J/m² is T4 content and only the floor exports.

### Tranche 5 — T5 bricks (blocked by A1)

30. **`four_momentum_ledger_covariant`** — Λ(Σp)=ΣΛp; frame-independent invariant mass; reverse triangle inequality M ≥ Σm_i.
31. **`newtonian_limit_remainder`** — |x(t) − at²/2| ≤ a³t⁴/(8c²) for at/c ≤ 1; the staked-coefficient gate tests the implementation against a theorem, not a fit.
32. **`mandelstam_sum`** — s+t+u = Σm² on shell, ring-provable.
33. **`no_single_speed_invariant_set`** (retyped `no_lattice_boost`) — relativistic velocity addition preserves no finite single-speed set with |v| < c; the honest statement that the single-speed gas is chart-bound. (The O(2,1)-on-spatial-vectors draft did not typecheck; kept in the record, marked dead.)
34. **`sector_table_is_pmu_table`** — (N,P) → P^μ injective on Fin 64; 53 sectors, profile 44/7/2, as a decide-level corollary.
35. **`verlet_shadow_exact`** — velocity Verlet on the harmonic oscillator conserves an explicit quadratic shadow form for ωdt < 2; what discretization preserves is a DEFORMED invariant, not the trajectory.

### Tranche 6 — new-object typing (blocked by M-number confirmation)

36. **Dislocation-as-loop-holonomy** (M26) — Burgers circuit as holonomy of a cycle of intact relations, on Core/RouteSymmetry's machinery; the typed home that prevents a third vocabulary at T2.5.
37. **Record-tag transport at the rigid seam** (L18) — attach to `repairable_does_not_factor`; the pristine-vs-preloaded wall pair as the finite witness.

---

*Kills, collected: the program as a whole dies if certificates do not transfer to measured reality (MISSION.md's linchpin — sim-to-real against data at every tier). MEET-1 dies if link (i) is ever claimed as machine-checked, or if the floor interface neither strengthens nor scopes. The whole-only slot's meaning dies by erosion if M20 is not enforced — that kill is architectural, and it is the one this document exists to prevent.*