# The blind ablation panel: five-for-five convergence (2026-08-20)

Design: the five-characteristic profile of Circumstances (uniquely content-free; uniquely
out-of-scope; misread as Facts when unlabelled; unsigned — the fixed-point class of the
polarity involution at 85-94%; load-bearing — removal perturbs everything else at
real-kind cost) was split five ways, each blind agent receiving FOUR characteristics with
a different one ablated. No agent could read the repo; no prompt contained the words
identity, zero, vacuum, unit, gauge, or dark. Each returned a ranked top-5 from math and
physics.

## Result

All five lists are the SAME object-family under different names: the identity/unit
element, zero / the DC-constant mode, the trivial representation, the vacuum/ground
state, the regression intercept / reference category, the choice of origin, the
non-dynamical background. No rank-1..3 candidate in any list falls outside the family.

Rank-1 winners: choice-of-origin (ablate-1), identity/unit element (ablate-2),
zero/constant mode (ablate-3), unit of a factorization system (ablate-4 — with the
UNKNOT, the identity of knot composition, at #2), pure gauge (ablate-5).

## Two findings sharper than the headline

1. **The ablation pattern reproduces the gauge test.** The only panel that crowned "pure
   gauge" is the one that could not see LOAD-BEARING (ablate-5). Panels that saw it
   demoted gauge — the blind design independently rediscovered why K-G1 fired: gauge fits
   everything about Circumstances EXCEPT that removing it costs.
2. **The intercept theorem is the gauge test as classical statistics.** Two panels
   surfaced the regression intercept / dummy-coding reference category, with the textbook
   result that REGRESSION THROUGH THE ORIGIN BIASES EVERY OTHER COEFFICIENT. Our removal
   experiment (+4.2pp perturbation of the untouched population, >= the real-kind control)
   is that theorem, measured on a panel.

## The identification, as it now stands

**Circumstances is the taxonomy's REFERENCE ELEMENT — its zero, its unit, its intercept,
its vacuum.** Blind, five-for-five, robust to ablating any single characteristic, with
the profile assembled from four independent measurements BEFORE the object was proposed.

Formalization target (fills the named gap: the stance's dark-medium wager has no Core
object): the unit of the change-composition structure, whose removal yields a
semicategory in which sameness is inexpressible, with the intercept-bias theorem as the
statistical face and the gauge test as the measured shadow. The cosmological reading
(the background medium — what the stance calls the paper) stays WAGER-CLASS pending the
four pin-matchers still running.
