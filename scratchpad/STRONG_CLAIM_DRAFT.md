# Pre-staked claim wording — IF the eigen-alignment reaches STRONG (frozen before data)

Written 2026-08-18, before `main_primary.json` exists, so the announcement cannot be
fitted to the result. If the verdict is not STRONG, this file stays as the record of
what we would have said — and did not.

## The claim as it would enter Stance.lean

- **headline**: The eleven kinds are directions: a language model's representation of
  change aligns with the taxonomy's eleven coordinates, and Record is measurably not a
  twelfth.
- **plain**: Describe a change to a document, and ask where it lands on an AI model's
  internal map. The eleven kinds show up as eleven real directions on that map, and the
  twelfth thing — whether the past can still be proven — shows up not as a twelfth
  direction but as something visible only when you are told which records survive. We
  did not teach the model the kinds; we found the kinds already there.
- **status**: measured (basis: EIGEN_ALIGNMENT_RESULTS + the replication run; never
  before both legs exist)
- **kill**: replication failure on the interleaved corpus; any rival 12-way partition
  matching the taxonomy's alignment; a third embedder family reading null; the standing
  Beat-the-Taxonomy bounty claimed.

## Scope fence (what STRONG does NOT license, frozen with equal force)

1. NOT "reality is 11+1" — the scope is representation geometry of trained language
   models on English change-descriptions, two embedder witnesses.
2. The Logos-embodiment wager gains its FIRST GEOMETRIC LEG and remains a wager.
3. The supersymmetry bridge (N18) becomes STATABLE — an object exists on our side —
   and remains unsupported. The integer 11 is still not an object match.
4. No instrument's `validated` flag moves; the human ceiling still gates all of them.
5. The DMA collapse upgrade is exactly this: from ergonomic bet to principled routing
   along the model's native coordinates — an engineering claim with A8's kill intact.

Promotion path: DETECTED tonight (any band) + STRONG on the interleaved replication →
this claim enters the stance. STRONG tonight alone does NOT promote (single corpus,
batch-confound arms only partially powered — §19-D9).
