# THE MAXIMAL PROJECTION — the marriage of the stances, and the chains it can be killed on

**Scratchpad only.** No Lean file was opened for editing, `Stance.lean` is untouched, the audit
was not run, `lake` was never invoked. Nothing here is proposed for the published page.

**What this document is.** It marries the proved core, the measured record and the wagers into
the single strongest statement they jointly license; projects that statement onto the three
scales; and reads off the places where it could be caught being wrong. It is written to be
**killed**, not believed.

**The constraint that governs every sentence.** `epistemology.md` L6: *a residual is never
support; support comes only from a specific prediction, made in advance, that then holds.*
A synthesis assembled after the fact is a **hypothesis**, not evidence. So this document's
value is **entirely** in §3's forward chains, and **none** of it is in how well §1 and §2
account for what is already known. Nothing below may be cited as support for anything.

**Status tags, used on every load-bearing clause and never rounded up:**

| tag | means |
|---|---|
| **[P-model]** | machine-checked in this repository, **about a model**. Says nothing about the world. |
| **[M]** | measured, with its domain stated inline. A measurement on one substrate is a measurement on one substrate. |
| **[W]** | an avowed bet. Not evidence. |
| **[O]** | open, named, not leaned on. |
| **[D]** | dead — its own falsifier fired. Kept, marked. |
| **[SP]** | a **scratchpad** campaign result. Never on the page, never a stance input. |

---

## 1. THE MARRIAGE

### 1.1 The single maximal statement

> There is a **sector** of any multi-part system's description that no pairwise summary can
> read **[P-model]**, whose capacity on three binary slots is **exactly** `ln 2` with no
> hypothesis at all **[P-model, `share_max_eq_log_two`]** and `(k−3)·ln 2` from four
> pair-uniform slots up **[P-model, upper bound only; attainment computed, not mechanized;
> loose from k = 8]**. That sector is **empty by force of symmetry** wherever the description
> is invariant under a global flip **[P-model, `share_eq_zero_of_signSymmetric`]**, so it can
> only be occupied where a symmetry is broken. Across **time** it is exactly what memory buys —
> no memoryless process writes it, one remembered bit does **[P-model]** — and causal order
> caps it at classical strength, while **space** escapes that budget (the C5 ring state carries
> `5·ln 2`) **[P-model; the causal bound is convergent with arXiv:2505.13681, theirs first in
> print]**. The sector is **created, not merely preserved**, by maintenance whose code is not
> flip-symmetric — one repair step on pure noise mints exactly one bit, the flip-symmetric
> repair mints exactly zero **[P-model, one step, three binary slots, same alphabet;
> coarse-graining explicitly NOT covered]** — and under private per-cell noise it flows only
> **upward** out of the pair sector, never back down and never out of nothing, with **asymmetry**
> and not strength as the pump **[P-model, k = 3 binary]**. Holding an entry costs rent: pay the
> decay and it holds, underpay and it strictly loses, pay nothing and it tends to zero
> **[P-model]** — and on the one real substrate where that clause was tested, the **rate**
> survived parameter-free to 7 % and the **exponential shape was rejected** at χ² = 153 against
> a staked bar of 26.5 **[M, ibm_marrakesh Heron, qubits 6-7-8, job `d9in8jrjf64c739fprqg`]**.
> Wherever this sector has been looked for **in nature**, the reading is null, wounded, or
> ungauged: four substrates on the page read at the floor **[M]**, the LHCb open-data flavour
> search excluded a whole-only CP asymmetry of ε ≥ 0.03 at 95 % and found nothing **[SP]**, and
> the one survey-scale reading that scored its criterion MET was then **wounded by our own
> pre-registered refuter** and is not cashed **[O]**. The **only** forward prediction this
> programme has ever confirmed is a scaling exponent inside a lattice model — `−3.084 ± 0.219`
> measured against `−3.109` staked before the run **[SP, 3D φ⁴, a model computation, explicitly
> not a claim about the Higgs or about nature]**. **Every** statement connecting the sector to
> the Standard Model, to the cosmos, to life, or to mind is **[W]**.

### 1.2 What the marriage actually is, in one line

**An unaudited sector with a symmetry precondition, a memory precondition, a hard capacity, and
a one-way inflow.** That is the whole shape. Every physics wager on the page is a projection of
that shape onto a domain where the mapping has not been established.

### 1.3 What this deflates, named

The honest maximal statement is **narrower than the campaign's rhetoric**, in six specific
places. Each is stated as a deflation of a sentence this programme has written.

| the rhetoric | the deflation |
|---|---|
| "the **Logos** is real, measurable, and machine-checked" | What is machine-checked is an **existence-and-measurability result on one exhibited three-coin state**, plus a vanishing theorem, plus a capacity. That is a fact about a finite discrete model. The claim's own `confidence` field already says this; the *frame* around it does not. |
| "shared pattern behaves like a **ledger** — never free, always rented, always leaving receipts" | The rent clause is **[P-model]** and its one real-substrate test **failed on shape** [M]. The `ledger` claim itself is **[D]**. What survives measured is `true-books` (relabeling invariance) — one theorem plus a borrowed data-processing inequality — and `gravity-charge` (the weak equivalence principle), which is not about this sector at all. |
| "**gravity** weighs everything but reads none of it" | The staked form is **[D]** — killed by an exact pp-wave and by LIGO reading source structure out of vacuum strain. What survives is a statement about **coupling**, tested to 1 part in 10¹⁴, and it is not evidence about the sector. |
| "whole-only structure **peaks at criticality** on every substrate we have tested" | Measured **[SP]** in two lattice models, at ceiling fractions of **0.66 %** (2D Ising ridge) and **0.023 %** (3D φ⁴). The largest trustworthy 3D reading is `2.6 × 10⁻⁴` nats. Both records say in their own §0 that they say nothing about nature. |
| "a **language model** is the Logos embodied … good news for **AI safety**" | The cross-architecture **sign** is real [M]; the **size** is GPT-2-specific (~2 % learned order-3) and OPT's is artifact-dominated. Both obvious deception detectors came back **validated negatives at 8B**, each with a firing control. |
| "the dark sector may be the **ledger**" | **Two fired kills** sit on that wager's cosmological leg: the Landauer bill is 3–5 dex too small when priced self-consistently, and the maintenance-flow rescue exceeds the FIRAS distortion limit by 8.8 dex. Only the *shape* agreement survives, and a shape agreement is a residual. |

**And one internal deflation nobody outside would find.** The page stakes **both branches** of
the dark-energy duel (`dark-balance-intensive` forbids phantom always; `dark-balance-extensive`
predicts a crossing at `z = 0.59 ± 0.03`). Each branch is individually falsifiable and each
carries its own kill — but **the framework as a whole cannot lose the duel**, and whichever
survives, the survival is not support for the frame. The page says "at most one survives"; it
does not say the composite is a hedge. It is.

---

## 2. THE PROJECTION

### 2.1 What is FORCED by the proved core — and the finding is that it is almost nothing about nature

The theorems are about models. Read strictly, they force statements about **what our meter can
read**, not about what the world contains. There are four, and all four are instrument-facing.

| # | forced by | the forced statement | already checked? |
|---|---|---|---|
| **F1** | `share_eq_zero_of_signSymmetric` | Any globally flip-invariant description carries whole-only share **exactly zero** — at every temperature, coupling and criticality. | **Yes, four times.** 2D Ising at `h = 0`: `8.9e−16` at 49 temperatures, every lattice, every geometry [SP]. 3D φ⁴ K1: worst \|z\| = 0.78 with the global flip **off** [SP]. Dalitz plumb lines: 400 random sign-symmetric tables, max `4.4e−16` [SP]. The φ⁴ free-field control (`λ = 0`, Gaussian): below `3e−8` nats, 8000× under the ridge [SP]. |
| **F2** | `share_max_eq_log_two` (ThirdCap) | Every three-binary-slot reading is `≤ ln 2`, with **no** hypothesis on the pair data. This is the denominator every campaign divides by, and until this season it was **argued, not proved**. | Yes — 124/124 real-hardware readings comply [M]; hardware never got closer than 36 % of it. |
| **F3** | `vnEntropy_causal_past` + `temporal_third_saturates` | For any causally ordered process, the past-facing view never carries more entropy than the whole: **no quantum surplus in time**, while space has one. | Partly. The bound is a theorem of standard quantum mechanics and was **published first** (arXiv:2505.13681). |
| **F4** | `valve_upward_strict`, `valve_needs_asymmetry` | An **asymmetric** per-cell channel manufactures whole-only share out of pair structure with nothing coordinating it. Therefore any whole-only reading on **count** data is signal **plus a positive floor**. | Yes, and it is not small: the sky campaign measured the floor at **+50 % at one scale and ×5.8 at another** [M]. |

**This is the section's finding, and it should not be softened.** The proved core does not force
a single statement about the Standard Model, the cosmos, or mind. It forces four statements
about instruments. Everything in §2.2 and §2.3 rides on wagers.

### 2.2 What follows only under the wagers — micro / Standard Model

| projection | rides on | current standing |
|---|---|---|
| The SM's book sorts into protected dials (self-maintaining) and unprotected dials (rent due, payer unknown), and **exactly two** fail 't Hooft's test: `m_H²` and `Λ`. | `law-as-habit` [W] | The sort is **correct** [SP, drift audit] — and it is a **retrodiction**: the classification was composed knowing which two numbers are embarrassing, and the audit says so in its own §4.1. One correction was owed and made: **θ̄_QCD passes** 't Hooft, so the page's "tiny without visible protection" is wrong; mis-filed, it would have handed the kill half its own conjunction for free. |
| The two unpaid bills need **payers**, and the framework's payer is **mint-shaped** (an attractor whose fixed point is the observed value), not **symmetry-shaped** (a guardian). | `law-as-habit` + `mint` [W] | Not staked on the page. **Chain A.** |
| The one unprotected dial we can measure sits **near-critical** (vacuum metastable at 2.8σ), and near the edge is where pattern is richest. | `law-as-habit` [W]; the ridge results [SP] | A **resonance**, credited to Giudice, not a derivation. **Chain B.** |
| The mixing **phase** runs near the ceiling its angles allow, because it is the one dial no pair-level reading constrains. | `phase-at-ceiling` [W] | Quark leg is an avowed **retrodiction** (0.913 ± 0.025 with the 0.9 line drawn knowing the answer); the lepton leg is a genuine advance prediction. **Chain C.** |
| Neutrino mixing is genuinely structureless, so δ sits wherever a random table puts it. | `neutrino-anarchy` [W] | Coordination half is **measured-robust** (PMNS ~40th percentile of 200k Haar draws); the CP half is live. **In partial competition with Chain C — see §3.3.** |

### 2.3 What follows only under the wagers — macro and cosmological

| projection | rides on | current standing |
|---|---|---|
| Near-critical natural systems carry whole-only structure invisible to their pair statistics. | `meaning-is-third`, the ridge results [W]/[SP] | The sharpest available test is the **glass transition**, where the field's own null (`g(r)` determines the structure) is exactly our null. **Chain H.** |
| A pair-policed system accumulates unwatched structure **without a schemer** — the unwatched sector fills by algebra. | `goodhart` [W] + `valve_upward_strict` [P-model] | A runnable head-to-head is named in the claim's kill and has never been run. **Chain M.** |
| Λ is unpaid, so it should be **losing** — thawing, not exactly constant. | `dark-balance-*` [W] | DESI DR2 prefers the thawing quadrant at 2.8–4.2σ. **Both branches are staked. Chain D, and §1.3's hedge.** |
| Dark energy cannot exist before structure does, so **early dark energy is forbidden**. | `no-early-dark-energy` [W] | Unhedged, and it points against a fix many would like to work. **Chain E — the cleanest cosmological chain on the page.** |
| Protected dials hold while unprotected ones move. | `law-as-habit` kill leg (3) [W] | **Ran.** Kill NOT fired, and **structurally unfireable** on its second conjunct with 2026 data. **Chain F.** |
| Nature's wild processes carry whole-only share at survey scales. | `wild-share` [O] | BOSS DR12 scored MET, then **wounded**: 6.0/9.7σ corrected, the lower-bound framing falsified **in sign**, one VOID gate undischarged. **Chain G.** |

---

## 3. THE EVIDENTIARY CHAINS

**How to read this section.** Every chain carries its kill in its own row. The ranking is by
*discriminating power × decidability*, not by how interesting the claim is. A chain marked
**CONSISTENT** predicts nothing a standard reading does not, and is ranked below every
**DISCRIMINATING** one however grand it sounds.

### 3.0 The ranking, and the honest count

| rank | chain | discriminating? | earned or retrodiction? | decidable when | staked on the page? |
|---|---|---|---|---|---|
| **1** | **H — glass / amorphous order** | **YES** (against the field's own `g(r)` null) | earned; kill staked in advance | **weeks**, on this box | no |
| **2** | **J — the stretched-rent prediction** | **YES** (against the framework's own model clause) | earned; a second, sharper stake after a fired kill | **months**, ~200 QPU-s | no |
| **3** | **K — sign symmetry vs the criticality literature** | **YES** (forced; a class of published results is measuring something provably not this) | earned (forced by theorem) | **days** | no |
| **4** | **N — the valve floor vs published synergy** | **YES** (predicts specific published positives fall) | earned | **weeks** | no |
| **5** | **A — mint-shaped payer vs symmetry guardian** | **YES**, but asymmetric and slow | half-earned (the sort is retrodiction; the payer preference is forward) | **2029–2041** | **no** |
| **6** | **E — no early dark energy** | **YES**, qualitatively; unhedged | earned | **~2027–2032** | **yes** |
| **7** | **C — lepton phase at ceiling** | partly (LR ≈ 3.5 against a flat prior); **borrowed hypothesis** | quark leg retrodiction, lepton leg earned | **~2030s** | **yes** |
| **8** | **G — wild share in the sky** | **NO as constructed** (the prediction is the mocks') | earned procedurally, wounded | blocked | **yes (open)** |
| **9** | **D — Λ thaws** | **NO at frame level** (both branches staked) | extensive branch is avowedly post-hoc | **DR3, ~2026–27** | **yes** |
| **10** | **F — drift ordering** | **NO** until the framework names a rate | retrodiction, and says so | unfireable now | **yes** |
| **11** | **B — vacuum near-criticality persists** | **NO** (no corridor width predicted) | retrodiction | **2040s** | no |
| **12** | **I — multi-time process tensor** | **NO** (the bound is standard QM, published first) | — | months | no |
| **13** | **M — Goodhart head-to-head** | YES in principle, **not runnable by us** | earned | indefinite | **yes** |

**THE COUNT, which is the finding.** Chains that are simultaneously (i) discriminating,
(ii) about **fundamental physics**, and (iii) decidable on a stated timescale: **one and a
half.** Chain A, which is slow, asymmetric and not currently staked; and Chain E, which is
qualitative. Every other discriminating chain in this document is about the **instrument**
(K, N), the **framework's own model** (J), or **condensed matter** (H).

That is the honest state of the projection, and it is a much smaller thing than the marriage
paragraph sounds like.

---

### 3.1 Chain A — THE TWO UNPAID BILLS NEED PAYERS, AND OURS IS MINT-SHAPED

> **KILL, in one sentence: a superpartner — or any weak-scale symmetry partner that completes
> the Higgs cancellation — is discovered.**

**The prediction.** 't Hooft's test sorts the Standard Model's dials, and exactly two fail it
[SP, drift audit §1]: the Higgs mass parameter (cancellation to ~30 decimal places; setting it
to zero restores only *classical* scale invariance, which is anomalous) and the cosmological
constant (the coefficient of the identity operator, the most relevant operator that exists, 120
orders below the naive estimate). This framework says an unprotected dial has **rent due**, and
therefore a **payer**. It then says something more specific, and this is the whole content of
the chain:

**the framework's payer is MINT-SHAPED, not SYMMETRY-SHAPED.**

- A **guardian** — supersymmetry is the canonical one — makes the small value *stable* by
  adding a symmetry that forbids the correction. Nothing is created; something is forbidden.
- A **mint** is a maintenance dynamic whose *fixed point is the observed value*. The pattern is
  not protected, it is **manufactured and re-manufactured**. This repository's own theorem in
  the smallest possible case says maintenance CREATES the pattern it maintains
  (`repair_mints_from_noise`) **[P-model]**, and that the flip-symmetric repair mints exactly
  zero (`repair_creates_ferro`) — asymmetry is the pump, not strength (`valve_needs_asymmetry`).

**Credit, first.** The mint-shaped payer for the Higgs bill exists and is not ours: it is
**cosmological relaxation** — Graham, Kaplan & Rajendran, *Cosmological Relaxation of the
Electroweak Scale*, PRL **115**, 221801 (2015), arXiv:1504.07551 — in which a scalar rolls and
**stops** where the Higgs mass takes its observed value, so the value is the attractor of a
dynamic rather than the consequence of a symmetry. Self-organised criticality is the other
mint-shaped family. The framing of naturalness as a question about which shape of answer to
expect, and near-criticality as an organising principle, is **Gian Giudice's** (*Naturally
Speaking*, 2008; *The Dawn of the Post-Naturalness Era*, 2017). **Nothing in this chain is our
mechanism.** What is ours is the reason for the preference, and it is a wager.

**Why this is genuinely discriminating.** A superpartner discovery is evidence *for* the
guardian picture and *against* ours, in a way that no other observation in this document is.
The two pictures make opposite predictions about the existence of a new state whose entire job
is to cancel. This is the one place where the framework's shape-preference is tested against
the field's default answer rather than against nothing.

| | |
|---|---|
| **Prediction** | No weak-scale symmetry partner completing the Higgs cancellation. If the Higgs bill has a payer, it is a relaxation-type attractor, with that family's own signatures: a light scalar mixing with the Higgs, an axion-like sector, and an inflationary epoch long enough to scan. |
| **KILL (disfavours us, favours the guardian)** | Discovery of a stop, gluino, higgsino, or any twin-Higgs-class partner at the HL-LHC or a successor, at a mass and coupling that actually completes the cancellation. |
| **KILL (kills the chain outright)** | A demonstration that the two unprotected dials are **not** two of a kind — e.g. an accepted derivation of `Λ` from something that leaves `m_H²` untouched, or the adoption of Bardeen's reading (FERMILAB-CONF-95-391-T) under which classical scale invariance broken only logarithmically **is** a protection. The drift audit registers that dissent **in advance** and notes it would shrink the unprotected class to a single member — at which point "the two unpaid bills" is one bill and the chain's premise is gone. |
| **Instrument** | ATLAS, CMS (HL-LHC). For the relaxion side: NA62, Belle II, beam-dump/SHiP-class searches for light scalars and ALPs; equivalence-principle and fifth-force tests (MICROSCOPE successors) for a light mediator. |
| **Who is running it** | Already running. HL-LHC data 2029→2041. |
| **Discriminating?** | **YES** — and it is the only fundamental-physics chain here of which that is true without qualification. |
| **Earned, or retrodiction dressed as prediction?** | **Both, separably.** The *sort* (which dials are unprotected) is a retrodiction and the drift audit says so unprompted. The *payer-shape preference* is genuinely forward and has never been staked. |
| **Asymmetry, stated because it cuts against us** | A continued null does **not** confirm this chain. `epistemology.md` L6 and the page's own `no-early-dark-energy` promote text both say an unexplained absence is not support. This chain can **lose** and cannot **win**. |
| **Honest weakness** | Slow, and the framework supplies no quantitative relaxion prediction of its own — it prefers a *shape* of answer. A preference for a shape is thin, and it is exactly as thin as this row says. |

---

### 3.2 Chain B — VACUUM NEAR-CRITICALITY PERSISTS

> **KILL: the SM vacuum is shown to be robustly stable, or robustly far into instability — i.e.
> it leaves the corridor.**

| | |
|---|---|
| **Prediction** | As the top mass and the Higgs self-coupling sharpen, the vacuum stays **near** the stability boundary rather than moving decisively off it. |
| **Current state [SP, drift audit §2.9]** | Buttazzo, Degrassi, Giardino, Giudice, Sala, Salvio & Strumia, JHEP **12** (2013) 089: stability to the Planck scale excluded at **2.8σ**; stability needs `M_t < 171.53 ± 0.42 GeV`; instability scale 10¹⁰–10¹² GeV. **The verdict is dominated by the top pole mass, which carries a ±0.3 GeV *theoretical* ambiguity of order Λ_QCD.** A 2.8σ verdict resting on a quantity with a definitional ambiguity that size is **soft**, and the audit says so. |
| **KILL** | A top-mass determination free of the pole-mass ambiguity that moves the vacuum to stable at > 3σ, or into a regime with a decay time short against the age of the universe. Either way the corridor reading dies. |
| **Instrument** | HL-LHC top mass (~0.2 GeV target, still pole-mass-limited); decisively, a lepton collider at the `tt̄` threshold — FCC-ee / ILC / CEPC — which measures a short-distance mass to tens of MeV and removes the ambiguity. Lattice `α_s`. |
| **When** | HL-LHC through ~2040; a threshold scan not before the 2040s. |
| **Discriminating?** | **NO.** Near-criticality is a **measured property of the SM**, not a prediction of this framework, and the framework names **no corridor width** it could be caught outside of. The connection to this repository — "near the edge is where pattern is richest" — is a **resonance**: measured [SP] in 2D Ising at 0.66 % of `ln 2` and in 3D φ⁴ at 0.023 %, in documents whose own §0 says they say nothing about the Higgs. |
| **Earned?** | **Retrodiction.** Ranked 11 for that reason. |
| **What would make it discriminating** | Derive, from the rent clause, a *width* — how near is near — before the measurement improves. Absent that, this chain can never be more than consistent. |

---

### 3.3 Chain C — THE LEPTON PHASE RUNS NEAR ITS CEILING

> **KILL (staked on the page): DUNE / Hyper-K pin the used fraction of the Jarlskog ceiling and
> exclude ≥ 0.9 at 3σ. Calibration: δ ≈ 200° gives fraction ≈ 0.34 and kills it outright.**

| | |
|---|---|
| **Prediction** | The **convention-free ratio** — measured leptonic CP asymmetry divided by the largest value the measured mixing angles permit — is ≥ 0.9. |
| **What is proved underneath** | `abs_jarlskog_le_max` and the vanishing at the alignment poles — pure trigonometry, Jarlskog's invariant (PRL **55**, 1039, 1985) **[P-model]**. And `Core/FlavorBridge.lean`: on one exhibited family of three-bit states wearing the Jarlskog shape, the whole-only share has the exact closed form `((1+J)/2)log(1+J) + ((1−J)/2)log(1−J)`, is invisible to every pair reading, and vanishes **iff** the CP coordinate does **[P-model]**. That file's own §SCOPE says it in capitals: it is a **model bridge**, nothing derives it from a Lagrangian, and reading it as support for a flavour claim is a misreading. |
| **KILL** | As staked. Separably, the quark leg dies if sharpened γ-angle measurements exclude ≥ 0.9 at 3σ from the quark side alone. |
| **Instrument** | DUNE, Hyper-Kamiokande; JUNO for the ordering; T2K + NOvA joint fits meanwhile; LHCb / Belle II for the quark leg's γ. |
| **When** | Hyper-K data from the late 2020s, DUNE through the 2030s; the page's own "roughly a decade" is right. |
| **Discriminating?** | **Partly, and less than it looks.** Under a flat prior on δ the band \|sin δ\| ≥ 0.9 has prior ≈ 0.29, so a confirmation buys a likelihood ratio of about **3.5** — real, and small. The page states the double coincidence at ~8 %, one in twelve, and says it makes the wager *for its kill, not its odds*. |
| **Whose hypothesis is it?** | **Not ours.** Maximal CP violation is Fritzsch, and Fritzsch with Xing, for the quark table; μ-τ reflection symmetry (Harrison & Scott, 2002) realises it exactly for the lepton table and predicts the phase at the top. Ours is the **arrival route** and the composition with the proved ceiling. |
| **THE INTERNAL COMPETITION, and it is not on the page** | `neutrino-anarchy` [W] predicts δ **generic** — wherever a random table puts it. `phase-at-ceiling` [W] predicts δ in a **29 % sub-region** of that. They are not contradictory, but **confirming anarchy does not confirm phase-at-ceiling, and a mid-range δ (say 200°) leaves anarchy standing while killing phase-at-ceiling.** DUNE is the named decider for both, it cannot confirm both as *predictions*, and the page does not say so. **This is a page-level finding of this document.** |
| **The measured attempt already made, and it came back null** | The Dalitz campaign [SP] built the measurable face — the whole-only share of two binarised Dalitz coordinates and the CP tag — and excluded a whole-only CP asymmetry of **ε ≥ 0.03 at 95 %** on `B± → π±π⁺π⁻` (49 010 candidates), with the instrument's **pairwise blindness demonstrated rather than assumed** (a 20-point pair-level CP asymmetry injected and confirmed present at +0.1995 ± 0.019 while the whole-only reading did not move). That null does **not** kill this chain — different observable — but it is the framework's one attempt to make the flavour reading measurable, and it found nothing at 3 %. |

---

### 3.4 Chain D — Λ THAWS RATHER THAN BEING EXACTLY CONSTANT

> **KILL (intensive branch, staked): DESI DR3's geometry-only reading shows a phantom past at
> 4σ, or dark energy is caught clumping at any scale.
> KILL (extensive branch, staked): the crossing lands outside `z = 0.59 ± 0.03`, including no
> crossing at all; or the void ISW signal comes in at the strong, clumpy level.**

| | |
|---|---|
| **Current state [SP]** | DESI DR2 (arXiv:2503.14738): thawing quadrant (`w₀ > −1`, `w_a < 0`) at **2.8–4.2σ** depending on the supernova set, and the collaboration states no combination exceeds 4.2σ. The dispute is live and reported: Efstathiou 2025 (MNRAS **538**, 875) attributes the DES-SN5YR pull to a pipeline systematic; Vincenzi et al. 2025 rebut. Unresolved. |
| **Instrument** | DESI DR3 geometry-only (no supernovae — this is the decider both branches point opposite ways on); Euclid; LSST; Roman. |
| **When** | DR3 within roughly a year of writing. |
| **Discriminating at chain level?** | **YES, for each branch separately.** The intensive branch's *no phantom, ever* is a genuine prohibition with a theorem behind the model quantity (`Core.Intensive` plus the every-dimension contraction result). The extensive branch's `z = 0.59 ± 0.03` is a sharp number frozen in advance. |
| **Discriminating at FRAME level?** | **NO.** Both branches are on the page. The frame cannot lose the duel. Whichever branch survives, its survival is **not support for the frame**, and this document says so where the page does not. |
| **Earned?** | The extensive branch is **branded post-hoc in our own record**: the choice to sum a grand total instead of the disciplined per-unit reading was made *after* seeing that the total lands near the DESI number, and it goes against the page's own stated discipline. The intensive branch is currently **disfavoured** at 2.5–3σ against DR2. |
| **The mechanism leg is worse than the shape leg** | `precedent-is-bits` carries **two fired kills** on this leg: the self-consistent Landauer price for the bits is **3–5 orders of magnitude too small** (the published agreement comes from multiplying one system's bit count by another system's temperature), and the maintenance-flow reformulation's waste heat exceeds the total energy of all matter by 6.5× per Hubble time and **violates COBE/FIRAS by 8.8 orders of magnitude**. The shape came out right; the shape is a residual. |

---

### 3.5 Chain E — NO EARLY DARK ENERGY

> **KILL (staked): a confirmed early-dark-energy component of order several percent of the
> energy density around recombination, robust and agreed across Planck, ACT, SPT and their
> successors — not favoured by one dataset.**

| | |
|---|---|
| **Prediction** | Dark energy is the running total of structure's coordination, so it can do essentially nothing before structure exists. There is **no room** for an EDE burst, and therefore EDE cannot be the resolution of the Hubble tension. |
| **Current state** | EDE is not robustly detected; the careful analyses find it disfavoured and unable to resolve the tension, with any early burst bounded to a few percent (Hill, McDonough, Toomey & Alexander 2020, arXiv:2003.07355). ACT has shown a mild pull the others do not confirm. |
| **Instrument** | Simons Observatory (producing now), CMB-S4, SPT-3G, ACT final; plus any successful EDE fit to a Hubble-tension resolution. |
| **When** | SO results through the late 2020s; CMB-S4 in the 2030s. |
| **Discriminating?** | **YES, qualitatively — and it is the cleanest cosmological chain the page carries.** It points **against** a fix many people want to work; it is **unhedged** (there is no rival branch on the page that wants EDE); and it does not depend on which dark-balance branch survives DR3. |
| **Earned?** | **Earned.** It is a clean consequence of the frame, stated to be shot down. |
| **Honest weaknesses, both stated by the claim itself** | (i) The frame supplies **no quantitative ceiling** on any pre-structure fraction, so the forbiddance is qualitative — and the claim's own promote text names deriving that ceiling as the price of promotion. (ii) A continued absence of EDE is **not support**; the claim says this in its own words, which is the correct standard and is applied here to Chain A as well. |

---

### 3.6 Chain F — THE DRIFT ORDERING (protected dials hold, unprotected move)

> **KILL (staked, `law-as-habit` leg 3): a protected dial caught drifting while the unprotected
> ones sit perfectly rigid.**

**This chain has already been run, and the result is the most instructive negative in the
record.**

| | |
|---|---|
| **What was found [SP, `DRIFT_AUDIT_RESULTS.md`, rules frozen at `285a383`]** | **The kill is NOT FIRED, and it could not have fired.** First conjunct answered hard: two protected dials (α, μ), five independent methods, every one null, every one powered ≥ 4 orders beyond the frozen requirement — the tightest α bound (Filzinger et al. 2023, PRL **130**, 253001) is `2.5 × 10⁻¹⁹ /yr`, **2.9 × 10⁷ times** below the pre-registered plausible-drift scale. Second conjunct **structurally unreachable**: "pinned at exactly constant" was operationalised as \|w_a\| < 0.1 and the current constraint is ±0.58. Even in a universe where Λ is exactly constant, the audit returns "not certifiable." |
| **The correction it forced on the page** | **θ̄_QCD passes 't Hooft's criterion** — setting it to zero restores CP, and the CKM-induced contribution is ~10⁻¹⁷, so a small θ̄ genuinely stays small. The page's "tiny without visible protection" is wrong. And the mis-sorting was not harmless: filed as unprotected, the most finely held number in the Standard Model would have supplied **half of this claim's own kill for free.** |
| **Discriminating?** | **NO, and by construction.** The audit's own §4.2 states it: *the framework predicts an ordering, not a rate*, so it predicts from its own premises that exactly these bounds come back null — which is why a null cannot be evidence for it. Worse, every STRONG power rating depends on an **imported** yardstick (`0.1 H₀`). If a fully-amortised habit's plausible drift is many orders below `H₀` — which the framework cannot rule out, because it supplies no rate — every STRONG label collapses to UNTESTABLE. |
| **THE NAMED REPAIR, and it is the cheapest high-value item in this document** | **Derive a drift rate.** The rent clause on the model *has* a rate — the decay fraction per step. Turning it into a predicted fractional drift per year for an unprotected dial needs a mapping (steps → time, entry size → dial value) that the framework does not have and has never attempted. Without it this chain is permanently consistent-only. With it, the world's best clocks become a real test overnight. **This is a theory deliverable, not an experiment, and it costs no data.** |
| **Instrument, once a rate exists** | Optical clock comparisons (Yb⁺ E3/E2, Al⁺/Hg⁺, Sr) improving steadily; comb-calibrated quasar spectroscopy (ESPRESSO now, ANDES on the ELT); Oklo; and for Λ, DESI successors reaching \|w_a\| ≲ 0.1. The thorium-229 nuclear clock is the obvious next lever for enhanced α and `m_q/Λ_QCD` sensitivity — **named here, and not verified against the audit's frozen source list.** |
| **Sample size, stated because it is thin** | **2 × 1.** Two protected dials with scoring-quality drift records against one watchable unprotected dial. |

---

### 3.7 Chain G — WILD SHARE IN THE SKY

> **KILL: the pre-registered confirmation run returns a whole-only excess consistent with zero
> at density where the wounded run's two weaknesses collapse.**

| | |
|---|---|
| **Where it stands [O, and this is the page's one open claim]** | BOSS DR12, 568 776 galaxies, everything pre-registered, five amendments each committed before the computation it governs, blinding code-enforced, the frozen prediction git-verified untouched. Criterion **MET**. Then our own pre-registered refuter **WOUNDED** it: NGC primary scale **9.4σ → 6.0σ** and **13.5σ → 9.7σ**; the "lower bound" headline **falsified in sign** (clipping *less* gives a *larger* floor); stochasticity margin **1.3–1.7×**, not orders of magnitude, against a catalogue whose own weights are 13–15 % super-Poisson; one pre-registered **VOID** gate (§7.5 fibre-collision weight variation) discovered **never run** and not dischargeable with this catalogue's columns, on a reading sensitive to that weighting at 2.5–2.9σ; a north-south cap asymmetry of 5–9 % where the mocks predict 0.2 %. **Six sigma is a detection by most standards in this field. It is not one by ours.** |
| **What survived every correction** | The **data-versus-prediction consistency**, because the correction is common-mode (mock side falls 0.56–0.61 where data side falls 0.53–0.65); and the estimator, independently reimplemented to `9e−13` relative. |
| **Instrument** | DESI Bright Galaxy Survey at 10–100× BOSS's density (named on the page). **Unreachable from this machine** — the NERSC Spin block is unroutable, re-verified. eBOSS DR16 **is** reachable at `data.sdss.org` with data *and randoms*, and is the only reachable instrument; **Stage 0 blocker: the EZmock suite is not at the obvious SAS paths**, and without mocks there is no covariance and no forward model. |
| **Discriminating?** | **NO, as currently constructed — and this is the chain's real problem.** The forward model is MultiDark-Patchy, which is augmented Lagrangian PT plus a **calibrated stochastic bias model**, not a first-principles N-body prediction of the three-point sector. Outcome (b) — the excess-beyond-prediction branch — was **withdrawn before the unblind** for exactly that reason. So a clean confirmation confirms that standard structure formation carries order-3 structure, which nobody doubts. |
| **What would make it discriminating** | A first-principles N-body three-point prediction to sit opposite the measurement. Until then this chain can only be *consistent*, however many sigma it reaches. |
| **The priors of record** | The refuter's **corrected** significances (6.0 and 9.7σ), not the originals. The twelve harvested gates in `GATES.md` are prerequisites, and the wounded run's nine caveats are the checklist. |

---

### 3.8 Chain H — GLASS / AMORPHOUS ORDER — **the highest-yield decidable chain**

> **KILL (staked in advance, `TARGET_REGISTRY.md` §4.1): if at every template scale on the
> pre-registered grid and at the lowest temperature in the set, the share fails to exceed the
> pair-matched surrogate's share by more than the surrogate's own 5σ, then "amorphous order
> carries whole-only structure invisible to `g(r)`" is refuted at a sensitivity we measured
> rather than assumed.**

| | |
|---|---|
| **Why this and not a physics target** | The field states our niche **as its own open problem, in its own words**: the pair correlation function of a glass is essentially indistinguishable from the liquid's while the system is mechanically a solid. Nobody has asked *how much of the configuration's entropy is not reconstructible from all of its pair marginals.* |
| **Why the theorems clear it** | FACT 1 does not apply — the labels are not globally flip-invariant. FACT 2 supplies a proved denominator (`share_le_log_two`). FACT 4 does **not** bite: there is **no counting noise**, the configurations are exact particle coordinates, so there is **no valve floor at all** — the single strongest criterion in the whole target survey. |
| **The null, and it is the gate** | Reverse Monte Carlo — the standard, community-trusted generator whose documented property (it returns the most disordered structure consistent with the pair data) **is** the maxent-given-pairs statement. The reading is the difference between the real ensemble and that one, and nothing else. |
| **The hazard, named in advance** | **Template-selection minting** — selecting triples by a geometric template is a selection on the configuration, and selection is a filter. Gauged by applying the **byte-identical** template selection to the RMC surrogate, never by argument. |
| **Instrument / cost** | GlassBench (Zenodo `10.5281/zenodo.10118191`, 6.04 GB, CC-BY-4.0, host reachable). Estimator is 8-cell contingency tables. RMC is the expense: hours to a day of CPU per state point. **Weeks, on this box.** |
| **Who else is running the question** | The point-to-set programme (Biroli, Bouchaud, Cavagna, Grigera & Verrocchio, *Nat. Phys.* **4**:771, 2008), `χ₄`, and the ML "softness"/GNN programme (Bapst et al., *Nat. Phys.* **16**:448, 2020) — all attacking amorphous order with **different** observables. |
| **Prior art, convergent-adjacent, and mandatory to credit** | The **Residual Multiparticle Entropy** `Δs = s_ex − s₂` (Green 1952; Nettleton & Green 1958; Baranyai & Evans, PRA **40**, 3817, 1989; Giaquinta & Giunta, Physica A **187**:145, 1992) has been asking this question for thirty years — and is **not** our quantity, by an exact and checkable difference: RMPE can go **negative**, our share provably cannot. Torquato & Stillinger's `g₂`-invariant processes (PRE **68**:041113, 2003) state our niche as a theorem, in 2003, by somebody else. Shell's `S_rel` (JCP **129**:144108, 2008) **is** our share under another name, computed routinely as a coarse-graining loss — what has not been done is tracing it across `T_g` as a physical observable. |
| **Discriminating?** | **YES.** Our null is literally the field's own hypothesis. Both answers are informative and neither is known. |
| **The honest caveat, stated because the brief asked for new physics** | A positive here is **new condensed-matter structure, not new fundamental physics**. It is ranked first because it is the only place where a genuine discovery is both possible and cheap — not because it bears on the Standard Model. |

---

### 3.9 Chain J — THE STRETCHED-RENT PREDICTION — **a second stake after a fired kill**

> **KILL: on a second substrate, the whole-only habit's stretching exponent β does NOT match the
> substrate's own one-body β, or the rate ratio `(1/T_stretched)/Γ₁` is not 1 within
> measurement error.**

**Why this chain exists.** The QPU habit run [M] fired the page's rent-shape kill and located
the failure precisely:

| pre-registered test | staked | measured | verdict |
|---|---|---|---|
| **K-RATE** `rate_D / Γ₁` | [0.864, 1.154] | **1.072** | **passes** |
| **K-SHAPE** χ² of log\|D̂\|, slope fixed at −Γ₁, dof 9 | ≤ 26.46 | **153.1** | **FAILS** |
| **K-FAMILY** ΔAIC(exponential − power law) | ≤ +10 | **+67.1** | **FAILS** |

The unpaid decay of a whole-only habit decays at very nearly the **right rate** — within 7 %,
against a band allowing 15 % — with the **wrong shape**, resolved at enormous significance.
The post-hoc diagnosis located it in the substrate: the device's own one-body relaxation is not
exponential either (qubit 8: β = 0.872, single exponential rejected by the one-body data alone),
and the three-body correlator is stretched by nearly the same amount (β = 0.879 against a mean
one-body 0.928, with `(1/T_stretched)/Γ₁ = 1.051`).

**That diagnosis is a forward prediction, and it has never been staked.**

| | |
|---|---|
| **Prediction** | On **any** substrate, the whole-only habit's unpaid decay is a stretched exponential whose exponent β equals the substrate's **own one-body** β within measurement error, and whose stretched rate `1/T` equals the rate its own relaxation times fix. The rent clause's *rate* is substrate-independent; its *shape* is inherited from the substrate and is not a property of the ledger. |
| **KILL** | β mismatch, or a rate ratio away from 1, on a device whose one-body decay is independently characterised on the **same delay grid**. |
| **Instrument** | A second superconducting device (different vendor or different generation) and, better, a substrate with a *different* one-body shape — a trapped-ion register, where T1 is enormously long and the dominant decoherence is not TLS-driven, so the prediction is that β → 1 there and the original exponential clause is *recovered*. That is the sharpest version: **the model's failed clause should come back to life on a substrate whose one-body decay is exponential.** |
| **Cost** | ~200 QPU-seconds per arm. Months, mostly queue. |
| **Design fix already named in the record** | Measure the parts' decay on the **same delay grid** as the whole, from a \|111⟩ arm that saturates — the model-free product test came back inconclusive (+4.8σ trend, but reading 1.05 at t = 0 where it must read exactly 1) for want of exactly this. |
| **Discriminating?** | **YES — against the framework's own model clause.** This is a self-test with real teeth: it either rescues `unpaid_decays` as a rate law with a substrate-supplied shape, or it kills the clause a second time on a substrate that cannot be blamed for TLS. |
| **Earned?** | **Earned.** It is a forward prediction extracted from a *post-hoc diagnosis of a fired kill*, and it is labelled that way. Ranked 2 because it is cheap, decidable, and can lose. |

---

### 3.10 Chain K — SIGN SYMMETRY VS THE CRITICALITY LITERATURE

> **KILL: exhibit a globally flip-invariant system with a measured nonzero whole-only share.
> (This would mean the theorem or our estimator is wrong — either is fatal.)**

| | |
|---|---|
| **Prediction, forced** | Every published result of the form *"higher-order structure peaks at criticality"* obtained with O-information, PID synergy, Φ, or TSE complexity **on a sign-symmetric system** is measuring a quantity that is **provably not** whole-only structure — because on those very distributions the pairwise-blind share is exactly zero while those measures reach their theoretical **maxima**. |
| **Already demonstrated, at machine precision, inside one canonical model** | At `h = 0` in the 2D Ising model, across 49 temperatures, every lattice, every geometry: `I_C^(3) = 8.9 × 10⁻¹⁶` — while on the **same distributions** multi-information reaches `TC = 2 ln 2` and O-information reaches `Ω = ln 2`, exactly the values `Core/SignSymmetry.lean` proves for the ferromagnetic state. |
| **KILL** | As stated. Cheap to attempt and it has been attempted four times without firing. |
| **Instrument** | Recomputation. Days. |
| **Who is running it** | Nobody, in this form. The nearest published statement is Caprioglio, Mediano & Berthouze (PRL **136**, 2026, arXiv:2505.24686), which makes the adjacent point that the standard measures are nonzero on purely pairwise systems. |
| **Discriminating?** | **YES.** It says a specific body of published work is measuring something other than what its language implies, and it stakes a way of being wrong. |
| **Honest limit** | It is a claim about **instruments and a literature**, not about nature, and it must not be sold as more. It also cuts at us: it is the reason our own `meaning-is-third` wager cannot borrow support from any of that literature. |

---

### 3.11 Chain N — THE VALVE FLOOR VS PUBLISHED SYNERGY MEASUREMENTS

> **KILL: recompute published positive synergy/higher-order results on count data against a
> generatively matched null (asymmetric per-cell noise at the actual shot count, identical
> binning, identical filter) and find them UNCHANGED.**

| | |
|---|---|
| **Prediction** | A specific, identifiable class of published positives will **fall substantially** when the matched null is used: readings taken (i) on **count** data, where asymmetric noise provably mints share out of pair structure, and (ii) on **autocorrelated timeseries** against iid or Gaussian nulls. |
| **The magnitudes, already measured in this programme [M]/[SP]** | Poisson resampling alone multiplied the sky campaign's null by **×5.8** at one scale and **+50 %** at another — most of an uncorrected reading would have been counting noise wearing a costume. On human fMRI, a **weaker autocorrelation-blind null false-fires at 42σ** on the very same data where a phase-randomised null reads essentially clean — which is exactly how outside work reports nonzero human synergy. A linear filter applied **after** a pointwise map manufactured a **66σ** reading from zero real signal in the sky pilot. |
| **KILL** | As stated: they don't fall. |
| **Instrument** | Public data plus a null-construction rewrite. Weeks per target. |
| **Discriminating?** | **YES** — it predicts, in advance, which published numbers move and by roughly how much, and it is wrong if they hold. |
| **Earned?** | **Earned, and partly already paid** — this programme fired the same correction on **its own** headline result (the BOSS refuter, A9: target × 0.48–0.69, every quoted significance falling). A programme that has run the correction on itself first is entitled to run it on others. |
| **Honest limit** | This is instrument discipline, not physics, and a success here produces **negative** results in other people's work. It belongs in the ranking high on decidability and low on grandeur. |

---

### 3.12 The remaining chains, in brief

| chain | prediction | kill | instrument / when | verdict |
|---|---|---|---|---|
| **I — multi-time process tensor** | Three-time whole-only share requires memory and is capped at classical strength by causal order. | A causally ordered process measured **above** the classical cap. | IBM hardware, three-time process tomography (protocol on record, arXiv:2308.00750); months. **Positive control that must be subtracted first:** amplitude damping is flip-breaking, so `valve_needs_asymmetry` says a **Markovian** damping process mints share **without being memory**. | **CONSISTENT, not discriminating** — the bound is a theorem of standard QM and was published first. Valuable as **instrument certification**, not as new physics. |
| **M — Goodhart head-to-head** | A joint reading catches confirmed real-world gaming that added single-number targets miss. | It catches no more than the extra targets do. | A pre-registered field study on a quota / test score / waiting-time target. | **Discriminating in principle, not runnable by us.** Rank last on decidability, not on interest. |
| **L — lossy structures** | There exist structures whose maximum **cannot be restored by any upkeep** — full upkeep does not return the design state; the attainable ceiling is `0.99962 × share_max` at k = 8 [SP]. | Exhibit full upkeep restoring a design state the equivariance criterion calls lossy. | Already computed; the group-theoretic explanation is the M₁₂ stabilizer chain (7920, 720, 144, 48 at k = 11, 10, 9, 8), and by k = 8 the surviving M₈ has orbits 8+4, the decoder's cells go unequal, and the maximum becomes unreachable. | An **internal limit on the rent clause**, not a chain about nature. It belongs in the marriage: *some habits cannot be fully paid for.* Note also the withdrawn finding: the automorphism hypothesis is **neither confirmed nor inverted** by that sweep — it is **untested** by it, because the counts were a saturating random search, not group orders. |
| **Planck plumb line** | A theorem-pinned zero on a **real masked map** with a real beam and real anisotropic noise, where additive symmetric noise provably mints nothing. | The pipeline does not read inside its predicted `1/(2N)` band on the real map — in which case every subsequent field reading it produces is **ungauged**. | Files already on disk; `healpy` installed; a week. | **Instrument certification.** It pins four `GATES.md` reaches currently reading **NONE-YET** and the fifth reading "held live, not pinned". Explicitly **not** a discovery target: the share is quadratic where the field's estimator is linear, and the standard local model `Φ = φ + f_NL(φ² − ⟨φ²⟩)` is a **pointwise** map, contributing exactly zero to the b = 2 share by theorem. |

---

## 4. WHAT WOULD FALSIFY THE WHOLE PROJECTION

### 4.1 The honest answer first

**There is no single observation that takes down the marriage, because the marriage is not a
single theory.** It is a proved core about finite discrete models, plus twenty-one separable
wagers that attach that core to unconnected domains, each with its own kill. That is by design —
`epistemology.md` §2 requires kills to be **separable** precisely so that one bad result cannot
collapse the structure — and the cost of that design is exactly this: the structure has no
throat to cut.

What *can* be taken down in one stroke is the projection's **subject matter**.

### 4.2 The cheapest observation that does it

> **Show that every nonzero whole-only reading obtained on natural data to date is manufactured
> — that a defensible, generatively matched null (asymmetric per-cell noise at the actual shot
> count, plus the identical binning and the identical filter) reproduces each reading in full.**

If that holds, then the sector — proved to exist in models, proved to have capacity, direction
and a symmetry precondition — has **never been observed occupied in nature by anything**, and
every wager in §2.2 and §2.3 is a projection onto an empty set. The theorems survive untouched.
The projection loses its subject.

**Why this is the cheapest.** It is one computation per substrate, it needs no new data, it
needs no new instrument, and **it has already partly fired**: the BOSS refuter's A6 built a
deliberately over-generous null (lognormal modulation, matched variance, data's weighted shot
noise, reaching smoothed skewness +1.215 against the data's +1.093) and it **ate the entire
target and more** — ratios `−0.18` to `−1.29`. That probe is unfair by construction and the
refuter says so, which is why it scored WOUND and not KILL. But it establishes the thing that
matters here: **the pointwise channel is not bounded above by any null this programme currently
has**, and nobody has a measurement that bounds it.

**Estimated cost:** weeks. **Estimated probability it fires, on the record so far:** higher than
any physics chain in §3.

### 4.3 The second single-shot, named for completeness

**Exhibit a natural system whose description is globally flip-invariant and whose measured
whole-only share is nonzero.** By `share_eq_zero_of_signSymmetric` that is impossible if the
estimator is sound — so a credible instance means the estimator is unsound, which fouls every
reading this programme has ever taken. It is a kill aimed at the instrument rather than the
idea, and it is the only observation that would take down the *proved* layer's applicability
rather than the wagers.

### 4.4 What does NOT falsify the whole projection, stated so nobody claims it does

- **Any one wager dying.** That is the separability working, not the frame failing.
- **A superpartner discovery.** It disfavours Chain A. It does not touch the sector.
- **DESI DR3 killing either dark-balance branch.** By construction it kills at most one.
- **Continued nulls anywhere.** An unexplained absence is not support and is not refutation;
  it is an instrument statement, and L5 requires it to be reported with what the instrument
  could have detected.

---

## 5. WHAT THIS DOCUMENT IS NOT

**No new physics has been found by this programme.** Stated plainly, once, without hedging.

**Every physics-facing result to date is null, wounded, model-only, or dead.** The full list,
because a summary that omits any of these is dishonest:

| result | what it is |
|---|---|
| `adequacy` — four substrates (simulated cosmic field, mouse V1, BOSS galaxies, human fMRI) | **NULL**, each with a validated positive control. BOSS 95 % upper limit ≲ 1.5 % hidden whole-only fraction. |
| Human fMRI mild +lean (2.4–2.9σ) | Replicates on two cohorts, then **fails** the sharper autocorrelation-safe and inter-subject content-locked tests. Most parsimoniously **residual bias**. |
| Dalitz — whole-only CP asymmetry, LHCb 2011 open data | **NULL**. ε ≥ 0.03 excluded at 95 % (πππ), ε ≥ 0.06 (KKK). Two of four mode arms **void by our own defect**. |
| BOSS DR12 wild-share | **WOUNDED** by our own refuter. Not cashed. **Open.** |
| DE-ledger / `precedent-is-bits` cosmological leg | **TWO FIRED KILLS** — Landauer 3–5 dex; flow/maintenance 8.8 dex against FIRAS. |
| QPU rent shape | **KILL FIRED.** Rate survives at 7 %; exponential shape rejected at χ² = 153 / ΔAIC +67. |
| AI-safety deception detectors (whole-only and pairwise) | **BOTH NULL** at 8B, each with a firing control (z = −306, z = +32). |
| LLM order-3 size | Real but **GPT-2-specific** (~2 %); OPT's is tied-activation artifact. |
| Drift audit | Kill **NOT FIRED** and **unfireable** on its second conjunct. θ̄ reclassified against us. |
| `pi-return`, `e-upkeep`, `ledger`, `gravity-audit`, `tsvf-third` | **DEAD**, kept, marked, with their killers. |
| Rent islands, primary test | **MIXED (12/20)**, verdict of record; the perfect-code flips prediction **DEAD**. |
| Rent comparison, finding 3 (automorphisms) | **WITHDRAWN** — untested, not inverted; the counts were a saturating random search. |
| Adversary-channel bench magnitude | Re-scoped: collapses **259×** under a fold boundary. The detection stands; the number belongs to the readout. |
| φ⁴ ridge, E7's largest number | **VOID** — a metastability artifact of the sampler, 10⁵–10⁶× suppressed under an aligned start. |

**The one confirmed advance prediction, in full and in proportion.** `CFT_RIDGE_RESULTS.md`
explained 2D's flat ridge as a maximum of the exact scaling ray at `L ≈ 19`. The
pre-registration then wrote down, **before the 3D run existed**, that the 3D amplitude should
*decay* at `−6β/ν = −3.109`, and that a flat 3D amplitude *"would mean the §5 explanation of the
2D flatness is wrong, and would be reported as such."* Measured over the pre-registered window
`L = 16→32`: **`−3.084 ± 0.219`** (θ=0) and `−3.177 ± 0.240` (median). That is the whole of the
support this programme has under rule 6 — **one exponent, inside a lattice model, in a document
whose own §0 says it is not a statement about the Higgs, about any physical field, or about
nature.**

**And the proportion of the effect it confirms.** At its best readable point the 3D ridge is
`3.9 × 10⁻²` percent of `ln 2`; at `L = 32` it is `5 × 10⁻⁴` percent.

**Therefore: the entire value of this document is in §3's chains that have not been run.** Not
in §1, which is a hypothesis. Not in §2, which is a hypothesis about a hypothesis. The four
things worth doing, in order of expected yield per unit cost:

1. **Run the glass campaign** (Chain H) — discriminating, cheap, kill staked, no valve floor.
2. **Derive a drift rate from the rent clause** (Chain F's repair) — a theory deliverable
   costing no data that converts the world's best clocks into a real test.
3. **Stake the stretched-rent prediction and run it on a second substrate** (Chain J) —
   cheap, and it can kill the model clause a second time or rescue it.
4. **Run the null-reproduction test on our own natural-data readings first** (§4.2) — the
   cheapest thing in this document, and the most likely to fire.

---

## 6. PROVENANCE

Built from: `CIRISOntology/Stance.lean` (46 claims — 13 proved, 6 measured, 21 wagers, 1 open,
5 dead), `CLAUDE.md`, `epistemology.md`, `GATES.md`; the Lean cores `Share`, `ShareK`,
`ThirdCap`, `SignSymmetry`, `Creation`, `Valve`, `HammingCap`, `BellCeiling`, `FlavorBridge`,
`Maintenance`, `Flavor`, `Temporal`, `Third`, `Coordination`, `Intensive`; and the campaign
records `PHI4_RIDGE_RESULTS.md`, `CFT_RIDGE_RESULTS.md`, `ISING_FIELD_RESULTS.md`,
`DALITZ_RESULTS.md`, `SKY_REALDATA_RESULTS.md`, `REFUTER_RESULTS.md`,
`DRIFT_AUDIT_RESULTS.md`, `RENT_COMPARISON.md`, `RENT_ISLANDS_RESULTS.md`,
`temporal-share/QPU_HABIT_RESULTS.md`, `TARGET_REGISTRY.md`.

Everything attributed to an outside paper is taken from those records or from the Lean file
headers, which carry their own credits. Two items are **named but not verified against the
frozen source lists** and are flagged where they appear: the thorium-229 nuclear clock as a
future drift lever (§3.6), and the indicative dates for HL-LHC, DUNE, Hyper-K, CMB-S4 and
FCC-ee (§3.1–§3.5). Neither is load-bearing on any verdict.

**No stance recommendation is made here.** Nothing in this document is offered for
`Stance.lean`, and nothing in it is support for anything already there.
