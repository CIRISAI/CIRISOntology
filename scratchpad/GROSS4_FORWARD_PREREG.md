# Forward stake: the gross-four traffic share on a NEVER-SAMPLED stream (frozen 2026-08-19)

Staked BEFORE any sampling code contacts the stream. The 4+7 observation (91.4% of wild
modal traffic in Facts/Rules/Manner/Identity across 279 items from four streams) is
stream-conditioned until it predicts forward. This is the rule-6 test.

## The stream (named now, never sampled by this programme)

**Stack Exchange post revision history** (api.stackexchange.com, /posts/{ids}/revisions),
sites: superuser + english.stackexchange + diy — three sub-communities chosen for genre
spread within the stream, NONE code-dominated (code-heavy sites would test a different
prediction; excluded by name here, not after looking). Sampling: recent edited posts,
single-revision body diffs, mechanical one-clean-paragraph gate exactly as
eco_sample_wiki2.py; provenance stripped; target n = 60, VOID below 45 usable.

## Protocol (identical to the ecological runs)

Panel = BASE condition x 3 judge models via plane_annotate.py, modal label per item,
same prompt, same exclusion rules. No instrument assistance — this is a panel-traffic
measurement, the same instrument that produced the 91.4%.

## The stakes (all set before data)

1. PRIMARY: gross-four share (Facts+Rules+Manner+Identity) of modal labels lands in
   **[0.78, 0.97]** (point prediction 0.89; band = the measured per-stream range 90–97%
   widened by 2-sigma binomial noise at n=60).
2. SHARP SECONDARY (the deep-subtle invisibility): **Model + Premises together get at
   most 2 modal labels** — the two deepest kinds stay invisible in wild traffic.
3. ORDERING: the modal #1 kind is Facts or Manner (technical-prose genre prediction).

## Meanings, frozen

- All three land -> the 4+7 traffic claim gains its first forward confirmation;
  promotion of the observation to a measured stance-claim becomes eligible (steward
  decision, not automatic).
- PRIMARY fails low (< 0.78) but >= 2/3 -> the split is weaker than measured; the 91.4%
  was partly diet; the observation stays an observation with the miss recorded.
- **KILL: gross-four share < 2/3** -> the universal-traffic reading of 4+7 is DEAD as a
  stream-artifact; recorded per rule 7. (The derivational 4+7 — surfaces and depths in
  the generator — is a separate object and does not die with it.)
- SECONDARY fails (>= 3 Model/Premises modals) -> the invisibility of the deep subtles
  is diet-dependent; the M-theory rhyme loses its measured face.
- VOID: n < 45 usable, or off-taxonomy/NO-FIT modal rate > 10% (legibility failure), or
  any judge model unavailable (protocol must match the 279 baseline).

Sampler seed: 20260819. Spend cap: $0.25.
