# Stance re-frame proposal — batched for steward review (2026-08-19)

Rule of application: NOTHING here touches Stance.lean until (a) NonFactoring.lean
compiles, (b) the axiom sweep lands, (c) tonight's eigen verdicts are in — then one
batched edit, reviewed. Current tallies: 15 proved / 9 measured / 21 wagers / 1 open /
5 dead.

## R1 — NEW proved claim: `one-theorem-thrice` (pending the brick)

**headline**: Three of this page's results are one theorem — there is a kind of
quantity no collection of partial views can see; the parity bit, the CP phase, and
whether-the-past-can-be-proven are instances of one shape.
**status**: proved (witnesses: nonfactoring_parity, nonfactoring_cp_phase,
nonfactoring_record — once compiled).
**kill**: mechanized instances die only by error; the CLAIM dies if the shape fails
where the stance needs it next — the trace test (A0's two-column design) reading the
non-factoring sector as empty where the frame-relation demonstrably lives.

## R2 — NEW wager: `plus-one-is-one-bit`

**headline**: The +1 is worth exactly one bit — Record's coordinate is binary because
the causal bound caps the non-factoring temporal share at ln 2.
**basis**: the temporal third (proved, ln 2 by machine) + the Babel geometry (11 x 2);
the IDENTIFICATION is the wager.
**kill (separable)**: a stable ternary Record state in instrument or panel data; a
wild artifact whose frame-bit demonstrably needs a third value; A0's share column
reading floor on override-rich triples while kinds do all the predicting.

## R3 — RE-FRAME `no-early-dark-energy`: name it the mint's corollary

Basis text adds the mechanized skeleton: `repair_mints_from_noise` /
`percell_no_creation` — only maintenance creates share; no maintainers before
structure, therefore no balance. Status unchanged (wager). Kill unchanged (early-DE
confirmed across experiments). This is cohesion, not promotion.

## R4 — RE-FRAME `eleven-plus-one`: basis gains the mechanized structure

The scan ({7,10,11}, monotone, terminal), the stack (four rungs, modulate_idempotent,
ground_terminal), and the fit-grading 4+3+1+3 with the measured confusion graph
obeying it (all absorptions within-block; cross-block leaks are exactly the mention
channel). Status unchanged (measured). The eigen geometry joins the kill/promote
ladder per the frozen prereg's bands.

## R5 — NEW wager: `the-ledgers-third-name` (whole-only share = Record)

**headline**: The instrument and the taxonomy meet — the whole-only share and the
Record coordinate are one quantity at different depths.
**kill (separable from R2)**: the A0 two-column retrodiction — if the whole-only share
of (action, context, outcome) trace triples fails to concentrate on the 1,962 recorded
overrides while kind-readings succeed, the identity is decorative and dies; also dies
if the share and frame-supplied Record readings decorrelate on any substrate where
both are measurable.

## R6 — cross-reference only: `precedent-is-bits`, `dark-balance-*`

DE = the non-factoring balance (R5's cosmological face). No status change, kills
unchanged (DESI DR3, 2027). One added sentence each.

## R7 — text amendment: `ai-safety`

The practical route now has: a mechanized instrument suite (validated flags still
false by theorem), the conscience/DMA re-orientation licensed by exhaustiveness, and
the A0 retrodiction as its first falsifiable engineering test. Status unchanged.

## Not proposed (and why)

- Confront's 12 entries as a claim: HOLD until the kind assignments survive
  adversarial review — they are staked, not adjudicated.
- Any STRONG-contingent language: the pre-staked wording in STRONG_CLAIM_DRAFT.md
  governs; nothing enters early.
- Scan/Stack as standalone claims: they are structure ON the taxonomy — folded into
  R4's basis, not separate page rows.

Net if all applied: proved 15 -> 16, wagers 21 -> 23, measured/open/dead unchanged.
