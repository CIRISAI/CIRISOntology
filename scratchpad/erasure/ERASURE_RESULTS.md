# Erasure unblind — adjudication, and THE PATTERN IN THE MISSES

**One line: the closure defect predicts survival where survival is at stake (E3, p=0.001,
velocity-dominant against a ruler biased the other way), the work bridge appears only
near the thermodynamic floor (E4), and every miss lines up on a single axis — the
protocols were TRAINED to make the hidden fiber irrelevant, and our detector watched
them succeed.** Stakes: `ERASURE_PREREG.md`; pins: `STAGE2_FREEZE.md` (a–c).

## Adjudication against the frozen stakes

| stake | verdict | reading |
|---|---|---|
| **E1** defect exists, contracts | **HALF-PASS** | Defect enormous — +0.066…+0.097 bits/sample, CIs ≫ 0, 0.05–3.2 ms — but it does **not contract** in-window: flat ≈0.07 from 0.4 to 3.2 ms. |
| **E2** τ_c = τ_R within 2× | **VOID-not-killed** (as pre-declared possible) | No CI touches zero in the 4 ms window ⇒ τ_c > 3.2 ms = 1.57 τ_R. Through the 2.45× ruler calibration the expected reading was ≈5 ms — beyond the window. Lower bound reported; unposable here. |
| **E3** witness → survival beyond the bit | **PASS on every posable cell; pooled primary flawed by design** | Basic_to0 +0.0485 [0.0265, 0.0714], Basic_to1 +0.0735 [0.0480, 0.1012], both p=0.001. The declared pooled primary (+0.0064) mixed cells with 1–6 failures in ~4500 trajectories — structurally nothing to predict; recording that as MY design defect, not rescuing it. |
| **E3 velocity sub-claim** | **PASS, against the ruler's bias** | vel +0.061/+0.089 vs pos +0.002/+0.0001 in Basic — overwhelming velocity dominance, on a ruler the gauge showed is biased AGAINST velocity. The underdamped signature is real and large. |
| **E4** W monotone in initial \|v\| | **NOT PASSED, NOT KILLED** (1 of 4 protocols; kill needed 0) | Monotone + significant in **both OptSingle cells** (3.50→3.89, CI [0.08, 0.72]; 3.43→3.90, CI [0.17, 0.78]); flat in Basic (W≈11–12 kT) and Enhanced (≈5.6); flat in OptMulti. |
| **E5** T_eff companion | **OWED** — chained streams not yet analysed. |

Sanity: the position pin's sign agreement reproduces each cell's success rate to the
third decimal — the mapping is right.

## THE PATTERN, through the misfit protocol

**Miss 1 — E3 dies exactly where failures die.** Failure counts: Basic ~250 per cell,
Enhanced 29–53, Optimized 1–6. The witness predicts survival wherever survival varies;
the optimized protocols erase so reliably there is nothing left to predict. That is not
a null — it is the detector watching **optimization manufacture closure**: a learned
protocol is precisely a controller trained to make the outcome independent of the
hidden state, and the Basic → Enhanced → Optimized ladder shows the witness's
predictive power going 0.07 → marginal → 0. The design-knowing repair
(`repair_creates_ferro`, the maintained-holonomy lesson) has a control-theory face:
**training closes the outcome view against the fiber.**

**Miss 2 — E4 appears only near the floor.** Initial kinetic energy is ~0.5 kT. At
W ≈ 11–12 kT (Basic) that is a 4 % effect and drowns; at W ≈ 3.5 kT (OptSingle, near
the underdamped-erasure floor) it is measurable and the staked monotonicity shows both
targets significant. **Rent is legible only when the gross spending approaches the
floor** — the ledger's fine entries are masked by dissipative overhead. And OptMulti —
trained for 100 consecutive erasures — is flat even at 3.5 kT: a protocol trained for
repetition is trained to be INSENSITIVE to its initial witness, and its flat
work–witness curve is that robustness, measured.

**Miss 3 — the defect does not contract on the oscillator timescale.** The matched
equilibrium-Langevin gauge reads 10× smaller gains that rise and fall inside 1 ms; the
real memory reads flat 0.07 out to 3.2 ms. The hidden sector is not oscillator phase —
it is **persistent per-trajectory heterogeneity** (post-drive non-equilibrium residue:
per-trajectory energy/drift), and the single-timescale contraction model in E1/E2
misfits the substrate. E2's successor needs a two-component model: fast oscillator
memory plus a slow heterogeneity mode.

**The one-axis summary:** every verdict — pass, miss, and void — is ordered by
*witness-relevance*: how much the fiber matters to the outcome. High (Basic): E3 huge,
E1 huge. Trained away (Optimized): E3 structurally saturated — and exactly there, E4
becomes visible because the overhead is gone. The dataset is a ladder of controllers
progressively closing a view, and the fiber machinery read off where each one sits.

## What this buys the programme

The first ground-truth contact for the bridge: **closure defect → survival** confirmed
where posable (p=0.001, velocity-dominant), **witness → work** confirmed in the
near-floor regime, both on someone else's thermodynamics experiment with pre-registered
stakes, a gauged instrument, and every miss carrying a mechanism. Not new physics — the
authors' protocols behaving as designed — but the object's vocabulary measured all of
it in one pass, which is the composition claim `OBJECT_PRIOR_ART.md` says is the only
thing on trial.
