# Chained streams — adjudication. **C3's derived law CONFIRMED as a forward prediction; C2's slow mode killed clean; C1 replicates optimization-manufactures-closure at 250×.**

Tree: `CHAINED_PREREG.md`, frozen before any chained value was read. C0 gauge: TRANSFERS.
Calibration self-check: halves within 12 % (< 20 %), C3 adjudicable.

## Verdicts

| stake | verdict | reading |
|---|---|---|
| **C1** within-erasure defect | **PASS, all protocols** | Basic/Enhanced peak +0.30–0.33 bits at 0.2 ms and CONTRACT to 0.04–0.05 by 0.8 ms — the contraction the single-erasure window could not pose. Optimized protocols: above floor but ~250× smaller (+0.0012 at 0.1 ms). |
| **C2** slow mode survives a drive | **KILL FIRES, cleanly** | m=1: Basic CI straddles 0, Enhanced touches 0, both Optimized NEGATIVE. The per-trajectory heterogeneity that read flat for 3.2 ms in single erasures **does not survive a drive** — it was within-window persistence, not trajectory identity. The two-component model loses its slow leg, as the pre-committed kill states. |
| **C3** ΔW(Q4−Q1) = ΔKE, ratio ∈ [0.5, 2] | **PASS — a rule-6 confirmation** | Basic **1.36** (ΔW 16.13 kT vs ΔKE 11.85 kT, quartile means 10.9 → 27.1, monotone over a 12-kT range); OptSingle **0.607** (1.03 vs 1.70 kT). Two of two adjudicable protocols in band; Enhanced **VOID** (665 work rows vs 605 trace rows, alignment unshipped — structural, declared at crash time, not after). |
| **C3 sub-stake** OptMulti ratio < 0.5 | **PASS — the second rule-6 confirmation, and it overshoots** | ratio **−0.081**, with ΔW CI [−0.31, −0.18] entirely below zero: the repetition-trained protocol spends *less* work on the hottest quartile, quartile means dipping at Q3. Not merely insensitive to incoming kinetic energy — it **exploits** it. Trained robustness, staked as a forward prediction, confirmed and exceeded. |
| **C4** companion | reported | corr(per-index KE, T(k)) = +0.19 / +0.04 / +0.24 / +0.14 — weak positive, no stake. |

## The two findings that go back into the object

**1. Optimization manufactures closure — now measured at TWO levels on fresh data.** The
survival-level ladder (single-erasure E3) showed witness-relevance trained away; C1 now
shows the same axis *within-erasure*: trained protocols leave ~250× less within-fiber
predictive structure in the assessment dynamics than hand-designed ones. Same row, second
independent face.

**2. The rent law holds where it was hardest, and its exception was predicted.** ΔW = ΔKE
held from 1.7 to 12 kT of dynamic range on protocols that pay their bills honestly — and
the one protocol *trained* to break it broke it in the staked direction, negatively:
the learned repeated-erasure controller harvests incoming kinetic energy. Both readings
were staked before the data; both are the programme's rule-6 currency.

## Corrections to earlier readings

The single-erasure "persistent per-trajectory heterogeneity" interpretation is **retired
by C2's kill**: the flatness was within-window only. `OBJECT.md`'s slow-mode row is
updated to carry the kill, not the speculation.
