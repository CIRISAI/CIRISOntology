# COMPOSITION-2 — verdict: **FALSIFICATION.**

Per the frozen sentence, verbatim: *"The frozen closure model failed cross-substrate
composition at arm(s) B1, B2, B3; the composition claim is recorded FALSIFIED at this
scope, and the page's wording follows `OBJECT_PRIOR_ART.md`: an unusually rigorous
synthesis, not a framework."*

S1′ was not run: with three S2 arms missed, no S1 outcome could change the verdict,
and spending quota post-verdict is not part of reaching an exit. No rescue.

## The arms

| arm | stake | reading | verdict |
|---|---|---|---|
| B1 independent sessions | both cross-defects ≤ null 99th | −0.00018 > −0.00027; +0.00074 > +0.00031 | **MISS** |
| B2 coupled halves | Δ_L→R ≥ 5×\|null\| ∧ ≥ reverse | at floor, both directions | **MISS** |
| B3 contraction | τ_c in the gauged band of τ_data | **no contraction at any lag** (τ_c = None) | **MISS** |
| **B4 K** | median divergence growth ≤ 1.05 | **K = 1.0012** | **PASS** |

B4 resolves the season's UNMEASURED row: **`Aggregation`'s non-expansiveness holds on
the real engine, K ≈ 1.001** — the horizontal-scaling condition is measured true.

## THE MECHANISM, and it is one mechanism for all three misses

**The engine is DETERMINISTIC, and the closure detector's null semantics assume
stochasticity.** On a deterministic substrate:

- **B1**: two causally disconnected sessions share the same dynamical laws and similar
  initial conditions — the hidden determinism is a UNIVERSAL common driver, so
  cross-context helps prediction without any causation. The atlas's own S2 lesson
  (closure defects read directed influence, not correlation) taken to its limit:
  determinism makes everything correlated with everything.
- **B3**: with no noise, a coarse view's fiber information never washes out — the
  defect cannot contract, at any lag. Contraction IS a stochastic phenomenon.
- **B2**: what coupling there is lives below a null that determinism has already
  poisoned.

**And the one arm that PASSED is the one INTERVENTIONAL arm.** B4 probes with a real
perturbation (the tiny second throw) instead of observing, and it read cleanly. The
causal hierarchy — intervention survives where observation fails — measured inside a
single substrate, on the same data, in one campaign.

## What the falsification buys the object

The composition claim as frozen is dead at this scope, and the boundary it found is
sharp: **the fiber/closure machinery is a theory of STOCHASTIC coarse dynamics.** The
lake had both halves already — `both_closed_iff_product` is deterministic-exact for
FULL-state views, `production_id_eq_log_degree` prices determinism's irreversibility,
and `StochasticHabit`'s R3 residue is precisely the stochastic successor — but the
frozen model carried closure's null semantics onto a deterministic substrate without a
stochasticity hypothesis, and the substrate said no. Any successor composition claim
must either (a) scope to stochastic substrates explicitly, or (b) replace the
observational independence signature with the interventional one that B4 validated.
That successor is a NEW claim requiring a NEW freeze; this one is falsified and stays
falsified.
