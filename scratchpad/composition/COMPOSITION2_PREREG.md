# Pre-registration — COMPOSITION-2: the binary successor

**2026-08-26, frozen before any instrument is extended and before any S2 value is read
(the 24000-frame engine CSVs are on disk, unopened past frame-level structure).**
Successor to `COMPOSITION_PREREG.md`, inheriting its three cashed repairs:

1. **Data-closable exits.** `CONFIDENCE` ⇔ ALL staked arms pass. `FALSIFICATION` ⇔ any
   posable arm misses. No unproven middle. VOID exists ONLY for: S1 job failing twice
   on device error, or a gauge STOP — and a VOIDed substrate voids the whole test.
2. **In-job anchors** (`hardware-bands-in-job-anchors`): every S1 band is relative to
   floors and references measured in the SAME job. No constant from a prior epoch.
3. **Realized-arrow discipline** (`comp_failure_convicts_second_leg`): no arm stakes on
   an ideal unitary's strength. The reciprocal arm uses the hop gate (`XXPlusYY`),
   whose realized bidirectional behaviour is established, not the composed-CRX channel
   S1 convicted.

**The claim on trial, unchanged:** ONE frozen model (law 2 closure signatures, law 3
contraction, `Aggregation` non-expansiveness) spans a quantum device and the classical
engine with zero per-substrate parameters. Eight arms; all must land.

## S1′ — QPU, one job, all bands in-job (floors = per-arm permutation 99th pct)

| arm | stake |
|---|---|
| **A1 idle** | both directional defects ≤ 3× floor |
| **A2 one-way** (CRX 0→1) | forward ≥ 50× floor ∧ forward ≥ 5× reverse |
| **A3 reciprocal** (`XXPlusYY(π/2)`, symmetric by construction) | both defects ≥ 10× floor ∧ asymmetry ≤ 3× |
| **A4 common-driver** (X⊗X vs none, pooled 50/50) | both directional defects ≤ 3× floor ∧ created correlation ≥ 50× its shuffle floor |

A4 passing is a REPLICATION of the atlas-derived discriminator — its second
independent hardware confirmation.

## S2′ — engine (24000-frame CSVs), gauge first

Gauge: matched synthetic AR pair (null) + one-way coupled pair (planted) at the same
estimator settings → floors, and the τ_c-ruler ratio on a known-τ series → the B3 band
is `[ratio/2.5, 2.5·ratio] × τ_data`, an in-data anchor by construction.

| arm | stake |
|---|---|
| **B1 independent** (two sessions) | both cross-defects ≤ 3× gauge floor |
| **B2 coupled halves** | Δ_{L→R} ≥ 5× floor ∧ Δ_{L→R} ≥ Δ_{R→L} (the throw entered left: influence flows from the perturbed sector) |
| **B3 contraction** | the active region's self-defect τ_c within the gauged band of the series' own velocity-autocorrelation time |
| **B4 K** | median per-step coarse divergence growth over the post-probe window ≤ 1.05 — `Aggregation`'s non-expansiveness on a dissipative pile, THE unmeasured row, staked at last |

B4 is a real risk knowingly taken: granular dynamics can be chaotic. If K > 1.05 the
mesh's non-expansive assumption falsifies on its own engine, and that is the verdict.

## Adjudication

Eight staked arms; Bonferroni is inherited inside each floor (99th pct per arm ≈
0.05/8 family-wise at 8 arms). **Verdict = CONFIDENCE iff 8/8; else FALSIFICATION.**
The verdict sentence for the page, both versions, written now:
- *Confidence:* "One frozen closure model landed eight pre-registered arms across a
  quantum processor and a classical granular engine with zero per-substrate
  parameters."
- *Falsification:* "The frozen closure model failed cross-substrate composition at
  arm(s) X; the composition claim is recorded FALSIFIED at this scope, and the page's
  wording follows `OBJECT_PRIOR_ART.md`: an unusually rigorous synthesis, not a
  framework."

No rescue. No third sentence.
