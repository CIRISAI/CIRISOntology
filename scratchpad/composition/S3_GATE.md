# S3 gate — VOID, declared

`data.npz` (Zenodo 19705797) carries per-trajectory WORK only (10,000-entry W arrays
per protocol condition, λ endpoints, τ_p) — **no position time series**. The gate
required both. S3 is VOID; the overdamped-corollary prediction (law 5) goes untested
in this campaign and is NOT scored. Consequence, per the tree: the composition test
now runs on exactly two substrates (S1 QPU, S2 engine), the minimum — **if either
voids, the test itself is VOID, never a pass by shrinkage.**
