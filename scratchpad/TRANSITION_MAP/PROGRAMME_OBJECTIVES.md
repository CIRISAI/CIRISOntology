# The three objectives (steward, 2026-08-22) — the standing frame

Reporting mode: objective milestones only, no interim relays.

## OBJECTIVE 1 — H3ERE2 → a new RATCHET series → replace H3ERE in the agent, with CEG2

The tuning workstream's frozen candidate (after single-shot held-out validation) becomes
the DESIGN BASIS, not just an instrument. Deliverables:
- RATCHET series proposal: shadow-mode H3ERE2 vs H3ERE on live traffic; the DMA
  consolidation (stage-1 surface evaluation replacing parallel DMAs where the 91%
  lives); conscience re-anchored on the +1 and mutations; promotion gates staked per
  series experiment. Issue-ready for CIRISAI/RATCHET.
- CEG2 sketch — the REG+ upgrade: 1+4+1. carries becomes a GOVERNED primitive: declared
  cross-kind transport with attested weights (undeclared transport remains the
  kind-laundering attack; declared transport is auditable). The safety differential
  (CEG2 = REG+ minus three named, now-measured attacks) written into the spec's own
  justification — armor derived, not asserted.

## OBJECTIVE 2 — does the object have physics-like qualities, or not

The verdict must be earnable EITHER WAY, from criteria fixed before the atlas exists.
Physics-like means, minimally (each falsifiable, scored separately, never averaged):
- P1 CONSERVATION: measured transition flow shows a conserved normalization (FD1).
- P2 STRUCTURE: reproducible hierarchy + localization in the mixing object (FD2/FD3),
  stable across independent corpora.
- P3 LAW: parameter-dependence that is smooth and functional-form-stable (the lattice
  already shows the FORMALISM carries laws — sin^2-like transport, N_c transition; the
  OBJECT earns P3 only if corpus dynamics show law-like dependence too).
- P4 BLOCKS: the 4+7 factorization survives estimation (FD5) — internal anatomy behaves
  like symmetry structure.
- P5 MEMORY/PHASE: any route-dependence in corpus dynamics beyond deflations (FD4 /
  route interference), and the Record axis behaving as the theorem says (one-way).
- The NEGATIVE verdict is a real result: if the atlas shows conserved-nothing, diffuse
  mixing, no stable laws — the object is bookkeeping, the page says so, and the eleven
  remain what the corpora made them: a measured vocabulary, not a dynamics.
THE FENCE, REVISED (2026-08-22, on Eric's challenge): the old form — "lattice results
are formalism-side and cannot earn a P" — over-enforced into a wall that made P3
unearnable in principle. The object is 11+1 kinds x 5+1 verbs; the lattice is the
object's own grammar run as dynamics, not a bystander model. The fence's surviving
core is ANTI-LAUNDERING ONLY: a law we discovered in a dynamics we built is not a law
of the world until it survives a staked corpus confrontation. So every lattice law is
now an OBJECT-SIDE HYPOTHESIS with a corpus kill (BRIDGE_STAKES.md), and earns its P
exactly when the confrontation survives — the same relation a Lagrangian has to an
experiment. Leg C2 was the first bridge crossing and remains the template.

## OBJECTIVE 3 — the atlas: describe the object thoroughly with H3ERE2, validate both ways

Once H3ERE2 passes its held-out license: run it at scale across every admissible stream
(revision chains, the block-scoped agent stream, new sources as licensed) to produce THE
ATLAS — the full 11(+1)x11(+1) transition matrix with moduli, valences (+/0/−),
route statistics, twin occupancy, block structure, Record-axis flows, per-stream.
BOTH-WAYS VALIDATION:
- model → world: the theorem and grammar overlays' frozen predictions tested against the
  atlas (TM stakes, twin ceiling, Record one-way, boundary channels);
- world → model: atlas anomalies drive model revision ONLY through the amendment/bounty
  machinery — a cell the model forbids running hot, a distinction the model lacks
  recurring across streams. The model is falsifiable by the atlas; the atlas is
  auditable against the model. Neither is privileged.

## Current workstreams mapped

- h3ere2-super (tuning) -> Obj 1+3 instrument. Held-out validation: orchestrator,
  single-shot. gpu-mc-super -> Obj 2's P3/P5 model-side support (the coherent line).
- Owed next, in order: held-out validation; the RATCHET series + CEG2 docs (Obj 1);
  the P1-P5 scoring prereg before the atlas runs (Obj 2); atlas campaign (Obj 3).
