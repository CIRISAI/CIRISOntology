# THE PHYSICS CONFRONTATION — the object vs reality, directly
# FROZEN 2026-08-22, steward-demanded ("how many 11-dimensional dynamical systems
# exhibit these behaviours?"), before any leg ran.

## The routing error this corrects, stated plainly

The A2A program judged the GRAMMARS against U(n) and both failed; the atlas program
holds the corpus dynamics but is blocked on the wild-label ceiling. Result: the MEASURED
OBJECT — the eleven with their validly-measured structure — was never directly
confronted with physics. This prereg runs that confrontation now, on structure already
in hand, respecting the standing no-gos (no derivation claims; structural comparison and
base-rate quantification only; every number named in advance).

## LEG A — THE COINCIDENCE INDEX (the steward's question, made computable)

The signature list S, frozen:
  S1 dimension 11 fixed by external data (not chosen);
  S2 a 4+7 block split with depth profile [3,2,0,2];
  S3 exactly two twin degeneracies protected by an automorphism (Klein four);
  S4 exactly one monotone (one-way) axis;
  S5 off-diagonal mixing localized in exactly 3 channels (measured: the boundary map);
  S6 a conserved normalization under dynamics;
  S7 route-memory destroyed by reading, by crowding, and by aggregation-flattening.
NULL ENSEMBLES, frozen: (a) random column-stochastic 11x11 matrices; (b) random U(11)
(Haar); (c) random two-block splits of 11 with random depth profiles; (d) random
cellular automata on 11 states. For each ensemble: the per-signature base rate and the
conjunction rate for the applicable subset, 10,000 draws, seeds 20260823+leg. THE
NUMBER: P(conjunction | generic), reported with its per-signature factorization.
KILL: if the conjunction is generic (P > 0.01 in any ensemble), the "special structure"
reading dies and is reported dead.

## LEG B — THE FLAVOR STRUCTURAL TABLE (the fermion/baryon confrontation, finally run)

Our measured invariants vs the measured flavor sector, row by row, MATCH / STRUCTURED
MISMATCH / INCOMPARABLE with numbers, nothing averaged:
  B1 hierarchy: Babel + curated mixing moduli vs the CKM Wolfenstein cascade
     (diagonal dominance exponent vs lambda-powers);
  B2 twins vs isospin doublets: our Klein-four protection vs the hidden-antiunitary
     protection theorem for two-fold degeneracies (the staked pin follow-up prediction);
  B3 anatomy: 4+7 [3,2,0,2] vs hadron multiplet decompositions (octet/decuplet
     partitions) — exact finite comparisons, decidable;
  B4 the one-way axis vs CP/T structure: Record's monotonicity vs the flavor sector's
     unitarity (expected STRUCTURED MISMATCH — carrying it is honest; the corrected
     A2A pairing applies: record<->amplitude, verdict<->rates);
  B5 localized channels: three boundary channels vs the CKM's nearest-neighbor cascade
     structure (both localized? both hierarchical? the comparison is of PATTERNS with
     staked match criteria, not vibes).

## LEG C — THE LATTICE BEHAVIOR TABLE (already-measured physics analogues)

  C1 FHP universality: CONFIRMED (the flat sector; credited, closed);
  C2 THE AAS TEST: Delta-nu(Phi) has measured period pi (phi<->pi-phi identical) and
     sin^2 shape — the Altshuler-Aronov-Spivak period-halving signature, which in
     physics arises from time-reversed loop-pair interference. STAKE: determine, from
     the frozen collision structure, whether the REG+ mechanism IS loop-pair
     interference (the three-route sectors contain conjugate route pairs) — a
     decidable model question. MATCH = mechanism-level rhyme (named, credited to AAS);
     MISMATCH = the shape is coincidental, reported so;
  C3 route-memory destruction: reading/crowding/flattening vs decoherence-by-
     measurement / thermalization / coarse-graining — classified per the corrected
     layer pairing, each labeled rhyme-or-mechanism.

## Verdict structure

No single verdict. Three legs report separately; the PHYSICS-LIKE summary table scores
P1-P5 from PROGRAMME_OBJECTIVES.md with leg results as evidence. The negative outcome
is first-class everywhere. Spend: Leg A compute only (local); Legs B/C are analysis
against sources already in the record plus named literature values. No stance change
without steward review.
