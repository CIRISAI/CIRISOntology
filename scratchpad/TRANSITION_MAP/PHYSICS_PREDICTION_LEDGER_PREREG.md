# PHYSICS PREDICTION LEDGER — prereg (FROZEN 2026-08-22, before any new computation)

Direction reversed on the steward's instruction: treat REG/REG+ as a physical-theory
candidate. For each REG theorem or measured law: state the physics it predicts (mapping
named), the published observation, the delta, and the delta's bin.

## The four bins (frozen)
- ENHANCEMENT: the delta identifies structure REG lacks; names the needed extension.
- CANDIDATE NEW PHYSICS: REG predicts something standard physics does not, in a
  non-degenerate regime, testable.
- MAPPING ARTIFACT: the delta dissolves when the mapping is corrected or is shown
  ill-posed (Aerts-beyond-Tsirelson class).
- DEGENERATE-LIMIT AGREEMENT (Sornette bin): REG coincides with standard QM/statistics
  in this regime; agreement is consistency, NOT evidence, and is marked so.

## Frozen conventions for the new computations
- C-CASCADE: one convention for all three objects, PROBABILITY space (confusion
  matrices are probabilities; unitary rows make |V_ij|^2 row-stochastic — the same
  estimator applies exactly). For 3x3 mixing matrices: the three upper-off-diagonal
  squared magnitudes sorted descending, two step ratios. For the corpus object: the
  sealed tier means (0.359/0.198/0.103, ratios 0.551/0.522 — already sealed in Leg B /
  UNIV-1; no recomputation).
- C-BRIDGE: the share prediction is Core/FlavorBridge.lean's exact formula
  share = ln2 − H2((1+J)/2), evaluated at the measured Jarlskog invariant of each
  sector. Small-J expansion share ≈ J^2/2 may be quoted alongside the exact value.
- Data sources: quark sector from the PDG 2026 extracts already in legb_sources/
  (retrieved and spot-checked 2026-08-22). Lepton sector (PMNS) values must be fetched
  from PDG/NuFIT and the retrieval recorded; if unfetchable this session they are
  marked [PENDING-SOURCE] and NOT computed from memory.
- No row enters the ledger without: prediction, mapping, observation+source, delta,
  bin, and either a kill or a named next instrument.

## Rows staked (computation order, before results are seen)
R1 bridge-share, quark sector: predicted share at measured J_CKM vs the Dalitz null.
R2 three-object cascade: CKM vs PMNS vs corpus under C-CASCADE — place all three on
   one axis; classify the CKM-steepening delta.
R3 maximal-CP: the standing stance wager (phase near ceiling in both tables) restated
   as a REG prediction with its existing kill (lepton phase converging low).
R4 even-harmonic returns: REG+ law vs Altshuler–Aronov–Spivak h/2e periodicity
   (Sharvin–Sharvin observation) — EXPECTED BIN: degenerate-limit agreement; the row
   exists to mark it so explicitly.
R5 conservation sectors → hydrodynamic limit (FHP → Navier–Stokes): expected
   degenerate-limit agreement, marked.
R6 twin structure: REG's two protected-then-lifted pairs vs flavor's multiplets —
   the protecting-symmetry TYPE difference (permutation automorphism vs internal
   symmetry); expected bin ENHANCEMENT or MAPPING ARTIFACT, argued not assumed.
R7 classical cap compliance: 124/124 hardware readings under ShareK cap (sealed) —
   consistency row, already measured.
R8 one-way Record axis vs unitarity+CPT: the corrected layer pairing
   (record↔amplitude, verdict↔rates); bin argued in the ledger.
