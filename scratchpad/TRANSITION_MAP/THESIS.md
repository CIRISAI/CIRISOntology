# THE OBJECT — thesis with line-by-line ties (2026-08-23)

Every clause below is followed by what grounds it: a machine-checked theorem (named,
with its file), a measurement (named, with its record), or an explicit WAGER. Nothing
is asserted without one of the three. Where physics supplied the finding, the physics
is the INSTRUMENT that built the clause, not a validation of it — the object is still
being built.

---

## I. THERE ARE ELEVEN KINDS, AND THEY ARE DERIVED

**I.1 The eleven are the exact image of a site model, not an enumerated list.**
→ PROVED `Generator.generator_image`, `Generator.generator_injective`,
  `Generator.every_site_classified` (`Core/Generator.lean`); count
  `WrongKind.basePlane_card` = 11 (`Core/WrongKind.lean`).
→ FENCE, stated in the file: theorem-GIVEN-model. Adequacy of the model is a
  world-facing measurement, not a corollary.

**I.2 Record is not generated by any site — it is a frame-relation, not a twelfth kind.**
→ PROVED `Generator.record_not_site_generated`; `FrameOrder.repairable_monotone`.

**I.3 The eleven wear four on the surface and seven in depth, in blocks [4,3,3,1].**
→ PROVED `Surface.block_cards`, `Surface.block_cards_sum`, `Surface.gross_card` = 4,
  `Surface.depth_card` = 7, `Surface.depth_counts` = [3,2,0,2] (`Core/Surface.lean`);
  `Stack.stack_card` (`Core/Stack.lean`).
→ MEASURED: the surface-carry law staked FORWARD at 0.89 and measured 0.883 on a
  never-touched stream — the campaign's rule-6 survival.

---

## II. THE OBJECT IS RIGID EXCEPT FOR TWO TWINS

**II.1 Of ~4×10⁷ relabelings, exactly four survive; grounding cuts 24 to 4.**
→ PROVED `Symmetry.aut_without_stack_card` = 24, `Symmetry.aut_with_stack_card` = 4,
  with completeness `Symmetry.aut_without_stack_complete`,
  `Symmetry.aut_with_stack_complete` (`Core/Symmetry.lean`).
→ The survivors are the two swaps Priorities↔Process and Structure↔Circumstances.

**II.2 The twins are ORDERINGS and ARRANGEMENTS — one form under one flip.**
→ WAGER (the reading). Priorities/Process = ends vs means; Structure/Circumstances =
  made vs found. Ties to I.3's block structure; not itself a theorem.

**II.3 Exact twin symmetry produces INVISIBILITY, not identity.**
→ PROVED `DarkState.twin_dark_state`, `DarkState.dark_state_decoupled`
  (`Core/DarkState.lean`) — over any commutative ring: the antisymmetric mode is an
  exact eigenvector AND is annihilated by every other row.
→ X: **Wittgenstein's aspect-seeing**, in its correct form — you cannot see both
  aspects at once. Non-simultaneity, not sameness. (This CORRECTED an earlier reading
  that leaned on degeneracy; recorded at REG_GAPS M3.)

**II.4 Broken twin symmetry has EXACTLY TWO parts: how much, and toward whom.**
→ PROVED `DefectCoupling.defect_split` (`Core/DefectCoupling.lean`):
  `tr(D²) = 2·(diagonal split)² + 4·Σ_{c∉{a,b}} (field direction)²`, for any symmetric
  coupling. Rank-≤2 structure: `DefectCoupling.defect_eq`.
→ MEASURED: the two twins carry near-identical diagonal splits (3.710 vs 3.685) yet
  decoupling defects differing 3.8× — the second dimension is real and independent.
→ X: **Trier's lexical field** — a distinction is a distinction against particular
  neighbours (REG_GAPS M2).

**II.5 Three-generation physics can carry only ONE of those two parts.**
→ PROVED `DefectCoupling.defect_three_gen_collapse`: for a symmetric 3×3 with equal
  row sums — the shape unitarity forces on |V|² — `tr(D²) = 6·(diagonal split)²`, the
  off-diagonals cancel identically.
→ MEASURED: every CKM and PMNS pair returns g_DB/|ΔS| = √3/2 exactly (12 digits).
→ This is why the object is NOT flavour-isomorphic: it carries a degree of freedom a
  3-generation unitary table structurally cannot.

---

## III. THE OBJECT IS ONE-WAY

**III.1 Record flows one way; the past can be proven or not, and that is a frame fact.**
→ PROVED `Generator.record_not_site_generated`, `FrameOrder.repairable_monotone`,
  `NonFactoring.nonfactoring_record` (`Core/NonFactoring.lean`).
→ MEASURED: Record min-flow-ratio EXACTLY 0.0000 (Leg A).

**III.2 The one-wayness has a SECOND face: the mixing is singly, not doubly, stochastic.**
→ MEASURED: row-sum spread 0.761 of the mean, against 2.5×10⁻¹⁶ for |V_CKM|².
  Sinkhorn scaling 1.892 measures the distance to a two-way ledger.
→ X: **the one-way character of reception** — every change must be read as something;
  no law returns traffic to a kind (REG_GAPS M1). One property, two instruments.

---

## IV. WHOLES DO NOT REDUCE — AND NEITHER DO THEIR LAWS

**IV.1 The founding shape: two wholes agreeing under every partial view, differing in
the quantity.** → PROVED `NonFactoring.nonfactoring_parity`,
  `nonfactoring_cp_phase`, `nonfactoring_record`, `not_computable_of_nonFactoring`.

**IV.2 NEW (M7): the object's LAWS do not survive a partition either.**
→ MEASURED: all 200 not-SSA-implied candidates at n=4 are connectedness-dependent and
  die the moment one subsystem decouples; the near-miss was literally `MMI + S(A)`.
→ SPECIFIES: the object's constraints are laws **of a connected field, not of kinds
  severally** — which is why every measured regularity is matrix-level and none is
  per-kind. Predicts: a "law of Priorities" stated without its field is malformed.

---

## V. STRUCTURE IS CONTROLLED BY DISTINCTNESS, NOT SIZE

**V.1 Low rank does NOT confine dynamics; repeated values do.**
→ PROVED `GrayAlgebra.Kmat_det_ne_zero` and its exact converse
  `GrayAlgebra.Kmat_det_eq_zero_of_not_injective` (`Core/GrayAlgebra.lean`), for every N.
→ X: **Saussure** — only differences, no positive terms. Expressive reach rests on
  non-synonymy, not on how many nominal dimensions exist (REG_GAPS M4).

**V.2 NEW (M8): hierarchy is RARE — 44 of 208 (~21%) of coherent structures are
  tree-realizable.** → MEASURED (CHORD-1, control hit exactly 44).
→ SPECIFIES: grounding is a real partial order but does NOT extend to a global
  hierarchy. **Depth is a gradient, not a foundation.** Anti-foundationalism with a number.

---

## VI. WHAT THE FIELD CANNOT SEE

**VI.1 A perturbation shared by every member is invisible to the distinction sector.**
→ PROVED `BathProjector.common_bath_zero`, with the general identity
  `BathProjector.trace_proj_eq_sum` (`Core/BathProjector.lean`) — needing neither
  orthonormality nor positivity.
→ The fourth appearance of the blindness shape, now in a physics setting.

**VI.2 The object's magnitudes nearly determine its phases.**
→ MEASURED: unistochasticity slack +0.098 against a diagonal-matched null at +0.263
  (p 2×10⁻²⁰); CKM sits at +0.0004, PMNS +0.060, generic matrices +0.472.
→ X: **the hermeneutic circle, BOUNDED** — route-dependence is real, and its capacity
  is near-minimal (REG_GAPS M5). This is the one clause that got smaller, and it made
  BS-2/BS-3 sharper by giving them a prior.

---

## VII. WHAT IS STILL OPEN, NAMED
- Whether the constants are substrate-independent (UNIV-2, running on a fresh arXiv
  substrate as of this writing; four stakes frozen before the corpus existed).
- Whether corpus route statistics carry the even-harmonic law (BS-2/BS-3) — gated on
  the agent stream.
- Whether the object's mixing structure is contextual (CBD-1, designed, unrun).
- Whether the strong-loss corner shows a genuine non-commuting s→0 / κ→∞ limit.

## STANDING RULE
When a confrontation returns a negative, the question is not "did the bridge hold?" but
"what did that tell us about the object?" — and the answer is owed to REG_GAPS with its
X before the result is filed. A negative with no entry there is an unpaid debt.
