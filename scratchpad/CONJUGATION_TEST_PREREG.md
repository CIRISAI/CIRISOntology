# The broken fit-conjugation's empirical test (frozen before items exist, 2026-08-20)

The symmetry analysis found the fit-conjugation (assertive <-> directive) broken by 4 vs 3:
the assertive block carries a strength site (Confidence) with no directive counterpart,
though Searle-Vanderveken says degree-of-strength is a component of EVERY force. Fork:
(a) a hidden directive-strength site exists — an internally generated candidate for the
exact kill `kinds-from-sites` stakes; or (b) the breaking is FORCED: epistemic strength
modulates a claim without changing what counts as TRUE; deontic strength changes what
counts as COMPLIANCE — so directive "strength" is content (Rules), not modulation.

## The test

Twelve authored items (recipe frozen here; items authored after this commit, single-span
verified mechanically, by the standing corpus recipe): institutional documents where the
ONLY change is deontic modal strength — must <-> should, shall <-> may, required <->
encouraged — with the governed action, its scope, and every other word identical. Four
domains x 3 items. No item may change WHAT is governed, only HOW STRONGLY. Panel: BASE x 3
judge models via plane_annotate.py, standard prompt, modal label per item.

## Meanings, frozen

- Modal label **Rules on >= 9 of 12**: the forced-collapse resolution (b) is CONFIRMED as
  the panel's reading — deontic strength IS content; the broken conjugation is a theorem
  about satisfaction-conditions, not an omission; `kinds-from-sites` survives its own
  internally-generated challenge and gains a sentence.
- Modal label **Confidence on >= 4 of 12**: the hidden-site prong (a) is ALIVE — the
  panel reads deontic strength as modulation, the enumeration may be missing a site, and
  the finding escalates to the steward as a live challenge to basePlane_card = 11 under
  the standing bounty's own terms. This outcome is REPORTED AT FULL VOLUME.
- Split (neither threshold): the boundary is genuinely contested — recorded as a new
  named confusion pair (Rules/Confidence), part-E corpus candidates, no verdict on the
  fork; the model-commitment resolution (b) may still be argued but only as philosophy,
  not measurement.
- VOID: any item failing single-span verification is dropped; below 9 surviving items the
  test is VOID; off-taxonomy modal on >2 items voids (legibility).

Spend cap $0.10. Scorer: modal by plurality of the 3 model votes, ties excluded per the
standing convention.
