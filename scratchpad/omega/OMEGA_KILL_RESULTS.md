# Ω-KILL — verdict: **FALSIFICATION**, at arm B3, by the frozen binary rule.

Seven of eight arms passed. The eighth missed, the miss is final, and the frozen
sentence applies with "square" for "closure model": the square failed
cross-substrate composition at arm B3; the composition claim is recorded
FALSIFIED at this scope.

## The arms

| arm | stake | reading | verdict |
|---|---|---|---|
| A1 idle | ≤ 3× floor both | 0.2× / 0.7× | **PASS** |
| A2 one-way | ≥ 50× floor, asym ≥ 5× | 298×, asym 14.0× | **PASS** — the in-job asym band survives the epoch that killed its predecessor |
| A3 hop | both ≥ 10×, asym ≤ 3× | 31× / 38×, asym 1.52 | **PASS** — the realized hop is symmetric on hardware |
| A4 common-driver | signature | 0.0×/0.0×, created 0.606 | **PASS — the discriminator REPLICATED**: second independent hardware confirmation |
| B1 sham twins | exactly 0, every frame | **0 / 1200** | **PASS** — the engine is bitwise deterministic |
| B2 pre-probe | exactly 0 | **0 / 240** | **PASS** |
| **B3 light-cone ordering** | left onset strictly first | **left = right = 240** | **MISS** |
| B4 pedestal-free K | ≤ 1.05 | 1.00444 | **PASS** |

## The miss, and its mechanism — the same defect, a third time

Both sectors "respond" at the probe frame itself: an instantaneous global jump
(pedestal split 48.4 / 115.8) that no contact wave can produce. Mechanism:
`Session::throw` → `certify_at` → global re-certification → node indices stop
naming the same grains — **the identity-instability artifact the A2 report
diagnosed, whose repair (compare by persistent grain ID, never array index) was
requirement 3 IN THE RECORD, and my instrument did not implement it.** The miss
convicts the instrument, for the third time, on the one mechanism that has never
been repaired at the engine level.

**Post-hoc diagnostic, labelled:** onset of EXCESS over the frame-240 pedestal:
left = 611, right = 1956. **The physics is left-first by 1345 frames** — the
stake was artifact-blind, not the substrate acausal. Not a rescue; the verdict
stands as staked.

## What the record now says

The square's laws passed **every arm that reached physics** — all four hardware
signatures including the replication of the atlas-derived discriminator, the
exact-zero determinism arms, and the non-expansiveness measurement (K = 1.00444,
now on an instrument whose sham and pre-probe windows read exactly zero). The
single miss is a THIRD instance of one unrepaired engine defect. Two consecutive
composition tests have now been decided not by the object's laws but by
**identity instability under re-certification** — the successor requirement is
no longer an analysis note; it is an ENGINE FEATURE (persistent grain identity),
and no further composition test should be staked on this engine until it exists.

Verdict for the page, per the freeze: falsified at this scope; the wording of
`OBJECT_PRIOR_ART.md` stands — an unusually rigorous synthesis, not a framework.
