# Pre-registration — QF-1: the emptiness rung's first measurement

**2026-08-27, frozen before the instrument touches the staked state or
family; admissible only if the audit exits 0.** The ladder's last unmeasured
rung, on the object the odd cycle already owns twice in this programme: the
C5 whose ring state is our quantum ceiling is the KCBS pentagon whose Lovász
theta is √5 (credit: Klyachko–Can–Binicioglu–Shumovsky 2008;
Cabello–Severini–Winter; Fine's theorem for the null; Abramsky–Barbosa–
Mansfield for the contextual fraction). Exact computation, no sampling —
"measured" in the sense of the flavour bridge: a MODEL reading, machine-exact,
not laboratory physics.

**The cover lesson, recorded as a refusal**: the C5 graph state's five
stabilizer GENERATORS are classically satisfiable (the all-plus assignment
gives every g_i = +1), so that cover reads CF = 0 identically and cannot pose
the rung — caught by the gauge before any state was measured. The emptiness
rung lives in the EXCLUSIVITY cover.

defects: D-UNITS (CF is dimensionless in [0,1]; the crossing is a parameter on
a frozen family), D-DET (the computation is exact and deterministic — the
"arms" are derived-prediction checks, not statistical tests; no floors), none
of the identity/epoch/gate defects touch a closed-form computation.

gauge: scratchpad/omega/gauge_qf1.log

Family-wise: three staked readings, each a closed-form check with its own
band; Bonferroni is vacuous for exact quantities but declared over the 3 arms.

| arm | stake (numeric) | witness: | posability |
|---|---|---|---|
| Q1 the lift's null | the classical lift (a mixture of pentagon independent-set assignments through the SAME pipeline) reads CF ≤ 1e-9 | witness: `lift_commutes` — the diagonal lift of a classical model has a global section by construction (Fine); a nonzero reading convicts the pipeline, not physics | gauged at 0.00e+00; the saturating pentagon model reads CF = 1.0000 through the same LP, so the pipeline CAN read nonzero |
| Q2 the rung is real | CF(KCBS state, λ=0) ≥ 0.05 | witness: `bell_ceiling` names the C5 quantum object; the KCBS value Σ⟨Π⟩ = √5 > 2 says the noncontextual polytope is left, and CF > 0 is its emptiness reading — the first measurement of the gluing rung in this programme | the Fine null reads 0 and the saturating model reads 1: the quantity spans its range through the real LP |
| Q3 the derived crossing | the CF zero-point on ρ(λ) = (1−λ)ψψ† + λI/3 sits at |λ* − (√5−2)/(√5−5/3)| ≤ 0.02, the value DERIVED from the KCBS facet Σ ≤ 2 before the LP ran | witness: none (a rule-6-style derived prediction: if the depolarizing line exits the noncontextual polytope through a different facet, the arm fires and the polytope's geometry is the misfit) | planted crossing at 0.30 reads gap 0.115 and FIRES (gauge_qf1.log) |

Exits: CONFIDENCE — the emptiness rung is measured, its null is the diagonal
lift's theorem made executable, and the facet prediction lands: the OPEN rung
becomes a measured row. FALSIFICATION — Q1 convicts the instrument (fix and
re-freeze, honestly); Q2/Q3 convict the rung's framing or the facet
derivation, and the misfit names which.
