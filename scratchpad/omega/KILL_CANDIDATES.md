# KILL_CANDIDATES — the remaining genuinely-new ways the object could lose
2026-08-26. Method: (1) corpus sweep for OPEN/UNMEASURED/OWED rows; (2) structural
pass — each Ω component without a measured instance, each theorem hypothesis never
checked against a substrate. "Genuinely new" = not a re-run of a repaired defect.

## The headline finding

We are NOT running low — but the live candidates are NOT where we have been
shooting. The Ω-KILL series has hammered T_c + closure signatures three times;
of Ω = (Fib, μ, T, ∇, g), the components **μ, ∇, and g have never had a kill run
at them**, and neither has recursion depth ≥ 2. An Ω-KILL-3 that only re-runs the
repaired arms tests the REPAIRS (predicted 8-posable-for-8), not the object.
The hunt's own final line already names the fix: "Ω tested AS THE TUPLE — all
five components load-bearing."

## Tier A — live, with prior evidence a kill could actually fire

| # | candidate | what dies | why it is live | cost |
|---|---|---|---|---|
| A1 | **Out-of-sample contraction** — freeze α̂ on segment 1 of the tweezer chain, test `defect_le_alpha_pow` on segment 2 | the contraction clause (law 3) as a *predictive* law | D-BOUND-DOB proved the in-sample form CANNOT fail (unposable) — so out-of-sample is the only honest version, and the tweezer bit+fiber chain is PROVABLY non-Markov: memory is exactly what would violate a Dobrushin extrapolation | data in hand, analysis only |
| A2 | **f on QPU decay** — G∞ = q/(ε+qλ) with ε from measured T1, reset-dose axis; stake the FLOOR (multi-mode §4.2), not equality | g_c's derived rent function on a third substrate class | the QPU rent-shape kill ALREADY FIRED once (stretched-exponential, χ² 153 vs 26.5): this substrate has killed a decay-shape claim before. Floor-vs-equality is the discriminator | **QPU ~150–250 s** of the ~470–520 s left |
| A3 | **Tiers-are-Closed-views** — the oldest UNMEASURED row. Per-grain id-join now makes it posable: is there an h with v∘T = h∘v between the engine's ACTUAL declared tiers, within Aggregation's budget? | the recursion reading itself ("the stack is a taxonomy after all" — its staked kill) | three composition falsifications at the aggregate level are circumstantial evidence the tier maps may also fail; and this row is the one the whole recursion story hangs on | engine only, free |
| A4 | **Slow-mode successor** — KE-identity persistence across drives (`ke_persistence.py`), the posable form of C2's VOIDed kill | the habit ladder's persistence rung (feeds "consciousness is trusted habit") | C2's mis-fire convicted the instrument, not the scene; the question is genuinely open both ways on data in hand | data in hand, analysis only |

## Tier B — genuinely new axes, no prior either way

- **B1 μ-face on wild data**: `sigma_antitone`'s hypotheses (same kernel each step,
  AbsCont) on an empirical time-inhomogeneous chain — the μ component has ONLY Lean.
- **B2 ∇ in the ENGINE** (OWED row): realized-transport holonomy vs closure defect —
  H2's engine face; includes the untouched LOSSY side of holonomy.
- **B3 View discovery**: closure-SELECTED views vs DECLARED views. If no stable view
  exists beyond the declared ones, "privileged scale" is convicted as post-hoc naming.
- **B4 Emptiness rung** (H3): qShare vs contextual fraction on C5 — the one ladder
  rung with zero measurements anywhere, and the road toward the stance's own open
  question (a WILD quantum-sector measurement).
- **B5 Depth-2 recursion**: a tier of tiers. Composition was falsified at depth 1;
  depth 2 has never even been posed.
- **B6 Frozen-constant transport**: f(γ,δ) with all constants frozen predicting W*
  on a substrate it never saw (the βW* dose-axis IOU) — the strongest rule-6 form.
- **B7 External theorem target — lattice gauge rent** (graded response to "a run at
  millennium items"): Hodge = rhyme only (shared-lemma trap, refused); NS = adjacent
  in spirit (turbulence closure IS non-Closed views) but the prize is about the fine
  dynamics; **Yang–Mills is the one object-level overlap** — gap = transfer-operator
  spectral gap, Wilson loops = our measured holonomy substrate, g_c already prices
  W* by the gap. Micro-stake with a fast kill: state f for a lattice gauge transfer
  operator (ε = spectral gap) and convict it against known 2D/3D lattice YM numerics
  in a weekend. Survival earns one sentence; death costs nothing.

## Tier C — confirmatory or housekeeping (NOT genuinely new)

- C1 Re-running the repaired composition arms (tests the repairs; predicted 8/8).
- C2 Q8 repaired-grid re-adjudication (owed per `/tmp/repair.md` receipt; SVD fix).
- C3 A concrete cross-chart ClaimTransport instance (grammar proved, instance owed).

## Recommendation for Ω-KILL-3

Freeze it as the TUPLE test: backbone = the repaired composition arms as
premise-checks (C1, carrying the 8-posable-for-8 stake — itself falsifiable
against the prereg discipline), plus **all four Tier-A arms**, each with its own
separable kill. That buys a CONFIDENCE exit that means something: four arms with
genuine prior probability of firing, on three components of Ω that have never
been shot at. A2 is the only QPU consumer — check quota before freeze.
