# Stance update — the season's batched proposal, FOR REVIEW

*Per the standing rule (research first, then stance; no page pushes
mid-avenue), `Stance.lean` was not touched during the campaign season. The
avenue is now closed — golden Ω, QF-1, the spin-out — so this is the batched
review package. NOTHING here is applied; every item is a proposed edit to
`CIRISOntology/Stance.lean` awaiting Eric's yes/no per line. One structural
change is required first and is flagged as item 0.*

## 0. REQUIRED FIRST: extend the audit's measured-basis rule

The stance audit currently requires `measured` claims to name the predecessor
record (CIRISAI/coherence-ratchet) as basis. The season's new measured claims
are based on THIS repository's campaign record (`scratchpad/omega/*`,
provenance chains committed). Proposal: the audit accepts either basis,
each named explicitly per claim. Without this, none of items 3–7 can enter.

## Proposed new claims (headline · status · kill)

1. **The square, and its no** · proved · six machine-checked walls
   (parity, CP phase, Record, exchange sign, coherence, stabilizer);
   `nonfactoring_iff_not_closed` collapses the founding shape into the
   square's failure certificate. Kill: an in-scope invariant of views and
   dynamics that is not a graded invariant of the square.
2. **The classical world is the diagonal retract of a quantum carrier** ·
   proved (model) · `Core/DiagonalLift.lean`: Born readout Closed with the
   classical step as rate; the wall is a Hadamard. Kill: an engine
   conformance failure of the retract test at the QASM harness.
3. **The tuple at CONFIDENCE** · measured · Ω-KILL-4: five faces live-tested
   on fresh data at learned scope, 14/14 posable arms, after four
   falsifications carved the scopes (basis: this repo,
   `scratchpad/omega/OMEGA_KILL4_RESULTS.md`). Kill: any arm at these scopes
   firing on replication; the drift-epoch premise voiding across ALL faces.
4. **Conditioning, and two-dimensional privilege** · proved + measured ·
   the chart's condition number is exact (`sum_perturb_le`, attained), ke's
   χ = 1 measured flat, momx's χ collapses 0.998→0.125; the level law holds
   EARLY-TIME (4-for-4 rule-6 at f=300) and died at late time (kept, marked).
   Kill: an early-time level decorrelation on a fresh geometry.
5. **Organization is a peaked settling phenomenon** · measured · five doses
   (1.2 → 14.9 → 12.3 → 7.7 → 2.5 across warm-up 30→150; peak near 60–75);
   monotone law dead-kept-marked; HLM literature check OWED before any
   first-claim. Kill: no organization window on a second engine.
6. **The emptiness rung is measured** · measured (model-exact) · QF-1 on the
   KCBS pentagon (= the Bell-ceiling C5): Fine null exactly 0, CF = 2√5−4
   closed-form, facet crossing to 2.9e-6. Kill: an in-scope contextual model
   whose CF the LP misprices against a derived facet.
7. **The stabilizer tier, and the price of magic** · proved (model) ·
   `Core/Stabilizer.lean`; Gottesman–Knill credited for efficiency. Kill: a
   non-Clifford motion under which the tableau view certifies Closed.

## Proposed amendments to existing claims

8. The **ai-safety / composition** wording updates to the four-falsification
   arc + golden exit (the page currently reflects the pre-season state).
9. The four PHILOLOGY_BACKPASS edits (Logos→ladder sharpening; rent
   promotion; habit amendment — autonomy not persistence; the reality
   clause's do-statement repair), unchanged from their file, still pending.

## What is NOT proposed

No Millennium-adjacent claims (graded: Hodge a rhyme, YM a foothold with its
weekend-scale stake unrun, NS the other problem). No wild-quantum claim (QF-1
is model-exact; the wild instrument question stays open). No promotion of any
`measured` to `proved`.
