# The defects registry — diagnosed, unrepaired, and what each poisons

*Every diagnosed-but-unrepaired defect, with the measurement classes it poisons.
The prereg audit (`Audit/prereg_audit.py`) REFUSES any freeze whose instrument
chain touches a registry entry without citing it and stating the mitigation.
"It was in the record" is now a mechanical gate, not a post-mortem.*

| id | defect | poisons | standing mitigation | cost so far |
|---|---|---|---|---|
| **D-IDENT** | `Session::throw` → `certify_at` re-certifies the mesh; node indices stop naming the same grains | ANY engine twin-divergence or per-node comparison by array index | **REPAIRED AT THE ROOT** — the identity existed all along: `nodes.holon` (the arena is append-only; `RuntimeArena::materialize` pushes children, never removes or reorders). Instrument: `examples/idjoin_probe.rs` joins twins BY HOLON ID. Validated: sham 0/2400 with full join; the probe-frame pedestal collapsed 6500× (115.8 → 0.018) with exactly 18/21 re-certified holons dropping from the join; per-grain light-cone left 324 / right 1047. THE GATE LIFTS | RESTORATION's magnitude (3.8×), Ω-KILL B3 (a falsification) — three convictions |
| **D-EPOCH** | device calibrations drift between sessions | any hardware band frozen from a prior epoch | in-job anchors only (`hardware-bands-in-job-anchors`) | COMPOSITION-1's one-way arm (18× vs 20×) |
| **D-GATE** | compiled gates under-deliver vs the circuit as written | any stake on an ideal unitary's strength | stake on realized channels; characterize in-job; `comp_failure_convicts_second_leg` localizes | S1-recip (0.082 vs 0.50), RESTORATION (105× fiber split) |
| **D-DET** | deterministic substrates break observational closure semantics | any observational closure/defect stake without a μ hypothesis check | interventional signatures (`Probe.lean`) or verified stochasticity; `det_defect_zero_or_one` is the theorem | COMPOSITION-2 (a falsification) |
| **D-RULER-TAU** | the first-CI-touching-zero τ_c estimator reads 2.45× long on known truth | any timescale band staked through it | carry the gauged ratio into the band, or use a rate-fit estimator (unbuilt) | E2 VOID |
| **D-BOUND-DOB** | the in-sample Dobrushin bound cannot be violated by plantable non-Markovianity (regime class absorbed into α̂) | any defect ≤ α̂^m arm on stationary in-sample data | out-of-sample α̂, or engineered echo memory; else the arm is unposable | Ω-KILL A5/M1 (caught pre-spend — the gauge worked) |
| **D-EXOG** | outcome variables dominated by unshipped exogenous randomness | any prediction stake whose target ≈ an exogenous draw (e.g., end-bit ≈ random target at high success) | posability audit: exhibit the target's reducible variance before staking | C2's mis-fired kill |
| **D-SATUR** | near-perfect success rates leave no outcome variance | survival/failure prediction stakes on saturated cells | posability screen: ≥30 outcome events per cell | E3's pooled primary |
| **D-MATERIALIZE** | `certify_at` changes state CONTENT (re-certified resolution), not just indices — global ULP-to-1e-7 dust in every observable, index-free aggregates included | any exact-zero-baseline onset or divergence stake on the engine across a probe | onset thresholds RELATIVE to response scale — **BANKED**: `scratchpad/omega/onset_analyzer.py` (works on index-FREE observables only; index-paired data stays unresolvable); engine probe without re-certification still unbuilt | Ω-KILL-2 B3′ (a falsification; the physical gap was 901 frames, visible at threshold) |
| **D-CHAN-DRIFT** | the realized channel's STRUCTURE drifts across epochs, not just its calibration scalars — the CRX pair's one-wayness collapsed 88× → 14× → 2.8× over three jobs | any fixed cross-epoch threshold on a channel property (asymmetry ratios, coupling strengths) | **BANKED ×2**: `onset_analyzer.py::oneway_premise` (in-job premise, impartially retro-retracts Ω-1's A2 pass) AND `pin_circuits.py` (compiled circuits serialized to QPY, reused verbatim — the 6×-within-hours jump identifies per-job RE-TRANSPILATION as the fast component; pinning removes it, the premise check covers what ageing remains) | Ω-KILL-2 A2 (a falsification) |
| **D-UNITS** | a frozen band compared a STANDARDIZED series to a RAW-unit series (Ω-3 N3: coarse/micro read 82.6 standardized-over-raw; 0.097 in consistent units) | any band comparing two series | every band names the units/normalization of BOTH sides; gauges must plant THROUGH the real pipeline's units, never synthetic same-scale series | Ω-KILL-3 (2026-08-26) |
| **D-FRAME-ZERO** | "frame 0" meant pre-step in the band but post-step in the emitted CSV (Ω-3 N3 conjunct i: 0.443 vs a 6.1e-11 band; the pre-step fact was true at 3.0e-18) | any construction-premise band on a stepped instrument | the band names WHICH side of the first step its zero lives on, and the instrument emits that frame | Ω-KILL-3 (2026-08-26) |
