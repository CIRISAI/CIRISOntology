/-
CIRISOntology — the machine-checked core of the current maximal stance.

A clean seed. Nothing here is inherited from prior experimental campaigns; every
declaration is either proved in this repository or explicitly recorded as open.

  Core.Coordination — the one quantity, and why the instrument is a lower bound.
  Core.Provenance   — what the instrument structurally cannot tell you.
  Core.Epistemics   — the honesty gates, as machine-readable commitments.
  Stance            — the published claims, each with its status and its kill.

See `epistemology.md` for how truth is determined here and which gates are
mechanically enforced in CI, and `axiomology.md` for how values are determined.
-/
import CIRISOntology.Core.Coordination
import CIRISOntology.Core.Provenance
import CIRISOntology.Core.Epistemics
import CIRISOntology.Stance
